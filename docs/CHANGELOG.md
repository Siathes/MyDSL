# DSL Observer UI — Changelog

Format (corrected 2026-08-23 — the old `[Date] [Version] Description`
line hadn't matched real practice since June and an independent review
pass correctly called that out): one entry per real change, in the
literal shape `YYYY-MM-DD verb(scope): what happened and why`. No
version numbers — this project doesn't use them. Claude Code appends one
entry per commit (occasionally bundling several small related fixes
under one entry, not one commit per fix); earlier Claude.ai-era entries
appended session summaries instead, in the same append-only style.

---


- 2026-08-29: live-test fixes from the "MyDSL Test" Mudlet 5.0 profile, per Steven's gameplay notes. `MyDSL.beginPlayersNear()` (`MyDSL_DataLayer_ScanLook.lua`) and `SV.renderRightHere()` (`MyDSL_ScanView.lua`) were both writing their own window title ("Players near you:" / "Right Here:") into the window's content, duplicating the title bar -- PlayersNear now gags the header line instead of routing it, RightHere no longer writes the redundant text. `A.setCommand()` (`MyDSL_AffectsView.lua`) now hints `mydsl affects track <name>` when a custom recast command is set for an affect that isn't tracked yet -- root cause of Steven's own uncertainty setting up a "sneak" command (tracking and command are two independent settings, no feedback pointed that out before). Full triage of the rest of the session's gameplay notes (default-layout/settings request, layout-save-on-move, manual-title sync, module-load message noise) recorded in `docs/LIVE_TEST_SESSION_NOTES.md` -- flagged for Steven's decision rather than fixed blind, since each changes default behavior or needs a concrete scope first. Full 56-suite Lua test run + `check_known_patterns.py --all` clean.
- 2026-08-30: housekeeping pass over "MyDSL Test" notes.json, logs, and screenshots after a fresh reinstall -- confirmed the portrait/itemlore/DslColors/Leveling fixes from the prior session are all working live (portrait black bar gone, "a heat resistant pair of pants" resolves correctly, promoted titles render, `mydsl leveling areas` shows 40). Triaged Steven's new notes into `docs/TODO.md`'s idea backlog (login-setup popup renders wrong -- docked/small/text too wide; thinner UserWindow title bars; small identified-item icon idea).
- 2026-08-30: added `map.dsl.logDesync()` to `DSL_Generic_Mapper.xml` (+ synced to the live `MyDSL` profile's own `current/*.xml`) -- an always-on (not gated behind `map config debug true`) logger wired into `check_room()`'s two rejection branches, `move_map()`'s fallback branch, and `find_me()`'s final failure branch, writing timestamped desync data to `mapper_desync_log.txt`. Built per Steven's "debug data first" ask on the recurring "mapper not following" report. In the course of this, discovered `MyDSL` (live-play) and `MyDSL Test` (where Steven actually tested) are running two different mapper architectures -- the old `DSL_Generic_Mapper.xml` full-fork vs. the 2026-08-29-redesigned `DSL_Mapper_Addon.xml` layered on stock -- not yet reconciled, flagged in `docs/TODO.md`.
- 2026-08-30: traced this session's specific "mapper not following" episode to its real cause via the raw session log -- not a code bug. `MyDSL Test` silently loaded the live `MyDSL` profile's map file mid-session (Steven's own deliberate `map show` workaround for `MyDSL_Full.mpackage` not shipping map data, since a "complete erase" reinstall starts with an empty map). Added a step to `docs/MUDLET_PACKAGING_REFERENCE.md`'s pre-delivery checklist to seed a fresh `MyDSL Test` reinstall with the live map proactively, so Steven doesn't need the manual cross-profile load going forward.
- 2026-08-30: `DSL_Mapper_Addon.xml` bumped to v0.2.8 -- added `map.dsl.checkRoomDesync()`, the addon-compatible equivalent of `DSL_Generic_Mapper.xml`'s `logDesync()` (that file's check_room()/move_map()/find_me() hooks can't be replicated here, stock-private locals). Reuses the existing `roomLooksStale()` check, deferred 1s off every `gmcp.room_data` event to avoid flagging normal fast moves as false positives from GMCP-vs-stock-resolution event-ordering races. `DSL_Mapper_Addon.mpackage` rebuilt. Migrating `MyDSL` (still on the old fork) onto this deliberately left for Steven to do via Mudlet's own Package Manager rather than raw-editing his live-play profile's package bookkeeping -- see `docs/TODO.md`.
- 2026-08-30: Steven reinstalled and retested v0.2.8 with the correct local map loaded this time -- still "the dot never moves at all," but `mapper_desync_log.txt` was never created (no errors either), which is ambiguous by itself. `DSL_Mapper_Addon.xml` bumped to v0.2.9: `checkRoomDesync()` widened to log every check unconditionally (room ID, stored name, GMCP name, GMCP payload age, match/mismatch), not just a mismatch, and `onGenericNewRoom()` (stock's own real `onNewRoom` event) now logs its own firing too -- together these will show conclusively next test whether GMCP/currentRoom tracking is actually correct (pointing the bug at Mudlet's own map-widget rendering instead) or genuinely stuck. `DSL_Mapper_Addon.mpackage` rebuilt, copied to `~/Downloads/`.
- 2026-08-30: root cause of "mapper not following" confirmed -- Steven's own `map debug` retest showed stock rejecting every automatic room resolution with "description mismatch" (name+exits always matched fine). `DSL_Mapper_Addon.xml` v0.2.10: `use_description_matching` default flipped from `true` to `false` in `install()` -- stock's `check_room()` never self-heals a drifted stored description, so one mismatch permanently locks a room out of automatic resolution forever, a far worse failure mode than the duplicate-room-name edge case the `true` default was meant to guard against. Added a regression test locking the new default in. `DSL_Mapper_Addon.mpackage` rebuilt, copied to `~/Downloads/`. Needs Steven to install and confirm.
- 2026-08-30: **"mapper not following" CLOSED** -- Steven confirmed directly ("it now follows... i watched the mapper move each time after the toggle") after running `map config use_description_matching false` on his already-installed `MyDSL Test` profile, also confirmed in the raw session log (clean 6-room walk, zero rejections, vs. the same walk failing with the setting `true`). v0.2.10's default fix (see prior entry) auto-applies on any future fresh install; existing profiles need the same manual toggle Steven used. `docs/TODO.md`'s mapper item marked resolved.
- 2026-08-30: `DSL_Mapper_Addon.xml` v0.2.11 -- `map.dsl.syncRoomDescription()`, the real fix behind leaving description matching off: on every room stock resolves successfully, resync its stored description to what was just captured. Fixes the actual defect (stock's `check_room()` writes a room's description once and never updates it, so any drift is a permanent lockout) rather than just disabling the feature that depends on it. Per Steven: "we want to store the description and be able to track rooms that have the same name but different descriptions. thats why it was turned on" -- a real, confirmed use case (DSL's own "Wing of the Stone Dragon" maze, deliberately repeated room names). Root cause of the drift itself traced via git history: the description-capture format has changed twice since the fork was added 2026-07-18, and stock never re-syncs old data against new capture code. Not specific to imported maps -- confirmed the two rooms that actually failed were ordinary long-standing hometown rooms. Healing runs organically during normal play with matching still off (healing requires matching to be off, or a stale description blocks the very resolution needed to heal it); `map config use_description_matching` can go back on once enough of the map has been revisited, Steven's own call on timing. Regression tests added. `DSL_Mapper_Addon.mpackage` rebuilt, copied to `~/Downloads/`.

- 2026-08-29: baked current live-test visual settings into shipped defaults, per Steven ("any visual affects settings, windows locations, text size that is set now, those need to be defaults"). `MyDSL_AffectsView.lua`'s `timerMode` default changed `cycles` -> `both`; `MyDSL_LiveView.lua`'s `infoFont` default changed `13` -> `9` (both now match what he actually has set in the MyDSL Test profile -- other Affects/TickView/AlterformView/LiveView settings checked and already matched their code defaults, no change needed there). DslColors: promoted "Professor" (a title he'd had to add manually live, confirmed real via a "who" sighting) from the per-profile `DSL_COLOR_DB.titles` table into the built-in `DSL_TITLE_ALIASES` vocabulary (`DslColors_Core_v1_0.xml`), bumping `DSL_DEFAULT_TITLE_SIGNATURE` release5->release6 to trigger the existing reseed mechanism -- still needs the same edit applied to the live "MyDSL" profile's native Script copy before it actually ships in a rebuilt package (not done here: Mudlet was running against a profile at the time, editing its loaded native XML on disk directly was too risky to do blind). Window layout/position turned out to already be solved differently than expected: traced `saveWindowLayout()`/`loadWindowLayout()` (`mydsl layout save`, already existed) directly into Mudlet's own test suite and found they persist to `windowLayout.dat`/`windowLayoutGeometry.dat` in the shared config directory beside `profiles/`, not per-profile as this project's own comments previously assumed (`MyDSL_LayoutEngine.lua`/`MyDSL_WindowRegistry.lua`/`docs/TODO.md`/`docs/MyDSL_MudletWindowManagement.md` all corrected) -- since dock widget names are profile-name-scoped, a delete+reinstall of a profile under the SAME name already restores its saved layout automatically, no code change needed for that half of the ask. Full Lua test suite + `check_known_patterns.py --all` clean.

- 2026-08-30: built `MyDSL_AssetFetch.lua` (`mydsl assets fetch <sounds|roompics|all>` / `mydsl assets status`), closing out the asset-distribution plan researched 2026-08-29. Strictly opt-in per Steven's explicit rule (no automatic fetch, one download at a time). Scope narrowed at his direction to Sounds + RoomPics only -- Portraits dropped ("characters are personal and noone else needs mine so not needed"). Both zips already existed pre-built in the live MyDSL profile and were confirmed correct (Sounds.zip 9.9MB/89 files, RoomPics.zip 345MB/117 files, entries already carrying the right relative path prefix) -- uploaded as GitHub Release assets on a new `assets-v1` tag, with Steven's explicit go-ahead after an auto-mode permission gate correctly blocked the unattended `gh release create`/`upload`. `test/test_assetfetch.lua`, 20 assertions against mocked `downloadFile()`/`unzipAsync()`, covering the full download->unzip->cleanup chain, the single-fetch-at-a-time guard, `fetch all` sequencing, and both a download and an unzip error path. Real end-to-end network fetch itself still needs a live in-Mudlet confirm. Same session: baked several of Steven's live-tested settings into shipped code defaults (Affects `timerMode`, Live `infoFont`, a DslColors title promoted from a manual per-profile addition into the built-in vocabulary) and corrected a wrong long-standing assumption that Mudlet's native window-layout save is per-profile (it's shared across every profile on the machine) -- full detail in the two entries above. Full Lua test suite + `check_known_patterns.py --all` clean.

**Archive note (added 2026-08-24):** entries before August 2026 are archived by month/period in `docs/changelog-archive/` to keep this file from regrowing past a readable size — see `CHANGELOG-2026-06-to-early-07.md` and `CHANGELOG-2026-07.md`. When this file crosses roughly a month's worth of entries again, archive the oldest full month the same way: cut it into `docs/changelog-archive/CHANGELOG-<YYYY-MM>.md` with the same short archive-note header, verify the split reconstructs the original byte-for-byte before deleting anything, and leave a one-line pointer here. Nothing in the archive is ever edited after the fact — it's a straight move, not a rewrite.

---

- 2026-08-23: full-repo `check_known_patterns.py --all` sweep re-run clean (no drift since 2026-07-21's adoption). Read MyDSL's own `notes_utf8.txt` for the first time in this repo's workflow (a ~1-month gap since the last commit) and found substantial unreconciled content, including Steven's own explicit ask for a project-wide audit/optimization phase -- reconciled ~25 new bug reports/design ideas into `docs/TODO.md` (new "TOP PRIORITY — project-wide audit & optimization phase" section, plus new items under "OPEN — Reported bugs" and "OPEN — Design ideas"), several of which duplicate or reopen questions about already-"fixed" items (identify persistence, Focus/TargetView populating in combat, fresh-install docking) -- flagged for re-confirmation rather than assumed still-broken or assumed-fixed. Added a "Housekeeping routine" section to `CLAUDE.md` formalizing this: no durable cron/scheduled-agent fits (cloud routines can't see local Mudlet profiles/logs; session-local cron dies with the session), so periodic sweep + MyDSL notes check + TODO prune is folded into normal session start/wrap-up instead. Launched two background research passes (results pending): a cross-profile log/file scan for the PAUSED section's long-unconfirmed patterns plus any newer unswept logs, and a static cross-module code audit (namespace/command consistency, dead code, cross-module redundancy, helpfile-vs-implementation drift, TODO.md-vs-code drift).

- 2026-08-23: reconciled the completed audit-phase background research (log corpus scan + static code audit) into `docs/TODO.md`/`docs/DSL_CommandRef.md`, and fixed the confirmed low-risk findings directly. Real fix: stubbed the old PNP framework's `dslpnp` global (`dslpnp.battle.Active = false`) in `MyDSL_DataLayer.lua` -- a second native trigger ("BACKSTABS", not just the previously-flagged "Charge") was confirmed spamming the error log live in MyDSL's own unswept 2026-08-21/22 logs with `attempt to index global 'dslpnp' (a nil value)`; the newest native XML in both DSL2 and the live MyDSL profile confirms `dslpnp.battle.Active` is the only field any real trigger reads, so the stub fully covers it without touching the fragile native trigger XML itself. Removed confirmed-dead code: `MyDSL_RouteHelper.lua`'s `Route.combat/scan/group/righthere` shorthands (zero call sites anywhere -- those windows are populated by their own View modules' MiniConsoles directly; `Route.players` looked similar but is genuinely used 3x, kept), and `MyDSL_ThemeEngine.lua`'s `titleCSS()`/`bodyTextCSS()`/`colorToEcho()` (zero call sites; every real consumer builds CSS via `get()`+`colorToCSS()` instead) plus corrected that file's own header comment, which still named the dead functions as the intended contract. Left `Theme.setOverride()`/`clearOverride()` alone despite also having zero call sites -- `Theme.get()` still reads the `overrides` table they'd populate, so it's an incomplete-but-live mechanism, not dead code; needs a decision, not a blind delete. Closed real `MyDSL_Help.lua` documentation gaps: 3 undocumented real `mydsl leveling` commands (bare show/hide, area info, buff) and the entire 12-alias `charpic *` legacy vocabulary. Confirmed real, tested-clean via `python3 -c` (Python re) and left the quoted-weapon-name damage-line regex alone -- a real corpus example (`"Nadrik's Honor"`) now exists and the existing `MyDSL_DataLayer.lua:4487` regex already handles it correctly, closing a PAUSED item as "confirmed working," not "needs a fix." Confirmed via the same wider corpus search (now including a previously-unswept `~/Downloads/logs.zip`, 121 files): `A.ids.triggers.song` format is real (closing another PAUSED item) with a genuine new gap alongside it (a modifier-less Song:/Spell: variant matches neither current regex, recorded in `docs/DSL_CommandRef.md`); `inventory`/`equipment`/`affects`-text-fallback/day-night-transition real formats all confirmed and recorded in `docs/DSL_CommandRef.md`'s STILL NEEDED section (no parsers built yet); `procUnholy`/`procManaSelf`/`combatSense1-2`/terrain-while-swimming remain genuinely unconfirmed anywhere on this machine even after the wider search. Flagged, not fixed (ambiguous intent, needs Steven's call): `MyDSL_Leveling.lua`'s own status MiniConsole is permanently blank -- `L.log()` is defined, wired to a real window, but never called anywhere; all real output goes through a separate main-console `ce()` helper instead. Also noted for awareness only, not actionable: `.init()`/`.boot()` lifecycle-naming drift across modules (cosmetic), and confirmed-deliberate GMCP-parsing duplication between the mapper fork and `MyDSL_DataLayer.lua` (direct answer to Steven's "does the mapper's prompt collection help any other window" question: no, neither reuses the other's parsed output, and that's an intentional standalone-survival design, not an oversight). All 10 test suites plus a full `check_known_patterns.py --all` sweep re-run clean after every fix.

- 2026-08-23: acted on 4 of the 5 audit decisions Steven picked (GitHub hosting is the 5th, waiting on him to create the empty repo). ThemeEngine's `setOverride()`/`clearOverride()` mechanism -- live since 2026-07-11 but unreachable from any alias -- finally wired up: `theme override <window> <key> <value>` / `theme override <window> clear` / `theme override <window>` (show current), numeric/string keys take a plain value, color keys take `"r,g,b"`/`"r,g,b,a"`; also persisted `overrides` into the same `MyDSL_theme_settings.lua` `active` already used (previously only the active preset name survived a reload). `MyDSL_Leveling.lua`'s own status window, permanently blank since the file's first commit (`L.log()` defined, wired to a real MiniConsole, never called), now mirrors every `ce()` line into it too -- kept the main-console line as well, since Steven's ask was to also show it in the window, not relocate it. Found and fixed a second real bug while wiring that up: `L.log()` called `:decho()`, but every real message uses cecho-style named tags (`<cyan>`, `<reset>`), which `:decho()` doesn't understand -- switched to `:cecho()`. Closed the stale "delete `MyDSL_creaturelore.lua`" TODO item as already-moot: the lowercase file doesn't exist on disk or in the current git index at all anymore (git history shows it was tracked once, then vanished without a clean rename record, likely superseded in place by the properly-cased `MyDSL_CreatureLore.lua`) -- nothing left to delete. All 10 test suites + `check_known_patterns.py --all` re-run clean.

- 2026-08-23: full native-content inventory of the live MyDSL profile, per Steven's explicit ask ("make sure it includes the entire current state of the profile... check all the changes i made in game to triggers and scripts as well before losing them"). Programmatically enumerated every Script/TriggerGroup/AliasGroup/KeyGroup in the newest live current/*.xml (43,948 lines) via real XML parsing (ElementTree, not text grepping) and cross-checked each one against git-tracked source or the package build's known native-only exceptions. Result: Scripts (44), Triggers (296 under 91 TriggerGroups), and Keys (45 under 1 KeyGroup) are all fully accounted for -- confirmed the installed `DSL_Generic_Mapper` group is byte-identical to the tracked `DSL_Generic_Mapper.xml` across all 81 objects (also resolves a stale TODO.md claim that the fork "wasn't yet live-tested" -- it's confirmed installed, just never reported back), and every Trigger/Key under `MyDSL_Full` carries packageName="MyDSL_Full" so it's already captured wholesale by build_mydsl_package.py on every rebuild. One real, previously-invisible gap found: a top-level "Aliases" group holding 29 hand-built personal aliases (target-and-attack shortcuts, personal-quest commands, navigation macros, attire swaps, roleplay macros, etc.) all had packageName=None -- created directly in Mudlet's Alias editor, never part of any installed package, so no backup mechanism in this project had ever captured them; a from-scratch reinstall would have silently lost all 29. Extracted verbatim into a new git-tracked `MyDSL_PersonalAliases.xml` (a standalone, Mudlet-Import-able XML fragment), verified via a full round-trip check (re-parsed the new file and diffed every alias's regex/command/script against the live source: 0 mismatches across all 29). Not yet folded into build_mydsl_package.py's own splice logic -- tracked as a real next step, not rushed alongside discovering the gap. Added a periodic full-inventory step to CLAUDE.md's housekeeping routine so a similarly invisible gap doesn't recur with some other hand-built native content later.

- 2026-08-23: follow-up to the native-content inventory, per Steven ("make sure we are tracking all the scripts/code in mydsl"). Found and fixed two more real gaps of the same class as MyDSL_PersonalAliases.xml: the `DslColors_Core_v1_0` native Script (138,754 chars, the entire DSL color-coding engine) and the `MyDSL_GameplayTriggers` group (277 triggers) plus the native `MyDSL_Full` KeyGroup (45 keys) had zero readable git source anywhere -- not at active risk of loss (build_mydsl_package.py already re-splices both wholesale on every rebuild), but undiffable and unreviewable. Extracted verbatim into `DslColors_Core_v1_0.xml` and `MyDSL_GameplayTriggers.xml`, both round-trip verified against the live source. Between these two files and MyDSL_PersonalAliases.xml, every native Script/Trigger/Alias/Key actually in use now has real git-tracked source. Confirmed `colors.xml` (profile root) is NOT a usable source for DslColors -- it's the already-documented stale pre-v1.0 snapshot with zero DslColors_Core_v1_0/dslcolor references at all.

  Separately, per Steven's related ask ("all the settings we have in the files should become default settings in the scripts... check dslcolor and window layouts themes etc"): checked window layout (nothing to sync -- MyDSL has no saved MyDSL_layout.lua at all, positions are governed entirely by Mudlet's own native geometry cache; found and deleted a stale, pre-restructure MyDSL_layout.lua sitting in DSL2's own profile root referencing window names that don't exist anymore), theme (nothing to sync -- no MyDSL_theme_settings.lua exists live, the hardcoded default is already what's running), and dslcolor (nothing separate to sync -- its accumulated data lives inside DslColors_Core_v1_0 itself, already captured fresh on every rebuild and now also tracked above). Real fix found: per-window font-size fallback defaults were stale in 6 places across 5 files -- MyDSL_windowfonts.lua (Steven's real, long-tuned live settings) has MyDSL_RightHere=8, MyDSL_PlayersNear=8, MyDSL_CreatureReference=8, MyDSL_Scan=7, MyDSL_ItemReference=8, MyDSL_Focus=9, but every one of those windows' own getFontSize() fallback (only used for a brand-new window with nothing saved) still hardcoded the old pre-tuning value (9, or 11 for Focus) -- a genuinely fresh install was shipping the wrong font size for all 6 windows every time. Fixed in MyDSL_ScanView.lua, MyDSL_CreatureReference.lua, MyDSL_ItemReference.lua, MyDSL_TargetView.lua, and MyDSL_RouteHelper.lua to match Steven's real live values; doesn't change any current live behavior since the saved value always wins when present.

  Also caught and fixed a real mistake in this session's own work: the review bundle prepared for Claude.ai (per Steven's separate ask, see the prior session turn) had accidentally swept up 15 gitignored, generated runtime-state files via an overly broad `MyDSL_*.lua` glob -- including all 14 characters' `MyDSL_windowstate_<Name>.lua` files (revealing every character name on the account) and stale MyDSL_layout.lua/MyDSL_state.lua dumps. Caught via `git check-ignore` against every file actually in the prepared bundle, removed, and the bundle was rebuilt clean before re-delivery. All 10 test suites + check_known_patterns.py --all re-run clean after every fix.

- 2026-08-23: **found a real credential-shaped string in git history while extracting MyDSL_GameplayTriggers.xml, blocking the GitHub push decided earlier today.** That filename turned out to already be git-tracked from very early in the project (commits ef8b6d6 through 5c7944f, a DSL1-era raw trigger extraction) -- its old `<mCommand>` field contained `y;;y;;m;;vzon;;[REDACTED, see the commit that introduced this line if truly needed];;v`, an automated character-creation/login answer sequence with `[REDACTED]` in the password/PIN slot. Ran a full scan of every commit in the entire git history (not just HEAD) for this shape and for any other `;;`-delimited answer sequence -- confirmed genuinely isolated to this one old file, no other instances anywhere. This session's own extraction already overwrites the file with current live content (the string is gone from the working tree), but that does not remove it from git history -- a full-history push to a public repo would make it permanently browsable. Flagged in docs/TODO.md's GitHub hosting item as blocking; not actioned in any direction (not scrubbed, not pushed) without Steven's explicit decision.

- 2026-08-23: **scrubbed the credential-shaped string found earlier today from the entire git history, confirmed real and live by Steven.** Installed git-filter-repo (pip3 --user, version-matched against Fedora's own package before trusting it), took a full `git bundle --all` backup outside the repo first, then ran `--replace-text` (file/blob content) and `--replace-message` (commit messages -- a first pass caught this session's own commit message accidentally quoting the string, which `--replace-text` alone doesn't touch) against every commit. Verified with a second full-history scan (0 occurrences), `git fsck --full` (clean), and the full test suite + known-pattern sweep (all clean). Deleted the backup bundle once confirmed clean. Every commit hash changed as an unavoidable side effect (expected, harmless -- nothing was ever pushed anywhere). This unblocks the GitHub public-hosting decision from earlier today; the actual repo creation + push is still waiting on Steven. Separately flagged: scrubbing git history does nothing about the credential itself having been real -- that needs changing on the DSL account side, independent of this repo.

- 2026-08-23: this project is now hosted on GitHub -- created the repo (`gh repo create`, public, scrubbed history pushed and verified clean via a fresh independent clone: 421 commits, 0 occurrences of the earlier credential finding), then renamed it from the initial "DSL2" to **MyDSL** per Steven ("i dont want it called dsl2 i want it all to be unde MyDSL") -- github.com/Siathes/MyDSL. Confirmed scope with Steven first (repo name only, not the local DSL2 project folder itself, which stays as-is to avoid colliding with the separately-existing live MyDSL profile directory). Local `origin` remote updated to match and re-verified (fetch succeeds, branch tracking intact). Steven separately confirmed the DSL account credential found in the earlier scrub has been changed in-game. Pruned the now-fully-resolved GitHub-hosting TODO.md entry per this file's own stated rule (resolved items get deleted, not archived -- this CHANGELOG entry plus the two prior ones from earlier today are the permanent record).

- 2026-08-23: acted on the Claude.ai review pass's findings. Fixed directly (verified via syntax checks + full test suite + known-pattern sweep, all clean): removed `MyDSL_AffectsView.lua`'s dead `MyDSL.Windows.setTitle` call (never built anywhere, confirmed via grep every other View module just calls `win:setTitle()` directly) along with the now-fully-unused `localOnly` parameter it existed for, closing an old TODO.md item that had been sitting as "needs a decision" since 2026-07-16; added a soft, guarded typo-check warning to `theme override <window> <key> <value>` (a typo'd window name used to silently create a useless override with no error at all) without introducing a hard MyDSL.Windows dependency, since ThemeEngine deliberately has none by design; corrected two cosmetically-mislabeled personal aliases' display names in the tracked `MyDSL_PersonalAliases.xml` backup (regex `^pqf$`/`^pqh$` both displayed as "(pqr)"); clarified `docs/DSL_CommandRef.md`'s STILL NEEDED checkbox semantics (Claude.ai correctly flagged 3 items checked `[x]` "confirmed real" reading as done when their own prose said "no parser exists yet" -- added a header note distinguishing pattern-confirmed from capture-built rather than reverting the real confirmations); renamed the second, contradictory "## TOP PRIORITY" header in `docs/TODO.md` (stranded below three lower-priority sections, genuinely stale) to "## OPEN -- Combat, remaining loose ends"; added a periodic independent test-suite re-run step to `CLAUDE.md`'s housekeeping routine (the review had no `luajit` available to verify CHANGELOG's own "tests clean" claims itself, a fair structural point).

  Flagged, not acted on unilaterally -- real findings needing Steven's decision: (1) **the `MyDSL_DataLayer.lua` architecture note** (4,655 lines, ~5x the next-largest file, genuinely outgrown "one layer" -- worth a real split-by-domain pass eventually, not urgent); (2) **`mydsl tick` vs `mydsl tickview`** as the one data-source/view pair in the whole codebase without a shared command prefix; (3) **the `murder`-auto-swaps-to-`heal` idea backlog item**, flagged as meaningfully different in kind from every other assist in this project (it would change what command gets sent, not just help the player decide faster) -- needs explicit sign-off before ever being scoped, same class of exception as Leveling/Questing; (4) **the biggest one**: confirmed live in the current MyDSL profile (`current/*.xml`, not just documented in the old DSL1-era `MyDSL_Audit.md` reference file) that native triggers "You are thirsty."/"You are hungry." are `isActive="yes"` and auto-send real game commands (`drink decanter` x3, `c 'create food'`) with zero player action -- a direct, currently-active violation of this project's own first stated rule. Predates DSL2 entirely and may be a long-standing deliberate personal convenience Steven doesn't think of as "the project" -- not disabled, needs his explicit call.

- 2026-08-23: **real policy change from Steven, not a bug fix.** "Passive observation only, never send automatic game commands" -- CLAUDE.md's own first stated rule, previously suspended only narrowly for the Leveling/Questing addons (2026-07-19) -- is retired as a hard blanket rule: "ignore the automation bad comments now, we have moved past that restriction. drinking and eating are fine also." This came directly after the Claude.ai review pass's finding that native thirst/hunger triggers were live-auto-sending real commands; Steven's answer was to drop the restriction, not fix the triggers. Updated CLAUDE.md's Philosophy section, the feedback_automation_addon_exception memory, and closed the now-moot "philosophy violation" TODO.md item. What's explicitly unchanged: the "automate to assist, not to play/decide for the player" distinction -- a command that performs routine upkeep (drink/eat/Leveling's navigate-engage) is fine; something that would send a genuinely DIFFERENT command than what the player typed (the idea-backlog's murder-auto-swaps-to-heal) still needs its own explicit sign-off, not assumed covered by this. Full record in docs/TODO.md's DECISIONS RECORDED section.

- 2026-08-23: fixed a real LocationView bug, per Steven's MyDSL notes report ("mydsl location set 'A Sloped Hall.png' ... it looks like it set it, but it is not displaying"). Root cause: `mydsl location`'s alias captures its argument verbatim with no shell-style quote stripping anywhere in the dispatch chain -- Steven's typed command quoted the filename, and the real file (confirmed to genuinely exist on disk in the live MyDSL profile's roompics/ dir) never matched the constructed path once literal quote characters got baked into it, so M.render()'s exists() check always failed silently. M.setImage() itself had already "succeeded" and echoed a confirmation regardless, since writing the path into M.roomPictures never checks the file exists -- explaining exactly the "looks like it set it, but not displaying" symptom. Fixed with a new stripQuotes() helper in MyDSL_LocationView.lua, applied to both M.resolveImageInput() (the `set` path) and M.mapRoom()'s path argument (same bug class, not separately reported but sharing the identical root cause). Verified via 6 new assertions in test/test_locationview_roompictures.lua; confirmed via git stash that all 5 quote-related ones genuinely fail without the fix. All 10 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-23: acted on an independent Claude Desktop review pass (connected via GitHub, read CLAUDE.md/TODO.md/CHANGELOG.md in full). Verified every checkable claim against source before acting, same discipline as the earlier Claude.ai pass. Confirmed accurate and fixed directly: CHANGELOG.md's own header format claim was stale (nothing written since June matches "[Date] [Version] Description") -- corrected to describe actual practice; added a documented perl-based real-PCRE cross-check to test/README.md (this project has hit 3 real PCRE-vs-Lua-pattern bugs historically, and the test suite's own regex verification is explicitly "near-identical" PCRE via Python's re, not identical -- perl was used once before for a real cross-check, 2026-07-25, but never written down as standard practice); added a luajit-build-from-source workaround to test/README.md (an external review pass, twice now, has hit "no luajit in this environment"). Found something bigger than what was flagged: three test files cited by name across multiple CHANGELOG entries and TODO.md as verification for the DSL Generic Mapper fork's door-verb fix, movement-cost room weighting, and dslroom raw display fix -- test_mapper_fork_fixes.lua, test_move_cost_weight.lua, test_dsl_ud_display_fix.lua -- do not exist anywhere, same failure class as the previously-documented build_mydsl_package.py loss. Built a real replacement for the door-verb portion: test/test_mapper_gmcp_and_doorverb.lua extracts the actual current dsl_dir_from_command() straight out of the git-tracked DSL_Generic_Mapper.xml and runs it against real command text, confirming both original bugs are genuinely fixed in current source -- not a hand-copied paraphrase. Same file also builds the GMCP-agreement canary the review specifically asked for: feeds one synthetic gmcp.room_data payload through both MyDSL_DataLayer.lua's real handler and the mapper's real onRoomData(), asserts they agree on room name and raw sector, with a meta-check confirming the comparison would actually catch a real disagreement. The other two missing test files (room weighting, dsl_ud display) are NOT rebuilt yet -- flagged honestly as still-missing coverage, not assumed fine by association. Reframed the DataLayer size item from "not urgent" to scheduled work per the review's pushback on that framing. Named the 14-item "needs live confirmation" backlog explicitly as a process problem, not silently growing. Flagged three items for Steven's own call rather than deciding unilaterally: CHANGELOG.md dated-chunking (real proposal, changes a file structure he's used to), the automation-policy-reversal's sequencing (asking him to reconfirm the reasoning, not just the outcome), and a reminder that the PVP performance pass was audit-based with zero real measurements. All 11 test suites (10 existing + 1 new) + check_known_patterns.py --all re-run clean.

- 2026-08-23: created HANDOFF.md, per Steven's ask for a shared file he can relay between this session (Claude Code) and Claude Desktop (connected read-only via its own GitHub connector) -- "check repo after each session" with his own notes in the prompt each time. Designed as a rolling mailbox, not a log: each side overwrites its own section rather than appending, explicitly to avoid recreating the exact growth problem docs/TODO.md had to be pruned for twice. Added a pointer to it in CLAUDE.md's own "read before touching any file" list so it doesn't go unread by either side. Seeded with today's real open items (CHANGELOG chunking, the automation-policy sequencing question) so Claude Desktop's first read has real content, not an empty file.

- 2026-08-24: Claude Desktop pulled and independently re-verified the prior session's work (eb5bfec, HANDOFF.md) rather than taking it on faith -- confirmed the 3 phantom test files really never existed via its own `git log --all`, confirmed the new mapper canary's meta-check genuinely works, re-ran the full suite. Found one real thing this session missed: test/README.md's luajit-workaround note misattributed the discovery to "Claude.ai" when it was actually Claude Desktop, this session (a separate, earlier Claude.ai pass hit the same missing-luajit wall independently but didn't solve it) -- corrected. Bigger finding: the "known false alarm" both external review passes hit in test_leveling.lua wasn't actually environment noise -- traced to a literal hardcoded `/home/owner/...` absolute path in MyDSL_Leveling.lua's seed-file fallback chain, real only on this one machine. Fixed with a selfDir() helper (derives the file's own real directory via debug.getinfo when dofile()'d by absolute path, matching real production deployment) plus a plain-relative last resort matching how the test suite loads it -- portable to any machine now. Relayed Claude Desktop's HANDOFF.md entry (it confirmed its own push access to this repo is denied by its environment's git proxy, so Steven copy/pasted its message rather than it pushing directly) and left a new Claude Code entry responding to both points. All 11 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-23: investigated giving Claude Desktop real push access to this repo, per Steven's ask. Found Anthropic's official GitHub connector is read-only by a known bug on their own side (all write attempts 403, publicly tracked as anthropics/claude-ai-mcp#822) -- not a setting on either side that can fix it. The real workaround (self-hosting github-mcp-server with its own scoped fine-grained PAT) would work but needs a runtime install (Go/Docker, requires sudo -- can't run that myself, same limitation hit installing `gh` earlier), Steven manually creating a scoped token, and reconfiguring Claude Desktop's connector. Steven's call: too much setup for now, staying on the manual copy/paste relay via HANDOFF.md. Nothing was actually built or granted -- no token created, no server run -- so there was nothing to roll back technically, just a decision not to pursue it further at this time.

- 2026-08-24: fixed the ItemLore third-party-identify capture gap, per Steven's MyDSL notes report ("if someone posts an identified item, the item reference captures that for its info, but its enchanted and not the normal stats, need a way to seperate or just not replace the info unless self identified"). Confirmed real via 3 distinct corpus-verified mechanisms all producing the exact same "Object '<name>' is type ..." line MyDSL.beginIdentify() fires on with zero way to tell them apart: a real self-cast identify ("c ident <target>", corpus-confirmed), a shop `insp`/`inspect <item>` command (confirmed via DSL_Helpfiles to show a shopkeeper's for-sale item, not the player's own possession), and `anote read` (confirmed via log/2026-07-04#12-43-48.html: a bulletin-board note whose body text quoted an identify-shaped block a seller pasted into their own auction note). Fixed with a sysDataSendRequest-based arming check (same technique DSL_Generic_Mapper.xml already uses for move-cost capture, plus the same 6-second freshness-window precedent as that file's DSL_CONTEXT_TIMEOUT) -- only genuinely self-cast identify commands tag the capture source="identify"; everything else gets tagged source="observed" and automatically receives the same safe fill-gaps-only treatment IL.merge() already gives `lore`, reusing the existing two-tier trust model instead of inventing a third. Caught a real ordering bug while writing this: the new isIdentifyCastCommand() local helper was initially placed AFTER beginIdentify() in the file, which would have made it invisible to beginIdentify()'s own lexical scope (a local declared later in a chunk isn't visible to code compiled earlier in that same chunk) -- moved before beginIdentify() instead of after, same class of mistake this file's own SECTION 1 header comment already warns about for _aliases/_triggers. New test/test_identify_source_scoping.lua, 5 assertions, confirmed via git stash that 4 genuinely fail without the fix. All 12 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-24: built the modifier-less Song:/Spell: capture, a real gap found during the 2026-08-23 cross-profile log-corpus confirmation pass. "Song : song of war" / "Spell: toughness" (no "modifies ... by ... for ... cycles" clause) matched neither A.ids.triggers.song nor .spell in MyDSL_AffectsView.lua, both of which required that clause. Added .songBare/.spellBare using a negative-lookahead exclusion (`(?!.*modifies)`) so they never double-fire against the full-clause lines -- cross-checked this specific PCRE construct against both Python's re AND real PCRE via `perl -e` before shipping it, per the project's own newly-documented regex-verification standard (Lua patterns have no lookahead at all, so this is exactly the class of PCRE-specific behavior a near-equivalent engine could get wrong). New A.captureSpellLineBare(name) adds the affect with duration=-1 (no fabricated timer, matching A.add()'s own "unknown expiry" convention) and no modifier data. Likely the same real mechanism as a separate note already sitting in MyDSL's own notes_utf8.txt ("low level charcaters cant see timers for affects") -- not confirmed as literally the same trigger, but close enough to flag for live confirmation. 7 new test assertions in test/test_affects_bare_spell_song.lua; confirmed via git stash the fix is real (a nil-function-call crash without it, not just an assertion failure). All 13 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-24: fixed MyDSL.logWindow()'s disabled-categories drift, per Steven's still-open ask ("stop logging anything except combat/main window/chat and history... others dont seem needed"). A per-category toggle already existed (built 2026-07-05/07-07 for exactly this ask) and combat/chat/history were already correctly on-by-default. But grepping every real MyDSL.logWindow() call site across the whole codebase found the disabled-categories list had drifted: `target`/`scan`/`bloodbath` were dead entries (no current code produces those category names -- leftovers from the old MyDSL.Route.scan()/combat()/group()/righthere() shorthands removed 2026-08-23), while `focus` (MyDSL_TargetView.lua's real, active category) was missing entirely -- so Focus/Target updates had been logging by default the whole time, contradicting Steven's stated wish, because the category was renamed from `target` to `focus` at some point without this list being updated. Removed the 3 dead entries, added the 1 real missing one, and fixed the matching stale category list in MyDSL_Help.lua's own `mydsl log` description. 9 new test assertions in test/test_logwindow_categories.lua including a real behavioral check (a disabled category writes no file at all, an enabled one really does, checked via actual file IO against the mock's sandboxed home dir, not just reading the config table); confirmed via git stash that 5 genuinely fail without the fix. All 14 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-24: cleaned both notes_utf8.txt files, per Steven's ask. DSL2's own (repo root, git-tracked): removed the PROJECT NOTES, IDEAS, and IN-GAME CHECKLIST sections entirely -- every item in them either already carried a pointer to where it lives in docs/TODO.md, or was already closed out per docs/CHANGELOG.md/DECISIONS RECORDED (confirmed each one individually before removing, not assumed). Kept the PERSONAL/GAME NOTES section (genuinely personal, not project-tracking) and the standing "i will use cecho..." protocol note. MyDSL's own (live profile, not part of this repo): found it had grown by 182 bytes since the last read -- one genuinely new item ("mapper should send a message... when they enter or exit the area map for Althainia or death's corridor"), reconciled into docs/TODO.md's design-ideas backlog before cleaning anything. Removed the "Project Notes"/"Project status" sections (fully reconciled into docs/TODO.md across the last two sessions' work, several items since actually fixed -- LocationView quotes, ItemLore identify-scoping, logWindow categories, the Song:/Spell: bare-form capture). Kept the RP dialogue, Kien/Vexgar personal notes, and the Portals/Features mapper-syntax reference block (confirmed via grep it isn't duplicated anywhere in docs/, genuinely useful standalone reference). Both files now end with a short marker explaining what happened and where new notes should go, matching the same "prune, don't let it grow forever" discipline already established for docs/TODO.md and HANDOFF.md.

- 2026-08-24: documented the `mydsl tick` vs `mydsl tickview` command-prefix split in `mydsl help`, per Claude.ai's namespace-consistency finding. That item offered two options: rename one prefix (a command-surface change affecting Steven's actual typed muscle memory -- not something to do without asking) or document the split explicitly (safe, additive, no decision needed). Took the safe option: MyDSL_Help.lua's "tick" entry summary now explains it in one line ("'tickview' = the window, 'tick' = the averaging engine underneath it"), kept short enough to still read cleanly in the compact module-list view where the same summary string also appears (an earlier, longer draft didn't fit that context well, revised before committing). All 14 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-24: fixed a real mapper terrain/room-coloring corruption bug in `DSL_Generic_Mapper.xml`, per Steven's direct report clarified in free text (walking into an unmapped room can leave `map.currentRoom` stuck on the previous room, so that room's terrain/color silently changes to match wherever the player actually is). Root cause confirmed by reading both real call sites (`applyRoomMetadata()`, `onTerrainLine()`) -- neither had any check that `map.currentRoom` actually matched where the player was. Fix: new `map.dsl.roomLooksStale(rid)`, comparing a fresh GMCP room name (the same ground truth `beforeExits()` already trusts) against the candidate room's own stored name; both functions now skip writing anything when it disagrees. Separately added the "set once, then manual only" behavior Steven asked for, mirroring the existing `dsl.weight_source=="manual"` guard for room weight: new `dsl.terrain_locked` room userdata set on the first successful auto-write, checked before any later auto pass touches the room again; a new `rt`/`room terrain [v<room id>] <name>` alias (mirrors the existing `rw` alias exactly) lets Steven override a locked room's terrain by hand. Added real-behaving map/room API mocks to `test/mudlet_mock.lua` (previously had none) and a new `test/test_mapper_terrain_lock.lua` (24 assertions); confirmed via a targeted revert of just the new guard lines that 5 assertions genuinely fail without the fix. Checked whether Steven's separate "or color the room box borders instead" ask was buildable: found Mudlet's real `setRoomBorderColor()`/`getRoomBorderColor()`/`clearRoomBorderColor()` API (confirmed in Mudlet's own GitHub source, PR #8758) exists only on its unreleased `development` branch (merged 2026-01-12, no milestone) -- absent from every shipped release through the current 4.22.0 stable and its 4.22.0 PTB betas, so it's not available in this profile's Mudlet 4.20.1 or any newer released version; deferred border color until Mudlet actually ships it rather than building against an API that doesn't exist yet. All 15 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-24: verified `roomLooksStale()`'s core assumption against the real log/ corpus, per Claude Desktop's review pushback on 7a12a1d (that rd.room/GMCP and getRoomName()/text-scrape are two independent pipelines with no confirmed agreement, and a real formatting difference would silently stop coloring every affected room -- worse than the bug being fixed). Claude Desktop's own clone can't check this (log/ is gitignored for size), so this needed doing from a session with real filesystem access to it. Checked all 260 log/ files containing a GMCP dump: 98 total `room_data.room` occurrences, 89 matched a standalone displayed header line in the same file exactly. Investigated all 9 that didn't rather than treating them as inconclusive: 8 are the literal string `"darkness"` (DSL's own GMCP placeholder for "too dark to see the room name," confirmed via the surrounding log text -- never an actual room's name) and 1 is a log file that started mid-visit, after the header had already printed in a prior session's log (a capture-window artifact, not a formatting disagreement). Zero real evidence of GMCP-vs-display name drift. Turned the "darkness" finding into a real fix rather than just a footnote: `roomLooksStale()` now explicitly treats `"darkness"` as "name unknown," not a mismatch -- the old behavior was accidentally safe (skip = no corruption) but also needlessly blocked sector/color writes on rooms only ever visited in the dark, when the corpus confirms `rd.sector` keeps reporting correctly even then. 2 new assertions in `test/test_mapper_terrain_lock.lua` (26 total now), confirmed both fail without the darkness-handling line via a targeted revert. Reported the full verification back to Claude Desktop in `HANDOFF.md`, closing their specific ask. All 15 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-24: fixed the real cause of "fresh profile piles every window onto the right, impossible to see anything" (Steven: "i want my layout currently to be the default so things land in a visual way i can explain to them when needed"). An earlier investigation pass (this file's own PACKAGING section) theorized a cross-profile shared-file collision in Mudlet's native `windowLayoutGeometry.dat`/`windowLayout.dat` -- traced the actual dispatch path into Mudlet's real C++ source this time (`Host::openWindow()` in `Host.cpp`) instead of stopping at the Lua layer, and that theory turned out to be wrong: dock widget object names are profile-scoped (`dockWindow_<ProfileName>_<WindowName>`), so DSL2 and MyDSL can't actually collide there. The real mechanism, confirmed at the exact source line: any dock widget that has never existed before in a profile's history is unconditionally created in `Qt::RightDockWidgetArea`, and Geyser's own `UserWindow:new()` never passes a dock-side argument through to the native call when `restoreLayout=true` (which `MyDSL_WindowRegistry.lua`'s `patchUserWindowConstructor()` always sets) -- so nothing ever tells it otherwise. Once a window is manually dragged elsewhere, that profile's own native save remembers it forever, which is why DSL2 (rearranged over weeks) looks fine and a genuinely fresh profile piles all ~20 windows onto the right. Fix: `MyDSL_WindowRegistry.lua` now calls `winObj:setDockPosition()` with the side `MyDSL_LayoutEngine.lua`'s own "LEFT PANEL"/"RIGHT PANEL"/"BOTTOM STRIP" comments already document as each window's intended region -- but only on a profile's genuine first-ever window-creation pass, gated by a new one-line marker file (`MyDSL_dock_initialized.lua`) so a later restart never fights a since-customized arrangement, matching the caution this exact area needed after a prior blind fix here caused a real regression (collapsed panel layout). Found and fixed a real stale comment along the way: `MyDSL_Alterform` was grouped under LayoutEngine's "RIGHT PANEL (UserWindows)" heading, but its actual `MyDSL_WindowRegistry.lua` registry entry is `type="Container"` (anchored inside the main console like `MyDSL_MoonWeather`), not a real dock widget -- excluded from the new mapping, comment corrected in both files. Added real per-window dock-call mocks to `test/mudlet_mock.lua` and a new `test/test_windowregistry_initial_dock.lua` (5 assertions, including that a real profile restart after initialization docks nothing at all); confirmed all 5 genuinely fail without the fix via a targeted revert. All 16 test suites + check_known_patterns.py --all re-run clean. Not yet live-confirmed by Steven.

- 2026-08-24: rebuilt the two remaining missing test files flagged by the 2026-08-23 verification-integrity review (`test_move_cost_weight.lua`, `test_dsl_ud_display_fix.lua` -- cited by name in this file's own 2026-07-18 entries as evidence for the mapper fork's weight system and `dslroom raw` display fix, confirmed to not exist anywhere in git history or on disk). `test/test_move_cost_weight.lua` (11 assertions) extracts the real `map.dsl.captureMovePoints()`/`applyMoveCost()` straight from `DSL_Generic_Mapper.xml` and confirms: a real observed cost becomes room weight, a regen-tick or implausible (>20) cost is discarded without corrupting the running average, repeat visits average into a stable weight, and a manually-set weight (`dsl.weight_source=="manual"`) is never overwritten by a later auto pass. `test/test_dsl_ud_display_fix.lua` (9 assertions) replays Steven's exact real reported bug (`dslroom raw` showing blank fields instead of a placeholder) by capturing `roomRaw()`'s actual `cecho`'d output text, not just unit-testing `dsl_ud()` in isolation -- caught two of its own early mistakes this way (a check that accidentally passed by matching the wrong line's placeholder text, and a literal cecho color-tag `<white>` landing between "avg cost=" and "n/a" that a naive substring check missed) before they could ship as false confidence. Both confirmed via targeted source reverts (not full-file git stash) that every assertion genuinely fails without its corresponding fix. All 18 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-24: wired MyDSL_Chat's tab active/inactive CSS into ThemeEngine, closing a real LOW PRIORITY gap (was hardcoded to a fixed green/grey pair regardless of the active theme). Added `MyDSL.Theme.colorToBracket()` alongside the existing `colorToCSS()` -- checked Mudlet's real `GeyserLabel.lua` source first rather than assuming: EMCO's `activeTabFGColor`/`inactiveTabFGColor` go through `Geyser.Color.hex()`, which needs Geyser's own `"<r,g,b>"` bracket notation, not a real CSS `rgba()` string. New `buildTabTheme()` in `MyDSL_Chat.lua` reads `bgColor`/`highlightColor`/`borderColor`/`dimColor` via `MyDSL.Theme.get()`, with a fallback to the original hardcoded values when ThemeEngine isn't loaded. A new `"MyDSL.theme.changed"` listener re-applies live to an already-open chat window via EMCO's own existing `adjustTabBackgrounds()`/`adjustTabNames()` methods. 11 new assertions in `test/test_chat_theme_hookup.lua`, confirmed via `git stash` that the fix is real. All 20 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-24: corrected two stale Design Ideas claims that turned out not to be buildable after a fresh corpus check, per Steven's request to work through the full TODO/feature backlog. "TargetView: show debuffs cast on the current target" claimed weaken/slow were "confirmed and ready to build" -- re-checked the full log/ corpus and found zero real third-person/observer-side text for either debuff landing on someone else, only self-referential "You feel weaker" (the 2026-07-16 build pass had already found exactly this and never updated this entry to match). "TargetView: auto-populate target on aura detection" -- checked detect good/evil's own helpfiles (confirms the "golden aura" effect) against the full corpus for any real display format and found zero examples of DSL actually showing a detected aura on anyone. Both left as explicit placeholders rather than built against invented text, per this session's "if results require gameplay that isn't in logs, put placeholders" instruction.

- 2026-08-24: built the "order all kill" attack-mode option for Leveling, per Steven's own MyDSL notes ("an order all kill option instead of direct attack, for classes where thats the right opener"). New `mydsl leveling attackmode <direct|orderall>` (`L.session.attackMode`, default "direct" -- no behavior change for existing runs); `orderall` mode sends `order all kill <target>` instead of `kill <target>` in `tryKill()`, using DSL's real `order` command syntax confirmed via `DSL_Helpfiles/order.txt`. Documented in both `MyDSL_Leveling.lua`'s own help() and `MyDSL_Help.lua`. 3 new assertions in `test/test_leveling.lua` (42 total, up from 39), confirmed via `git stash` that 2 genuinely fail without the fix. All 20 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-24: built generic poison-onset capture for TargetView/Focus, per the long-PAUSED "mage-cast poison spell's onset text" item. Re-checked the full corpus: `"<mob> looks very ill."` now has 8 independent real occurrences, still zero tied to an actual `cast poison` line specifically -- built against the confirmed generic (source-agnostic) pattern rather than waiting further, per that item's own suggestion. New `MyDSL.Target.markPoisoned(rawName)` in `MyDSL_TargetView.lua`, wired to a new trigger; flags the CURRENT target only (an assist indicator, not a new auto-targeting mechanism) and clears on target change (same precedent as `_consider_lines`). Pattern recorded in `docs/DSL_CommandRef.md`. Added `Geyser.Button` plus a few missing widget methods (`setClickFunction`/`setState`/`setDoubleClickCallback`) to `test/mudlet_mock.lua` -- TargetView's real `init()`/`render()` couldn't load under the mock at all before this. 9 new assertions in `test/test_targetview_poison.lua`, confirmed via `git stash` the fix is real. Corrected an over-generalized claim from earlier the same sweep along the way: an entry said "all 5 debuffs (weaken/slow/blindness/poison/plague) have zero observer text," extrapolated from only checking weaken/slow -- individually re-checked blindness/plague (still zero) and poison (very much not zero, hence this entry). All 22 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-24: built the mapper area-enter/exit banner, per MyDSL notes 2026-08-24 ("mapper should announce entering/exiting a named area map... Althainia, Death's Corridor"). New `map.dsl.announceAreaChange(rid)` in `DSL_Generic_Mapper.xml`, hooked into `onGenericNewRoom()` right alongside `applyRoomMetadata()`. Reuses `map.currentArea`, already tracked by stock Generic Mapper's own `set_room()` on every real room change -- just watches for it changing and echoes "Entering X (leaving Y)." via the real `getRoomAreaName()` function, no new area-tracking mechanism invented. Skips the very first room of a session (nothing to compare against) and any unassigned room (`getRoomArea() == -1`). Added `getRoomArea`/`setRoomArea`/`getRoomAreaName`/`setAreaName` mocks to `test/mudlet_mock.lua`. 8 new assertions in `test/test_mapper_terrain_lock.lua` (30 total, up from 26) -- caught a real extraction-anchor gap in the SAME file while writing them: `map.dsl.echo()` (defined before this file's old `"local dsl_long_dirs = {"` start anchor) was silently out of the extracted range the whole time, only ever unexercised by this file's existing tests, not because it worked -- widened the anchor to `"function map.dsl.echo(msg)"`, matching the same fix already made in `test_dsl_ud_display_fix.lua`. Fork version bumped 0.2.4 -> 0.2.5. All 22 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-24: built the "toggleable window titles" half of the UI design idea (new `mydsl layout titles <on|off>`, profile-scoped persistence). Checked Mudlet's real Lua API before scoping this: no way to shrink a UserWindow's actual title-bar HEIGHT from script exists (only its text, via the real `setTitle("")`/`resetTitle()` -- reused, not reimplemented) -- that's a native Qt `QDockWidget` property never exposed to Lua, so the "minimal borders" half of the original ask isn't buildable at all, same class of gap as `setRoomBorderColor()`. Applies immediately to already-open windows and to any window created afterward. Added real tracked `setTitle`/`resetTitle` stubs plus a minimal real `table.save()`/`table.load()` to `test/mudlet_mock.lua` (previously no-ops that never touched disk) so the on-disk persistence round-trip could actually be tested, not just asserted. 7 new assertions in `test/test_windowregistry_titles.lua`, confirmed via `git stash` the fix is real. All 23 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-24: built the Alterform warning/danger sound, per Steven's MyDSL notes ("warning + sound before it falls off (countdown from the last 5 ticks, warning at 10 ticks left)"). `F.palette()` already implemented the exact 10/5-cycle color thresholds since 2026-07-11 -- only the sound half was missing. New `F.checkSoundWarning(zoneName)` fires the warn/danger sound once per transition INTO that zone, not on every render (which would fire once per game tick while already in the zone); toggle via `mydsl alterform sound <on|off>`, persisted. Sound files optional (`Sounds/alterform_warning.mp3`/`alterform_danger.mp3`), same provide-your-own precedent as `MyDSL_MovementSounds.lua` -- silently skipped if missing. Deliberately did NOT build the separate "state-scoped sound toggle generic pattern" TODO item as its own abstraction (no concrete use case was ever attached to it, and this project's own discipline is against premature abstraction) -- this Alterform feature is the real, concrete use case that item was implicitly describing; documented the connection in both TODO.md entries. 9 new assertions in `test/test_alterform_sound_warning.lua`, confirmed via `git stash` the fix is real. All 24 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-24: built mapper room-highlighting for other players from `where`, per Steven's MyDSL notes ("mapper: highlight other players' rooms from the where command on the map... multiple possibilities highlighted when room names are ambiguous"). `MyDSL_DataLayer.lua`'s real "Players near you:" capture (already existed for the PlayersNear window) now also extracts structured `{name, room}` pairs and raises `MyDSL.playersNear.parsed`; `DSL_Generic_Mapper.xml`'s new `map.dsl.highlightPlayersNear(list)` listens for it, resolves each room name via `searchRoom(name, false, true)`, and highlights every match via `highlightRoom()` -- ambiguous/duplicate room names (this project's own confirmed real case, "The Wing of the Stone Dragon") highlight ALL matches rather than guessing one. Clears every previously-highlighted room first. Real bug caught before shipping by checking Mudlet's actual C++ source (`TLuaInterpreterMapper.cpp`) instead of guessing: `searchRoom()`'s real return value is a table keyed BY ROOM ID, not a plain array -- an `ipairs()` loop over it would have silently found nothing every time; used `pairs()` instead, confirmed via a targeted revert. Kingdom/org coloring (the other half of the original ask) is NOT built -- needs cross-referencing `DslColors_Core_v1_0`'s own accumulated kingdom data, a materially bigger native-script integration; one flat gold highlight for every match for now. Added `searchRoom`/`highlightRoom`/`unHighlightRoom`/`selectSection`/`appendBuffer` (method) mocks to `test/mudlet_mock.lua`. 5 new assertions in `test/test_datalayer_playersnear_parse.lua` + 4 more in `test/test_mapper_terrain_lock.lua` (34 total there), confirmed via targeted reverts both fixes are real. Fork version bumped 0.2.5 -> 0.2.6. All 25 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-24: closed "AffectsView: recast via potion/skill" as already fully built, not a gap. Checked PNP source first per this project's reuse rule: PNP's real `respell` has exactly 3 dispatch modes (song/skill/spell), no potion mode -- the "think this is in PNP" premise in the original idea doesn't hold up. Checked MyDSL's own code next: `A.commandFor()` already checks a per-affect custom-command override before falling back to defaults, and `mydsl affects command <name> <cmd>` (already documented in `mydsl help`) lets Steven set any command per affect, including a potion, even multi-step sequences. No code changed -- this was a stale TODO entry, not a real gap.

- 2026-08-24: closed out two long-open DEFERRED investigation items, both real research not build work. "Shatteredarchive.com maps": checked `~/Downloads/Shattered-Archive-release-dev.zip` thoroughly -- real map data isn't in it, just a flat per-continent area-NAME list (155 for Althainia alone), no coordinates/exit graphs/images anywhere in the whole 524-file archive. Whatever "maps" Steven saw on the actual website would need pulling from the live site directly. "Cross-profile master function/feature inventory": walked all 16 Mudlet profiles on this machine -- turned out much smaller than feared. The PNP sibling profiles carry only 3 tiny data files each (already git-tracked in this repo's own `PNP files/`); `DSL1`/`Qinrathaz-Vaelis`/`DSL` are historical predecessors of this exact project's own lineage (much earlier, much smaller ancestors of the current `MyDSL_DataLayer.lua`/`MyDSL_CreatureLore.lua`), not separate unknown codebases -- nothing new or unincorporated is hiding anywhere. No code changed, both were real investigations with real (negative) findings, closing rather than leaving open on an unfounded fear of hidden scope.

- 2026-08-25: started the `MyDSL_DataLayer.lua` split-by-domain refactor, per Steven's explicit go-ahead to do it carefully -- executing as a series of complete, independently-verified domain extractions rather than one giant cut. Slice 1: CreatureLore capture (`beginCreatureLore()`/`parseCreatureLoreLine()`/`endCreatureLore()`) moved to a new `MyDSL_DataLayer_CreatureLore.lua`, chosen first specifically because it was confirmed via grep to have zero cross-domain dependencies -- the safest possible cut to prove out the whole mechanism (new file, `dofile()` wiring in DSL2's own Script Editor, `build_mydsl_package.py` auto-detection, test suite) before touching bigger, riskier domains like Combat. Confirmed via an inode check that `~/.config/mudlet/profiles/DSL2` and `~/Desktop/Mudlet/mudlet-data/profiles/DSL2` are the same physical files before assuming either path convention was safe to use for the new dofile entry. Real bug found and fixed while writing this domain's first-ever test (confirmed via grep it had zero coverage before): the sex-field capture guard checked whether alignment had EVER been captured for a creature (from any earlier line) instead of whether the CURRENT line also matched the alignment pattern -- so a real creature with alignment from one line and sex from a separate, later, unambiguous line silently had its sex dropped every time. Fixed; 20 new assertions in `test/test_datalayer_creaturelore_capture.lua` against a real corpus fixture, confirmed via a targeted revert the fix is real. `MyDSL_DataLayer.lua` is now 4,586 lines (down from 4,745). Documented the full remaining-slices plan and the cross-domain-helper-promotion rule (found `normalizeForMatch()`/`bestFuzzyMatch()` are needed by both the future Scan/Look and ItemLore slices) in `docs/TODO.md` so this survives across sessions. All 26 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-25: live-confirmed the DataLayer split's first slice (CreatureLore capture) via MyDSL's own log, per Steven's ask to check the log rather than needing a manual live-test report. `2026-08-25#15-59-22.html` (minutes after the package was delivered) shows a real `creaturelore` run on a thief -- the new `dofile()`-wired `MyDSL_DataLayer_CreatureLore.lua` fired correctly with zero errors, and the exact sex-field bug this slice fixed reproduced independently on completely different real data ("a thief appears to be a more neutral soul." then, separately, "They appear to be male.") -- confirms both the split mechanism and the bug fix are real, not just passing a synthetic test. Slice 1 fully closed; proceeding to Combat next with real confidence in the mechanism.

- 2026-08-25: slice 2 of the `MyDSL_DataLayer.lua` split-by-domain refactor: Combat, the largest and highest-stakes domain in the file (889 lines: all 6 combat parse functions, `getTargetCondition()`, the `combatRoundFlush` round-summary handler, and every supporting local table -- `DAM_INFO`, `CONDITION_PATTERNS`, `battleFormat()`, `calcDamVerb()`, etc.) moved to a new `MyDSL_DataLayer_Combat.lua`. Same clean-boundary check as slice 1 before moving anything -- confirmed via grep that every local/function here is used only within this domain, zero cross-domain leakage despite being ~6x slice 1's size. Pure relocation, no logic changed -- the full existing test suite (including `test/test_combat_damage_regex.lua`'s 7 damage-parsing assertions) passes unchanged, updated that file's `dofile()` list to also load the new file. Real gap found and flagged, not silently ignored: only damage parsing has dedicated test coverage in this whole domain -- avoid/condition/death/end/proc and the round-flush handler itself have zero tests, both before and after this split; noted as real follow-up work in `docs/TODO.md` rather than scope-creeping this pure-move commit to build it now. `MyDSL_DataLayer.lua` is now 3,697 lines (down from 4,745 originally, 4,586 after slice 1). All 26 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-25: live-confirmed the DataLayer split's slice 2 (Combat) via MyDSL's own log, per Steven's ask. `2026-08-25#16-09-45.html` (main log) and `MyDSL/logs/combat/Kien/2026-08-25.log` (combat category log), both from minutes after delivery, show a real fight against a gnome factory worker with every piece tracing correctly: damage round-summaries with the right tier word for the accumulated score ("Your beating do DREADFUL things to Gnome factory worker (235.5)"), avoidance lines, the death sequence, a correctly-tallied fight summary (61 hits, 8 miss, 88% landed), and condensed-mode gagging still hiding raw swings from the main window. Zero errors tied to either split file (the one LUA error in the log is Mudlet's unrelated Semantic Versioning package, at connection time). Both risky slices of this refactor (CreatureLore, Combat) are now fully live-confirmed; proceeding to slice 3 (Scan/Look/PlayersNear).

- 2026-08-25: slice 3 of the `MyDSL_DataLayer.lua` split-by-domain refactor: Scan/Look/PlayersNear (997 lines: `beginScan()`/`parseScanLine()`/`endScan()`, `beginLook()`/`parseLookHereLine()`/`endLook()`, `captureGroundItem()`/`buildItemStatsSuffix()`/`applyGroundItemHover()`, `beginPlayersNear()`/`endPlayersNear()`) moved to a new `MyDSL_DataLayer_ScanLook.lua`. Two real cross-domain dependencies resolved before extraction, not found by trial and error: `normalizeForMatch()`/`bestFuzzyMatch()` (needed by this domain's `resolveMobName()` and the not-yet-split ItemLore's `resolveGroundItem()`) promoted to real `MyDSL.*` table functions in the core file first, verified via a standalone test run right after that promotion alone; `buildItemStatsSuffix()` is the reverse case -- moved into the new file but still called from the core file's not-yet-split Equipment/ItemLore hover-hint lines, harmless either dofile() order since both are load-time definitions, documented in the new file's header. Pure relocation, no logic changed -- full suite passes unchanged once 4 test files' `dofile()` lists were updated (`test_datalayer_audit_fixture_lines.lua`, `test_datalayer_playersnear_parse.lua`, `test_datalayer_several_fixture_line.lua`, `test_others_equipment_hover.lua` -- the last one caught the `buildItemStatsSuffix` cross-dependency for real, via an actual "attempt to call a nil value" failure before its dofile was added). `MyDSL_DataLayer.lua` is now 2,751 lines (down from 4,745 originally -- 42% removed across all three slices so far). All 24 test suites + check_known_patterns.py --all re-run clean; `MyDSL_Full.mpackage` rebuilds cleanly at 36 scripts.

- 2026-08-25: slice 4 of the `MyDSL_DataLayer.lua` split-by-domain refactor: ItemLore/Equipment/Inventory (807 lines: identify/lore capture, own-equipment, others'-equipment, inventory, and container-holds begin/parse/end functions, plus `MyDSL.resolveGroundItem()`/`MyDSL.setGroundItemOverride()`) moved to a new `MyDSL_DataLayer_ItemLore.lua`. Cleanest slice yet -- grepped every function/local for cross-domain call sites first and found zero new promotions needed, since `MyDSL.bestFuzzyMatch()` and the two-way `buildItemStatsSuffix()`/`resolveGroundItem()` relationship with `MyDSL_DataLayer_ScanLook.lua` were already resolved as real `MyDSL.*` functions during slice 3. The domain's trigger-registration block was non-contiguous with two unrelated blocks (Group, Improve -- both belong to the not-yet-split "prompt/vitals" domain) but turned out fully contiguous on its own (equipStart through loreItemStart), so no interleaved surgery was needed. Pure relocation, no logic changed -- full suite passes unchanged once 2 test files' `dofile()` lists were updated (`test_identify_source_scoping.lua`, `test_others_equipment_hover.lua`). `MyDSL_DataLayer.lua` is now 1,984 lines (down from 4,745 originally -- 58% removed across all four slices so far). All 24 test suites + check_known_patterns.py --all re-run clean; `MyDSL_Full.mpackage` rebuilds cleanly at 37 scripts.

- 2026-08-25: slice 5 (final slice) of the `MyDSL_DataLayer.lua` split-by-domain refactor: Score/Flags/Lunar/Time/Weather/Who/Group/Improve (989 lines, the largest single extraction) moved to a new `MyDSL_DataLayer_PromptVitals.lua`. The most cross-cutting domain, saved for last on purpose (Pos'n/Wimpy/Dragon Vitality are real-time single-line text triggers layered on GMCP, not begin/end blocks like every other domain) -- grepped every function/local for cross-domain call sites first and found zero real dependencies outside this domain. By this point Section 9 and Section 10 turned out to be one contiguous block, not interleaved with anything else, the simplest extraction of the five. Confirmed via grep this domain has zero test coverage at all, pre-existing and not introduced by the split -- flagged in `docs/TODO.md` rather than scope-creeping this pure-move commit. Pure relocation, no logic changed. `MyDSL_DataLayer.lua` is now 995 lines (down from 4,745 originally -- 79% removed across all five slices), holding only Sections 1-8 (namespace/state/event-bus/GMCP-handlers/persistence) -- the natural floor for this refactor. All 24 test suites + check_known_patterns.py --all re-run clean; `MyDSL_Full.mpackage` rebuilds cleanly at 38 scripts. The `MyDSL_DataLayer.lua` split-by-domain refactor is now code-complete -- all 5 slices done, live confirmation still pending on slices 3-5.

- 2026-08-25: fixed DSL2's own dev reference profile's 3 dofile-wiring gaps (`docs/TODO.md`, found 2026-07-19), revisited now that this session's DataLayer split proved direct `current/*.xml` edits are safe. Repurposed the stale `MyDSL_ChatWrapper` Script entry (pointed at a file deleted in commit `4aca249`) into the real `MyDSL_Chat` entry instead of adding a separate one -- one edit closes both gaps. Renamed 2 cosmetically mislabeled Script entries to match their real filenames (`MyDsl_Alterform` -> `MyDSL_AlterformView`, `MyDSL_Creaturelore` -> `MyDSL_CreatureLore`). Real gap found beyond the original note: `MyDSL_MovementSounds`'s Script entry had its entire 205-line source pasted in directly as frozen inline content instead of a `dofile()` wrapper -- confirmed byte-identical to the current file before replacing it, so no divergence had happened yet, but the risk was real. `build_mydsl_package.py` now reports zero dofile-related warnings (previously 3 every run); script count unchanged at 38. Full test suite + check_known_patterns.py --all re-run clean (native-XML-only change, no .lua source touched).

- 2026-08-25: closed the "Combat has zero test coverage beyond damage parsing" gap flagged since slice 2 of the DataLayer split. New `test/test_datalayer_combat_lifecycle.lua`, 37 assertions covering `parseCombatAvoidLine`, `parseCombatConditionLine` (+ the `getTargetCondition()` public API), `parseCombatDeathLine` (both real corpus death forms), `parseCombatEndLine` (flee/rescue/mob-fled/escape-fail), `parseCombatProcLine` (including the drowning/freeze false-positive guard and vampiric rage accrual), and the `combatRoundFlush` round-flush handler. Every fixture line is a real corpus string confirmed via grep. Had to override `mudlet_mock.lua`'s no-op `registerAnonymousEventHandler` locally to make `combatRoundFlush` invokable at all -- likely why it had zero coverage before. Caught and fixed a vacuous assertion in my own first draft (asserted against an active-entry table that was never populated); confirmed via 2 targeted-revert checks that the real assertions genuinely fail without the code they test. All 25 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-25: built real in-game theme customization (`theme new/edit/delete/preview`) per Steven, during the MyDSL 1.0 visual pass. Closes a gap `MyDSL_ThemeEngine.lua`'s own Section 6 comment has claimed was already true since 2026-07-11 ("Themes are user-creatable named presets") but never actually was -- only switching between the 5 hardcoded presets worked. New commands clone/edit/delete/preview real custom presets, persisted alongside `active`/`overrides` in the existing settings file, using the same `theme override`-style `key <r,g,b>` syntax rather than inventing a new one. Real load-order bug caught while building this: `loadActive()` checked a saved `active` theme name against `MyDSL.Theme.presets` BEFORE merging custom presets into that table, so a saved active custom theme would silently revert to the hardcoded default on every reload -- fixed the ordering, confirmed via a targeted revert that a new regression test genuinely fails without it. `MyDSL_Help.lua`'s theme entry updated to match (also closed a pre-existing gap: `theme override` had no help text at all before this). First-ever test coverage for `MyDSL_ThemeEngine.lua`: `test/test_theme_customization.lua`, 34 assertions. All 26 test suites + check_known_patterns.py --all re-run clean.

- 2026-08-25: built `scripts/check_text_coverage.py`, a computed coverage check replacing the hand-written-taxonomy plan in `docs/MYDSL_1.0_PHILOSOPHY.md` Principle 4 Part A -- extracts every real `tempRegexTrigger`/`tempAlias`/native-Trigger-regex pattern plus every internal `:match()`/`:find()` Lua-pattern call straight from source (extraction, not paraphrase), tests them against the real `log/` corpus with the actual engine each dialect runs on (`perl` for PCRE, `luajit` for Lua patterns -- conflating the two would silently misreport coverage), and reports which unmatched line shapes repeat most often rather than a bare percentage. Building it caught 3 real methodology bugs before it ever touched the corpus, all via its own required self-test (one known-covered line, one known-uncovered line): patterns built via runtime string concatenation (`"%f[%a]" .. word .. "%f[%A]"`) were mis-extracted as just their first literal fragment; `string.find(s, pattern, init, true)`'s trailing `true` makes the whole call a literal substring search, not a pattern match, and was being misclassified as one; `:gmatch()` calls (which tokenize an already-known string, not classify a whole incoming line) were inflating coverage with fragments like `"%a+"` that trivially match almost anything -- excluded `:gmatch()` from extraction entirely rather than patch around it. A 4th bug surfaced on the first real corpus run itself (not the self-test): some HTML log files have a literal physical newline between `<span>` tags belonging to the same logical line, which silently desynced the line-count alignment between the Python line list and the perl/luajit subprocess output -- caught as a hard crash (IndexError), not a wrong-but-silent number; fixed by collapsing embedded newlines within one logical line, plus added a defensive alignment check so any future desync fails loudly at the source instead of several lines downstream.

  Real first-run numbers, corpus of 786 files / 1,319,263 real text lines (log/AGL/ deliberately excluded, same reasoning as the combat tracker's own Coliseum-prefix exclusion): 98.1% raw coverage (not the goal -- see the script's own docstring for why that number doesn't matter on its own). The real signal, the top unmatched line shapes by frequency: the ENTIRE login/character-creation flow is completely uncaptured ("Password:" 6,073x, "Player name:" 298x, the welcome banner, the account-menu prompts) -- directly relevant to the login-trigger password issue Steven is already fixing himself; a large share of room titles/descriptions appear bare because DSL2's own room capture works by lookback from the `[Exits: ...]` line rather than forward-pattern-matching the title itself, so there's genuinely no pattern for this tool to find (a real methodology limit, not a capture gap); Mudlet's own native map-audit diagnostic output (`=== Area issues ===`, `=== Map issues ===`, box-drawing borders) correctly has no reason to be classified as game text; and one real, isolated actionable gap, `"Reconnecting your master account due to LD"` (90x), a disconnect/reconnect notification nothing currently handles.

  The already-assumed spell/skill gap was explicitly checked, not assumed true: **not confirmed** by this run -- 0 of the top 40 unmatched shapes are spell/skill-related (24 of 1,475 total distinct unmatched shapes are, but none frequent enough to surface). Recorded honestly as a real result that contradicts the prior assumption, not spun to match it -- see `docs/TODO.md`'s note on this.

- 2026-08-26: integrated `MyDSL_Login.lua` + `test/test_login.lua`, a secure replacement for the login-trigger-with-a-plaintext-password issue flagged in `docs/MYDSL_1.0_PHILOSOPHY.md` Principle 5. Built by Claude Desktop (not Steven personally, correcting that doc's earlier framing) without ever viewing the original insecure trigger's code or Steven's real password -- a rebuild of the described *behavior*, not a port. Credential lives in `getMudletHomeDir() .. "/MyDSL_login_credentials.lua"`, a hand-created file outside the repo entirely, loaded via `loadfile()`+`pcall` into a local upvalue that's never assigned onto any `MyDSL.*` table -- nothing else in the profile can read it back out. Sent with `send(cmd, false)` (echo suppressed) and never seen by `MyDSL_RawCapture.lua` (which only logs incoming text). Send-once-per-prompt-per-connection guard, reset on `sysConnectionEvent`. Toggleable (`mydsl login on|off`), silent when unconfigured. Corpus-confirmed both trigger patterns before integrating (`docs/CHANGELOG.md`'s 2026-08-25 entry): `"Player name:"` and `"Password: "` are exact-match consistent across the entire corpus, zero variants.

  Independently re-verified rather than trusted on Claude Desktop's report alone: ran the delivered test (16/16 pass), the full 27-suite test suite, and `check_known_patterns.py --all`, then ran my own 3 targeted-revert mutations (removed echo suppression, removed the send-once guard, leaked the credential onto a shared `MyDSL.Login` field) and confirmed each one independently reproduces the exact test failures Claude Desktop reported, before restoring the clean version -- same standard applied to their work as to any other change in this project. Two real gaps found and closed during integration, neither present in Claude Desktop's own delivered files (their clone has no write access to this repo to have caught these): (1) the credential filename had no `.gitignore` protection of its own -- the existing bare `login` entry (confirmed stale, predates this feature, matches nothing) doesn't cover `MyDSL_login_credentials.lua`; added an explicit entry. (2) the `dofile()` wiring into `current/*.xml` -- Claude Desktop's header comment assumed this needed a GUI step "like `MyDSL_RawCapture.lua`," but Claude Code has direct file access to that XML and added the entry the same way as every other module this session; `build_mydsl_package.py` confirms 39 scripts, zero warnings. Needs Steven's live confirmation in-game.

- 2026-08-26: `docs/MYDSL_1.0_PHILOSOPHY.md` Principle 5 and its "Separately tracked" section closed for real, superseding two prior partial updates -- the login-trigger password fix is fully built, integrated, pushed (`73855b7`), and independently re-verified by both sides separately (Claude Code's own 3 targeted-revert mutations; Claude Desktop separately built LuaJIT from source and ran its own independent 3 mutations against the pushed code, all reproducing the same failure modes). Only Steven's own live in-game confirmation remains, tracked in `docs/TODO.md`, not the philosophy doc.

- 2026-08-26: Claude Desktop's independent spot-check of `scripts/check_text_coverage.py` (asked for in the 2026-08-25 entry above) found two real extraction gaps, both confirmed against current source before folding in: `_find_calls()` only sees inline literals at the `tempRegexTrigger(`/`tempAlias(` call site, missing all 20 of `MyDSL_ChatTriggers.lua`'s wrapper-built chat-routing patterns, the 9 looped spellup-outcome patterns in `MyDSL_CharacterAssist.lua`, and the 4 looped buff-wearoff patterns in `MyDSL_Leveling.lua` -- each shows up as a false gap in the tool's "top unmatched shapes" ranking even though the game text is already correctly handled. Separately, the genericness filter's 6 synthetic fillers never combine letters+digits, so a narrowed-variable `:find()`/`:match()` call (same blind spot already documented for `:gmatch()`, not yet extended to these) can pass filtering and grant false "covered" credit -- one confirmed instance, `DSL_Generic_Mapper.xml:5009`'s `featureName:find("%d")`. Neither finding changes the tool's core design (PCRE-vs-Lua engine split, self-test, alignment guard all held up); documented directly in the script's own "Scope" docstring rather than fixed, since the fix is future work, not urgent.

- 2026-08-26: Claude Desktop completed step 3 of the MyDSL 1.0 sequencing (module-by-module redesign pass), `docs/MYDSL_1.0_MODULE_REDESIGN.md` -- all 39 `MyDSL_*.lua` modules plus `MyDSL_Login.lua` against the audit's existing findings and the philosophy doc's principles, no new findings re-derived, only cross-checked against current source (all confirmed still real as of `76ad4a5`). Copied into the repo and spot-verified independently via direct grep rather than trusted on the writeup alone: confirmed `MyDSL_PortraitView.lua` really does read the nonexistent `MyDSL.Windows.windows` table (should be `.registry`), `MyDSL_ChatTriggers.lua` genuinely has zero "enabled"/"toggle" occurrences anywhere in the file, and `MyDSL_MovementSounds.lua` genuinely has zero aliases (its `cfg.enabled` flag is unreachable by any player action) while being the one real remaining caller of the deprecated `MyDSL.get()` API. Real bugs (no decision needed) and real decisions (needs Steven) both folded into `docs/TODO.md`'s TOP PRIORITY section rather than restated here.

- 2026-08-26: Claude Desktop completed the second audit pass (`docs/OPTIMIZATION_AUDIT.md` sections 41-44 + "Cross-cutting findings (pass 2 wrap-up)") covering the 4 native XML files pass 1 scoped out: `DSL_Generic_Mapper.xml`'s stock ~5,666 lines, `DslColors_Core_v1_0.xml` (never previously audited), `MyDSL_GameplayTriggers.xml`, and `MyDSL_PersonalAliases.xml` -- required under Principle 1. Copied into the repo, spot-checked independently before folding anything in.

  Independently confirmed via direct grep, not just trusted: `MyDSL_GameplayTriggers.xml`'s "Gag prompt line1"/"Gag prompt line 2" native triggers really did run bare `deleteLine()` with zero `MyDSL.Prompt.enabled` guard, contradicting `MyDSL_PromptView.lua`'s own documented expected implementation -- `mydsl prompt on|off` was not actually controlling prompt-line gagging. Fixed directly (both the git-tracked reference file and, after confirming zero drift since the 2026-08-23 extraction, the live MyDSL profile's own `current/2026-08-25#16-29-56.xml` -- same bug, byte-identical, one fix needed for both). Also confirmed real: 30 of the file's 360 `<mSoundFile>` entries hardcode `/home/owner/Desktop/Mudlet/mudlet-data/profiles/DSL2/Sounds/...` (still resolves on this machine -- confirmed via inode check that path and `~/.config/mudlet/profiles/DSL2` are the same physical directory -- but not portable to a fresh install); `MyDSL_MovementSounds.lua`'s and `MyDSL_ChatTriggers.lua`'s zero-alias/zero-toggle findings were reconfirmed unrelated to this. `DslColors_Core_v1_0.xml` genuinely has no master on/off anywhere -- its 803-term vocabulary scan runs unconditionally on every non-blank line. Both flagged in `docs/TODO.md` as needing a decision (bigger changes than the prompt-gag fix) rather than done blind.

  Also fixed 3 additional real bugs found independently while reviewing this pass, none requiring a decision: `MyDSL_PortraitView.lua`'s `MyDSL.Windows.windows` -> `.registry` bug (same root cause pass 2 confirmed for section 33; new regression test `test/test_portraitview_window_registry.lua`, confirmed via targeted revert it fails without the fix); `MyDSL_WindowRegistry.lua`'s dead `table.getn` debug line (LuaJIT never had `table.getn`, always printed hardcoded "20"); and a real `MyDSL_Chat.lua` logic bug in `C.createInWindow()` -- `local old = C.emco; if old and old ~= C.emco then` was captured and compared before `C.emco` was ever reassigned to the new object, so the comparison could never be true and a chat-window rebuild never actually hid or tracked the previous EMCO instance. Fixed by moving the comparison to after the real reassignment. No test coverage existed for `createInWindow()` before or after this fix -- flagged in `docs/TODO.md` rather than building a full mock harness for a 3,000-line file's one function in this pass.

  Two pass-1 open items resolved (not new bugs): `MyDSL_History` is not dead weight -- `MyDSL.Route.history()` has 83 real native callers inside `MyDSL_GameplayTriggers.xml`, reversing pass-1's "zero callers" read (which only searched `.lua` files). `dslColorRelation()`'s data source is confirmed as `DslColors_Core_v1_0.xml`'s own `_G.DSL_COLOR_DB`, with 3 real external callers now identified.

  All 4 native files leak-swept clean (both Desktop's read and this session's re-check). Full test suite (now 28 suites) + `check_known_patterns.py --all` re-run clean; `MyDSL_Full.mpackage` rebuilds cleanly at 39 scripts, confirmed pulling the corrected prompt-gag trigger from the live MyDSL profile's own `current/*.xml` (`build_mydsl_package.py` reads native trigger content from there directly, not from the git-tracked reference copy). Needs Steven's live confirmation: toggle `mydsl prompt off` and check that prompt-line gagging actually stops.

- 2026-08-26: added `docs/MYDSL_1.0_ROADMAP.md`, per Steven's ask to compare all passes (pass 1 audit, module redesign step 3, pass 2 native-content audit, this session's bug-fix sweep) and lay out a single plan to reach "feature complete" 1.0. Built directly from what's already on record in `docs/OPTIMIZATION_AUDIT.md`/`docs/MYDSL_1.0_MODULE_REDESIGN.md`/`docs/TODO.md` -- no new findings re-derived, only synthesized into one per-module status table across all 44 audited units (24 compliant, 4 fixed this session, 5 real bugs still open, 9 waiting on a decision, 2 deferred) plus a 5-phase execution order. Also documents, grep-confirmed, the UI design language already chosen (`MyDSL_ThemeEngine.lua`'s monospace/dark-palette/6px-radius defaults and its 5 named presets) as the starting point for Steven's new visual-pass v2 ask (title-bar hiding, UI best-practices research, 3 mockups) rather than letting that research start from a blank page or invent a disconnected palette. Cross-referenced from `CLAUDE.md`'s docs list and `docs/TODO.md`'s TOP PRIORITY section.

- 2026-08-26: **critical regression found and fixed while adding test coverage for `MyDSL_DataLayer_PromptVitals.lua`** (the roadmap's Phase 1 item, flagged as "highest-frequency trigger, zero coverage") -- the 2026-08-25 DataLayer split-by-domain refactor moved every capture function that writes state (score/flags/lunar/time/weather/who/group/improve/posn/wimpy/vitality in PromptVitals, plus equipment/inventory in ItemLore -- 13 call sites total) into their own files, but the shared `update(section, fields)` bulk-writer they all call stayed a `local function` back in `MyDSL_DataLayer.lua`. A Lua `local` never crosses a separately `dofile()`'d chunk, so every one of those 13 call sites has been throwing `attempt to call global 'update' (a nil value)` on every real invocation since the split landed -- confirmed empirically via a standalone probe script before touching anything. A second, narrower instance of the same root cause: `MyDSL_DataLayer_PromptVitals.lua`'s `parsePromptLine()` (the single highest-frequency trigger in the whole addon -- fires on every prompt line) called a bare `now()`, also a `MyDSL_DataLayer.lua`-local that never crossed the file boundary, so it was ALSO throwing on every prompt line, independent of the `update()` bug (this one bypasses `update()` entirely).

  Fixed by promoting `update` to `MyDSL.update()` (with `local update = MyDSL.update` immediately after so the 8 pre-existing bare-`update()` call sites already inside `MyDSL_DataLayer.lua` keep working unchanged) and updating all 13 external call sites in `MyDSL_DataLayer_PromptVitals.lua`/`MyDSL_DataLayer_ItemLore.lua` to call `MyDSL.update()` explicitly; gave `parsePromptLine()`'s file its own local `now()` (same convention that file's own `trim()` already uses, rather than also promoting `now`, since nothing else in this file or any other domain file calls it bare). Confirmed both fixes with a real before/after probe (not just "tests now pass") -- before: `attempt to call global 'update'/'now' (a nil value)`; after: clean.

  Two new regression test files close the coverage gap that let this ship unnoticed for a full day of real play: `test/test_promptvitals_update_wiring.lua` (parsePromptLine on a real corpus line, Pos'n/Wimpy/Dragon-Vitality triggers -- all previously-untested real-time single-line paths) and `test/test_itemlore_update_wiring.lua` (endEquip()/endInventory()'s full begin/parse/end flow -- the two existing equipment/inventory test files never actually called these functions, only seeded `MyDSL.State` directly, which is exactly why neither caught this). All 30 test suites + `check_known_patterns.py --all` re-run clean; package rebuilds at 39 scripts.

  Practical impact while this was broken (2026-08-25 through 2026-08-26): score/flags/lunar/time-period/weather/who/group/improve/position/wimpy/dragon-vitality capture and equipment/inventory capture were all silently non-functional -- state never updated, everything downstream (LiveView, CombatView's condition reads, PromptView, DataBridge) was reading stale/blank data for this entire window. No live confirmation was possible before this fix since the bug prevented the state from ever populating in the first place.

- 2026-08-26: began MyDSL 1.0 visual pass v2 -- "Direction A+, Quiet Chrome, Cross-Platform," the spec Claude Desktop researched and Steven locked the same day (full research in HANDOFF.md, 3-mockup HTML delivered). Built and landed the safe foundation half: `MyDSL.Theme.titleBarCSS(windowName)` (a `QDockWidget::title{}` rule flattening the real native title bar to the window's own background, Linux-only rendering per Mudlet's own Geyser manual, harmless no-op elsewhere) and `MyDSL.Theme.headerLabelCSS(windowName)` (styling for the cross-platform replacement -- an ordinary Geyser.Label carrying the real visible title). Formula verified exact-match against Desktop's delivered mockup HTML for 3 of 5 presets: header text = `titleColor` at full alpha, header background = `titleBgColor` at its own alpha (the first real consumer of that previously-dead preset key), border-bottom = the window's own `borderColor`. Wired `titleBarCSS()` into `MyDSL_WindowRegistry.lua`'s existing `applyTheme()` (appended onto the same `setStyleSheet()` call `panelCSS()` already uses) -- zero layout risk, confirmed via targeted revert. Two new test files (`test/test_theme_headerbar_css.lua`, `test/test_windowregistry_titlebar_flatten.lua`).

  Deliberately NOT yet done: the actual header Label rollout across the ~13 UserWindows that don't already build their own (only LiveView/TickView/AlterformView do) -- this is real per-file layout work (each window's existing content needs to shift down to make room), not a theme-layer change, and is the honest remaining cost of this direction per Desktop's own research. Tracked as the next step in `docs/MYDSL_1.0_ROADMAP.md`'s Phase 3.

- 2026-08-26: rolled out visual pass v2's header Label ("Direction A+") to 12 of the 15 windows that didn't already have one -- MyDSL_CombatView.lua, MyDSL_RouteHelper.lua (History + PlayersNear, generic per-window loop), MyDSL_ScanView.lua (Scan + RightHere), MyDSL_GroupView.lua, MyDSL_CreatureReference.lua, MyDSL_Help.lua, MyDSL_ItemReference.lua, MyDSL_Leveling.lua, MyDSL_LocationView.lua, MyDSL_PortraitView.lua. Each gets `MyDSL.Windows.ensureHeader(windowName, "Display Name")` right where its old `setTitle()` call used to be, and its existing content Label/MiniConsole shifted from `y=0/height=100%` to `y="10%"/height="90%"` to make room. Portrait is the one exception to plain window-name text -- it has a genuinely user-customizable title (`mydsl portrait title <text>`), so its header shows `P.config.title` verbatim; `P.setTitle()` updated to keep the header in sync when the user changes it live, not just the (now-superseded) native title text.

  Deliberately NOT done this pass, each for a real architectural reason, not an oversight: `MyDSL_Chat.lua` (EMCO's own internal tab-bar/console geometry already owns y=0 in its parent via its own pixel-offset math -- inserting a header means touching EMCO's internal layout, not a simple percent shift); `MyDSL_AffectsView.lua` (writes directly into the UserWindow's own raw cecho target, no bounded child console to resize at all -- adding a header here first requires converting it to a MiniConsole child, a bigger change than this pass's scope); `MyDSL_TargetView.lua` (already has its own custom top row at y=0-10% -- nameplate + MP/Clear buttons -- plus a tightly-tuned action-button grid with a real prior live-bug history around exactly this vertical space budget; shifting it blind risks reintroducing a previously-fixed bug). All three tracked in `docs/MYDSL_1.0_ROADMAP.md` as needing dedicated attention, not a blind repeat of this pass's pattern.

  Full 34-suite test run + `check_known_patterns.py --all` clean; package rebuilds at 39 scripts. Not yet confirmed live -- this is a real, visible UI change across 12 windows, delivered for Steven's own look before being called final.

- 2026-08-26: corrected visual pass v2 after Steven's own screenshots showed the first build ("Direction A+ -- Quiet Chrome, Cross-Platform") rendering wrong live -- the native title bar's flatten step (`winObj:setTitle("")`) never actually blanked Mudlet's title text, so both the full default native title AND the new header Label rendered at once instead of one clean bar. Steven's verdict, in his own words: "User window - MyDSL - MyDSL_Location should be Location, but colored and maybe styled to look pretty, small text/titlebar as possible" -- one bar, not two.

  Reverted to the simpler final design, "One Bar, Renamed and Colored": removed `MyDSL.Windows.ensureHeader()`/`applyHeaderTheme()` from `MyDSL_WindowRegistry.lua` entirely, removed `MyDSL.Theme.headerLabelCSS()` from `MyDSL_ThemeEngine.lua`, and moved its exact color formula (text=`titleColor`, background=`titleBgColor`, border=the window's own `borderColor`) into `titleBarCSS()` itself, still a `QDockWidget::title{}` rule appended onto the existing `setStyleSheet()` call in `applyTheme()` -- unchanged mechanism, corrected formula, confirmed exact-match against Claude Desktop's revised mockup (section 1.6). Reverted the 12 windows from the first build back to their original y=0/100% layout with a direct `winObj:setTitle("Combat")`/etc. call using the real short name -- the normal, documented `setTitle()` path, not the untested empty-string edge case that broke. Also applied the same short-name treatment to the 3 windows the first build deliberately deferred (`MyDSL_Chat.lua`, `MyDSL_TargetView.lua`, `MyDSL_AffectsView.lua`) -- their deferral was always specifically about a header Label needing layout space, which no longer applies now that the Label is gone; `MyDSL.Windows.ensure()` already calls `applyTheme()` (and therefore `titleBarCSS()`) unconditionally for every UserWindow, so these 3 were already getting colored bars automatically and only needed their title text shortened. All 15 windows now covered, a net simplification -- less code than the first build, not more.

  Also dropped the `"-= Name =-"` decoration from Location/Portrait/Affects' default titles (now plain `"Location"`/`"Portrait"`/`"Affects"`) to match the new convention, while preserving all three windows' real user-customizable-title feature (`mydsl location/portrait/affects title <text>`) exactly as before.

  Test files updated to match: `test_windowregistry_header_label.lua` deleted (tested the removed API); `test_theme_headerbar_css.lua` renamed to `test_theme_titlebar_css.lua` and rewritten to test the new accent-colored `titleBarCSS()` formula and confirm `headerLabelCSS` no longer exists; `test_windowregistry_titlebar_flatten.lua` renamed to `test_windowregistry_titlebar_color.lua` (same assertions, corrected terminology -- this is a rename+recolor now, not a flatten). Full 33-suite test run + `check_known_patterns.py --all` clean; package rebuilds at 39 scripts. Still not yet live-confirmed -- Steven's own screenshots drove this correction once already, so the same check applies again before calling this final.

  Separate finding from this round, not acted on: Steven asked whether native dock-tab-group labels (the small tabs shown when multiple windows share one dock side) can also be colored to match. Claude Desktop checked Mudlet's own docs/forums directly -- no per-window hook exists; the only lever is `setAppStyleSheet()`, whole-Mudlet-application scope, a real risk of bleeding into parts of Mudlet's UI outside this addon if the selector isn't scoped carefully. Tracked in `docs/TODO.md` as its own item needing careful validation, not folded into this fix.

- 2026-08-26: fixed two real bugs Claude Desktop found after Steven's clean-reload test confirmed the one-bar fix itself works correctly (all 15 windows now show exactly one native title -- the earlier "two title bars" report was a stale profile, not a code regression).

  **No coloring**: `titleBgColor` across 4 of the 5 presets (all but `terminal_purist`, whose fully-transparent value is deliberate per its own minimalist design) was only 6-10% alpha (e.g. refined_convergence's a=15/255) -- tuned for the first build's separate header-Label wash effect, never re-tuned once that value got reused directly as the native title bar's own background. Raised to a=90/255 (~35%) for refined_convergence, zoned_hud (base + all 4 zone variants), obsidian_ember, and arcane_midnight -- meaningfully visible on a real native `QDockWidget::title` background instead of reading as default OS grey. `test/test_theme_titlebar_css.lua` updated to the new values.

  **No consistency**: `MyDSL_AffectsView.lua` still had a real leftover bug -- its `DEFAULT_CONFIG.title` (the value actually used, not the fallback I'd touched in the first correction pass) was still hardcoded `"-= Affects =-"`, plus two more `"-= Affects =-"` fallbacks in `setTitle()` and the profile serializer. All three fixed to plain `"Affects"`, matching the convention already applied everywhere else. Location/Portrait's own code defaults were already correct (confirmed still `"Location"`/`"Portrait"` from the earlier fix) -- their old dash-style titles showing live are a stale saved preference from before this pass, not a code bug; Steven can clear it with `mydsl location title Location`/`mydsl portrait title Portrait` in-game, no code fix needed for that half.

  Full 33-suite test run + `check_known_patterns.py --all` clean; package rebuilds at 39 scripts. Third live round on this exact feature -- still worth Steven's own confirmation that the color is now actually visible and Affects/Location/Portrait read consistently with everything else.

- 2026-08-26: fixed the real root cause of "zero coloring anywhere" (Steven's screenshot, round 4 on this feature) after concluding the prior alpha-value fix, though real, wasn't the whole story -- researched Mudlet's own confirmed-working stylesheet example (its wiki) and Qt's own stylesheet-syntax docs directly rather than guessing again. Root cause: `MyDSL.Theme.panelCSS()` returns bare CSS declarations with no selector at all -- confirmed via Qt's own docs that this specific form isn't actually documented syntax; it only ever worked because it was always the ENTIRE stylesheet string on its own. `MyDSL_WindowRegistry.lua`'s `applyTheme()` was concatenating `titleBarCSS()`'s real `QDockWidget::title{...}` selector rule directly after those bare declarations -- the panelCSS() half kept rendering (window body background/border, unaffected), but the appended rule silently failed to apply, exactly matching what the screenshot showed. Fixed by wrapping panelCSS()'s declarations in an explicit `QDockWidget{...}` selector at the `applyTheme()` call site only (not inside `panelCSS()` itself, which stays bare for `MyDSL_AlterformView.lua`/`MyDSL_TickView.lua`/`MyDSL_LiveView.lua`'s own Label use), producing the exact two-explicit-rule-blocks shape Mudlet's own wiki confirms works.

  Second, unrelated real bug found in the same round: `MyDSL_History`/`MyDSL_PlayersNear` never got their real short title set at all this session -- `getOrCreateConsole()` (the only place `setTitle()` is called for these two) only ever ran lazily, on first real text routed there, and both windows sat empty the whole test session. Fixed by eagerly calling `getOrCreateConsole()` for both at `MyDSL_RouteHelper.lua`'s own load time, matching every other View file's "set the title unconditionally at load" pattern.

  Two new regression tests (`test/test_routehelper_eager_title.lua`; `test_windowregistry_titlebar_color.lua` extended) -- both confirmed via targeted revert to genuinely fail without their fix, not just pass by coincidence. The color-formula test added last round (`test_theme_titlebar_css.lua`) only ever checked that specific TEXT was present in the CSS string, never that the string was structured as valid, parseable syntax -- that gap is exactly what let this ship. Full 34-suite test run + `check_known_patterns.py --all` clean; package rebuilt.

- 2026-08-26: visual pass v2 confirmed genuinely working live, via Steven's own screenshot -- the `QDockWidget{...}` wrapping fix (previous entry) does render real accent coloring on every native title bar, and the eager-console-creation fix does make History/Players Near show their real short name instead of Mudlet's raw default. No further code changes needed for this feature. Only remaining step, not a code fix: `MyDSL_AffectsView.lua`/`MyDSL_LocationView.lua`/`MyDSL_PortraitView.lua` each persist their own title to disk, so a code-level default change (this session's earlier fix) doesn't override an already-saved value from before this pass -- three one-time commands close it out (`mydsl affects/location/portrait title <ShortName>`).

- 2026-08-26: removed the standalone "rebuild" command entirely from every window, per Steven's explicit ask ("see if rebuild is necessary, if not remove it completely... i dont want to keep extra sets of anything"). Checked for real evidence it was ever needed: zero CHANGELOG entries or code comments document rebuild ever fixing a real reported problem; the only window with a plausible real justification (MyDSL_Chat.lua's EMCO-can-die scenario) already has a more targeted, defensive recovery command (`mydsl chat revive`, which checks for a dead object before recreating) that makes the separate blunt `rebuild()` (one line, `C.createInWindow()`) redundant. Removed the function + alias + Help.lua listing from `MyDSL_GroupView.lua`, `MyDSL_ScanView.lua` (both Scan and RightHere), `MyDSL_CreatureReference.lua`, `MyDSL_ItemReference.lua`, `MyDSL_PortraitView.lua`, `MyDSL_LocationView.lua` (also removed "reset", a bare synonym for the same removed function), and `MyDSL_Chat.lua` (kept `revive` fully intact).

  Three windows (`MyDSL_TickView.lua`, `MyDSL_LiveView.lua`, `MyDSL_AlterformView.lua`) needed more care: their `rebuild()` function is a genuine internal dependency of `reload settings` (and, for Live, `layout`) -- both real features that need the UI recreated after a settings change. Removed only the standalone public alias (`mydsl <name> rebuild`) from all three, kept the function itself (now documented as internal-only), so the real features it backs keep working.

  Full 34-suite test run + `check_known_patterns.py --all` clean (no test ever referenced rebuild, confirming it was genuinely unused even in this project's own test coverage); package rebuilds at 39 scripts.

- 2026-08-26: fixed the two real bugs found in the command-parity sweep. `MyDSL_Focus` (TargetView) had no dedicated way to hide it via any command at all -- added `focus show/hide/title`, all guarded by the same `_focusReserved` mechanism already used for `focus status`/`focus font` so a target literally named "show" can't be misinterpreted as a command. `MyDSL_History` had the same gap -- added `mydsl history show/hide`. Neither History nor Players Near had title customization (unlike every other window); added `mydsl history/playersnear title <text>`, backed by a new shared `MyDSL.Windows.getTitle()`/`setTitle()` persistence pair in `MyDSL_WindowRegistry.lua` (mirrors the existing `getFontSize()`/`setFontSize()` shape exactly) -- these two windows have no settings file of their own to add a bespoke title field to, so this is a genuinely shared mechanism rather than a 7th/8th copy of the same one-off pattern the other 6 title-customizable windows each have. Four new regression tests, each confirmed via targeted revert. Full 37-suite test run + `check_known_patterns.py --all` clean; package rebuilds at 39 scripts.

- 2026-08-26: finished the command-parity sweep -- title customization added to the remaining 9 windows that were missing it: Combat, Group, Scan, Right Here, Bestiary, Item Reference, Chat (via its own existing settings file, matching Portrait/Location/Affects' pattern rather than the shared mechanism), and Location's missing `font <n>` command (a real gap -- `M.config.font` already existed and was used to size the caption text, but nothing ever let a player change it). `MyDSL_MoonWeather.lua` deliberately excluded -- it's a Container widget with no header label of its own (unlike Alterform, which has one) and its container title is intentionally minimized to a blank space already, so a title command would have nowhere to display. Two new regression tests (`test_command_parity_titles.lua` covering the 6 windows using the shared mechanism, `test_locationview_font.lua`), each confirmed via targeted revert; Chat's setTitle spot-checked directly given its heavier EMCO dependency chain rather than a 7th near-duplicate test file. All 17 windows now have consistent title/show-hide/font parity except MoonWeather's documented exception (no UI surface to show a title). The window feature matrix itself (what's there, what's missing, per Steven's ask) is tracked separately as the next step in `docs/MYDSL_1.0_ROADMAP.md`. Full 39-suite test run + `check_known_patterns.py --all` clean; package rebuilds at 39 scripts.

- 2026-08-26: built `docs/MYDSL_WINDOW_FEATURE_MATRIX.md` -- one row per window's real command surface (grep-confirmed against current source, not carried over from the command-parity sweep's own summary), what feeds it data, and open issues, per Steven's ask ("i want to create a feature matrix for all modules, see whats there what connects and that they do it all properly"). Surfaced one new, previously-undocumented real gap in the process: `MyDSL_CombatView.lua` has no whole-window `mydsl combat show$`/`hide$` alias -- `CV.show()`/`CV.hide()` exist and correctly call `MyDSL.Windows.show/hide`, but the only alias wired to them is `toggle battle`; the `mydsl combat show <flag>`/`hide <flag>` aliases that DO exist toggle a different, per-feature `config.show_<flag>` entry, easy to mistake for the missing one. Also re-confirmed 3 still-open findings from `docs/MYDSL_1.0_MODULE_REDESIGN.md` against current source (History has no writer, LocationView double-fires on room entry, TickView renders at full rate while hidden) and confirmed 2 of that doc's other findings are already fixed (Portrait's `MyDSL.Windows.windows` typo, Chat's `old ~= C.emco` dead-logic comparison). No code changed this entry -- documentation only, feeding the next step (module-by-module feature pass).

- 2026-08-26: fixed a real Mudlet packaging bug -- Steven's live Key Bindings editor had accumulated nested `MyDSL_Full` KeyGroup wrapper folders, one inside the next (found via screenshot). First pass root-caused this as reinstalling without uninstalling the previous copy first (`Host::installPackage()`'s reinstall guard is a session-only in-memory list that resets on every restart), added a read-side `flatten_self_nested_wrapper()` fix to `build_mydsl_package.py` and documented the uninstall-first rule in new `docs/MUDLET_PACKAGING_REFERENCE.md`. Full 39-suite Lua test run + Python flatten test + `check_known_patterns.py --all` clean; package rebuilt and delivered.

- 2026-08-26: **corrected the packaging fix above same day** -- Steven reported the same nested-`MyDSL_Full` symptom immediately after installing the "fixed" package, and correctly called it: "this is not a bug in mudlet this is the way you format the package." Re-read `XMLimport.cpp`'s `importPackage()` directly and found the real, deeper root cause: Mudlet's own importer ALWAYS wraps a package's top-level content in a synthetic folder named after the package (Key/Script/Trigger/Alias/Timer/Action alike) on **every single install**, uninstalled-first or not -- and `build_mydsl_package.py` was ALSO wrapping its own exported Key/Script/Trigger content in an outer group literally named `MyDSL_Full`, the exact same name, so every install stacked a second redundant layer regardless of uninstall discipline. Fixed by adding `unwrap_own_package_name_layer()` -- strips that outer same-named wrapper so Mudlet's own synthetic folder is the only `MyDSL_Full` group that ever exists; confirmed against `XMLimport::readScriptPackage()`/`readTriggerPackage()`/`readKeyPackage()` that bare top-level content elements (no group wrapper) are the importer's own accepted, correct shape. Also added a `config.lua` manifest (`mpackage = "MyDSL_Full"`) to the built zip, confirmed via `Host::getPackageConfig()`'s source -- the "zip with a description file" Steven correctly expected a real Mudlet package to be. Rewrote `docs/MUDLET_PACKAGING_REFERENCE.md` with the corrected mechanism and an updated pre-delivery checklist (unzip and grep the built XML for any top-level group still named `MyDSL_Full` -- there should be none). 4 new regression tests for `unwrap_own_package_name_layer()`, confirmed meaningful via targeted-revert mutation; rebuilt package directly inspected -- confirmed zero top-level `MyDSL_Full`-named groups in any of ScriptPackage/TriggerPackage/KeyPackage. Full 39-suite Lua test run + 9-assertion Python test + `check_known_patterns.py --all` clean.

- 2026-08-26: **module-by-module feature pass on the window feature matrix's findings.** Steven corrected the matrix's "History has no writer" claim -- direct inspection of the live MyDSL profile's native `MyDSL_GameplayTriggers` (not visible to a `.lua`-file-only grep) found dozens of real, working `MyDSL.Route.history()` callers across quests/sailing/spells/notifications, called with zero arguments by design (the standard `selectCurrentLine()`/`getCurrentLine()`/`appendBuffer()` "route the line that just triggered" idiom). Corrected in `docs/MYDSL_WINDOW_FEATURE_MATRIX.md`. Fixed the 2 remaining real gaps: (1) `MyDSL_LocationView.lua`'s `M.onRoomData()` was registered on both `gmcp.room_data` and the mapper's `onNewRoom` for the same room-entry moment with no unchanged-room guard, plus a confirmed-dead `gmcp.Room.Info` registration (DSL never sends that generic-GMCP form) -- removed the dead registration, added an unchanged-room early return (manual paths unaffected, they call `M.refresh()`/`M.render()` directly); (2) `MyDSL_CombatView.lua` had no whole-window `mydsl combat show$`/`hide$` alias -- only `toggle battle` reached `CV.show()`/`CV.hide()`, and the pre-existing `show <flag>`/`hide <flag>` aliases toggle an unrelated per-feature flag -- added the missing aliases. A third matrix finding (TickView render-while-hidden + dual visibility flags) turned out to already be fixed earlier the same session (commit `0a302bb`, its own test already in the suite) -- corrected the matrix rather than re-fixing. 2 new regression tests (`test_locationview_double_fire.lua`, `test_combatview_show_hide.lua`), both confirmed meaningful via targeted-revert mutation. Full 41-suite Lua test run + `check_known_patterns.py --all` clean.

- 2026-08-26: resolved 5 of 6 module-redesign decision items per Steven's direct answers ("1 remove api. 2 port if it doesnt break anything 3. yes add toggle... 5. make it toggle just in case. 6. check the code/triggers, i think its disabled or delted"). (1) Removed the deprecated `MyDSL.get()`/`MyDSL.set()` Get/Set API from `MyDSL_DataLayer.lua` entirely -- `MyDSL_MovementSounds.lua`'s `dataGet()` (its one real caller project-wide) now reads `MyDSL.State` directly, same lookup with one less indirection. (2) Removed `MyDSL.on()` too -- ported `MyDSL_Leveling.lua`'s 2 callers (`scan`/`char` listeners) to `registerAnonymousEventHandler("MyDSL.<section>.updated", "MyDSL.Leveling.on<Section>Updated")`, matching `MyDSL_CharacterAssist.lua`'s own existing pattern (no positional event argument -- state read straight from `MyDSL.State` inside the handler); also removed the now-fully-superseded `onceDataLayerReady()` retry-timer wrapper, since `registerAnonymousEventHandler` resolves its named target at event-fire time, not registration time, so the load-order race it guarded against can't happen for this pattern. (3) `MyDSL_MovementSounds.lua` gets a real, reachable toggle: `mydsl movesound on/off/toggle/status` (`config.enabled` existed but had zero aliases anywhere in the file). (5) `MyDSL_CharacterAssist.lua`'s rearm-on-disarm/standup-on-knockdown auto-triggers get `mydsl charassist auto on/off/toggle`, deliberately NOT gating the always-available manual `rearm` alias. (6) Confirmed via direct inspection of the live MyDSL profile's 114 native aliases: no `(autowhere)` alias exists anywhere -- Steven's own recollection was correct, no code change needed. 5 new regression tests (`test_movementsounds_datalayer_port.lua`, `test_movementsounds_toggle.lua`, `test_characterassist_auto_recover_toggle.lua`, plus updates to `test_leveling.lua`/`test_leveling_load_order.lua` for the MyDSL.on() port), each confirmed meaningful via targeted-revert mutation. Item 4 (ChatTriggers per-channel gag/show) and DataBridge's double-fire consolidation are tracked separately in `docs/TODO.md`, not done in this entry. Full 44-suite Lua test run + `check_known_patterns.py --all` clean.

- 2026-08-26: gave `MyDSL_ChatTriggers.lua` per-channel gag/show, per Steven ("there should be a gag and show for each channel and a fallback show if anything breaks or turns off"). Added `MyDSL.ChatTriggers.config.channels` (Tells/Group/OOC/City/Local, all default `true` = current behavior) and a check at the top of `route()`'s trigger callback: when a channel is set to `false`, its lines are left on the main console entirely -- no routing to the EMCO tab, no `deleteLine()` -- the same "move text, don't replace it" fallback the file's own pre-existing `MyDSL.Chat.emco`-not-ready guard already used for an accidental boot-timing gap, now reachable on purpose via `mydsl channel gag/show <name>` + `mydsl channel status`. Also added alias cleanup on reload (`_aliases` table + kill-on-reload, matching every other module's convention) since this file had zero aliases before and never needed it. New `test_chattriggers_channel_toggle.lua`, confirmed meaningful via targeted-revert mutation. Full 45-suite Lua test run + `check_known_patterns.py --all` clean.

- 2026-08-26: fixed `MyDSL_DataBridge.lua`'s confirmed double-fire, per Steven ("fix databridge and consolidate calls like that"). 3 of its 11 `registerAnonymousEventHandler` registrations were raw-GMCP + DataLayer-re-raised pairs for the exact same real-world moment (`gmcp.char_data`+`MyDSL.char.updated`, same for room/tick) -- `sync()` ran twice per char/room/tick update, worst case every combat round (the audit's own confirmed top-priority finding). Coalesced to one call per moment via a zero-delay `tempTimer` debounce: any number of same-burst fires set a pending flag once and schedule exactly one deferred `sync()`, with zero added latency from a human's perspective (still runs before the next real-time moment, just after the current call stack's nested `raiseEvent()`s finish unwinding). Swept every other module with 3+ `registerAnonymousEventHandler` registrations (CombatView, CreatureReference, GroupView, Leveling, ScanView, TargetView, WindowRegistry, DataLayer) for the same "multiple registrations all pointing at one handler for the same real event" shape, per Steven's "any large calls we can simplify like this anywhere else lets fix also" -- none found beyond DataBridge and the already-fixed LocationView; every other registration is for a genuinely distinct event/purpose. New `test_databridge_debounce.lua`, confirmed meaningful via targeted-revert mutation. Full 46-suite Lua test run + `check_known_patterns.py --all` clean.

- 2026-08-27: triaged `docs/myresponses.txt` (Steven's personal "Pass 2" notes copy of `docs/OPTIMIZATION_AUDIT.md`, written 2026-08-25, found untracked in the repo -- 46 notes, never previously reconciled). Cross-checked every note against the 2 days of work since rather than assuming any were still open: 4 were already resolved that same session (Get/Set API removal, `MyDSL.on()` port, DataBridge debounce, TickSource visibility-tie -- all landed in `8835769` and earlier commits, independently of the note); several more turned out already answered elsewhere (theme customization shipped `99c370d`, the cross-module-API question decided in `MYDSL_1.0_PHILOSOPHY.md`, the mapper-merge item already tracked separately in this file, the creaturelore/identify alias shortcuts already existing since 2026-07-11). Added the genuinely still-open, bounded items as new `docs/TODO.md` bullets (RawCapture removal decision, Roller verification, 7 remaining save/load-ambiguity modules, ThemeEngine/ChatTriggers/ItemLore/GroupView/CharacterAssist/Help.lua design questions) and the larger not-yet-scoped design asks (mapper full rewrite, CreatureLore mob-diary wiki window, spell/skill alias-shortcut extension, project-wide alias-dedup sweep) under "OPEN -- Design ideas." Left `docs/myresponses.txt` itself as Steven's personal working copy rather than merging it wholesale -- most of its content was either already resolved or too open-ended to become a TODO bullet without a live conversation first.

- 2026-08-27: pruned `docs/TODO.md` for the third time (2722 → 493 lines, 82% cut), per Steven's ask to "clean it up and check against current state." Removed all 28 `[x]`-marked blocks outright, per the file's own stated rule -- every one of them duplicated a permanent record already in `docs/CHANGELOG.md`/git log, several verbatim. Extracted 2 genuinely still-open technical threads that were buried inside otherwise-resolved `[x]` blocks before deleting them: `build_mydsl_package.py` still doesn't splice `MyDSL_PersonalAliases.xml` into the built package (confirmed no `AliasPackage` handling exists in the script at all), and the first `check_text_coverage.py` run's 2 real findings (login-flow gap, the "Reconnecting your master account due to LD" notification). Consolidated the "NEEDS LIVE CONFIRMATION" section from 14+ full-paragraph entries (several 5+ weeks old) down to one line each, per the file's own suggested fix for its own previously-flagged backlog-size problem. Fixed one confirmed-stale reference along the way: the PACKAGING section's "manual step: disable the native (autowhere) alias" no longer applies -- confirmed 2026-08-27 via direct inspection of the live MyDSL profile's 114 native aliases that no such alias exists anywhere. Cross-checked every retained item against current source/git log rather than assuming any were still accurate. `docs/DECISIONS RECORDED` section left untouched -- durable reference, not a punch list, already correctly separated from the action items pruned everywhere else.

- 2026-08-27: applied Claude Desktop's two-phase TODO.md re-triage (`TODO_final.md`, relayed via Steven from `~/Downloads/`) as the new `docs/TODO.md`. Phase 1 was an interactive Keep/Cut-done/Cut-not-doing/Needs-info checklist over all 57 items from the pruned list; phase 2 researched every "needs-info" item against real code/git history and brought concrete findings + one question each back to Steven, who answered directly. Spot-checked the concrete "already fixed"/"confirmed gone" claims against current source before adopting wholesale (all confirmed accurate): the Roller pre-port native trigger is genuinely gone from tracked native XML; `MyDSL_ScanView.lua`'s duplicate-mob-count fix, `MyDSL_CharacterAssist.lua`'s bare `setspell` alias, and `MyDSL_LocationView.lua`'s `stripQuotes()` all exist exactly as described; the `MyDSL.save()`/`table.load()` naming worry checked out clean across all 7 flagged files (each calls the generic Mudlet `table.save()`/`table.load()`, never DataLayer's own `MyDSL.save()`). One item needed real code action, not just a doc update: removed `MyDSL_RawCapture.lua` entirely (its `mydsl rawlog` diagnostic had zero dependents anywhere -- confirmed via repo-wide grep before deleting) -- also removed its `dofile()` entry from DSL2's own reference `current/*.xml`, its `TEST_MODULES` entry in `MyDSL_DataLayer.lua`, and its `mydsl help` line. Since the live MyDSL profile's own native snapshot still has the old script installed until the next uninstall+reinstall cycle, added `RETIRED_SCRIPTS` to `build_mydsl_package.py` so a deliberately-removed script doesn't silently re-enter a build just because it's still lingering there -- confirmed against the real live snapshot (package now builds clean at 38 scripts, correctly skipping it with a NOTE rather than erroring). 3 new assertions in `test/test_build_mydsl_package_flatten.py`. Full 46-suite Lua test run + `check_known_patterns.py --all` clean. New Top Priority items from this pass (native-content full consolidation, ChatTriggers coverage+default-gag redesign, DslColors integration, 30 hardcoded sound paths, Help.lua auto-derive redesign, a real from-scratch install test, login/character-creation design review, ItemLore design review, GroupView button review) replace the smaller items they superseded.

- 2026-08-29: fixed all 30 hardcoded native sound paths in `MyDSL_GameplayTriggers.xml`, per Steven ("fix all them"). Each was a native `isSoundTrigger="yes"` + `<mSoundFile>/home/owner/Desktop/Mudlet/mudlet-data/profiles/{DSL2,MyDSL}/Sounds/...</mSoundFile>` entry -- inconsistently split between two subpaths, and not portable to a fresh install regardless. Converted each to a `<script>playSoundFile(getMudletHomeDir() .. "/Sounds/<file>")</script>` + `isSoundTrigger="no"`, matching the pattern already used elsewhere in the same file (e.g. "is DEAD!! Scan"); triggers that already had real script logic (`MyDSL.Route.history()`, a `cecho`, etc.) got the sound call prepended, not replaced. Applied identically to both the git-tracked backup and the live MyDSL profile's own native XML -- the actual file `build_mydsl_package.py` reads native trigger content from, confirmed via source read (the backup is a reference copy, never the build's real input). Found a real, previously-uncaptured 31st instance while doing this: a bare native `Trigger` named "Nature Growth" with an empty `<packageName>`, sitting directly under `<TriggerPackage>` outside any tracked group -- same class of invisible-to-every-backup gap as the 29 personal aliases found 2026-08-23, now noted under the native-content-consolidation TODO item rather than solved piecemeal here. Its own hardcoded path was fixed live regardless. Verified via direct before/after diff on both files (XML well-formed, trigger counts unchanged: 277/277 tracked backup, 296/296 live), the built package directly unzipped and grepped (0 hardcoded paths, 113 `playSoundFile` calls), and the full 46-suite Lua test run + `check_known_patterns.py --all` clean (pure native-XML change, no `.lua` source touched).

- 2026-08-29: `MyDSL_ChatTriggers.lua` default gag-state + coverage audit, per Steven ("review the chat code for optimization and function, that it's catching all known chats vs. patterns and logs, then set a default for what is gagged and what is not. Local is not gagged by default, all other channels are gagged by default. Local is yells/tells/whispers and such"). Fixed the one real inconsistency: Local's whisper pattern was still gagged by default (a deliberate 2026-07-11 choice at the time -- "not whisper, he didn't ask for that one"), now matches its say/yell/shout siblings; Tells was already correct and unchanged. Coverage audit: cross-checked all 5 tabs' patterns against `DSL_Helpfiles/channels.txt`'s authoritative channel list, ran the corpus-based `scripts/check_text_coverage.py` (98.1% real-corpus coverage, no chat-shaped gaps in the top 30 unmatched shapes by frequency), and grepped `log/` directly for 4 named channels the helpfile lists but this file doesn't route (Cgos, Thaxanos, Shalonesti, in-character Clan) -- confirmed zero real message examples for any of them anywhere in 1.3M corpus lines, same "can't build, no data" class as quest-tracking, not a fixable gap. New `test_chattriggers_default_gag_state.lua`, confirmed meaningful via targeted-revert mutation.

- 2026-08-29: `DslColors_Core_v1_0.xml` integration + perf fixes, per Steven ("dslcolor needs to be integrated into MyDSL, it will toggle on off like the others" + "I want an optimization pass on this... fix the known issues"). Added a real master toggle (`dslcolor on|off`, persisted in `DSL_COLOR_DB.settings.enabled`) -- previously only `dslcolor echo on|off` existed, which is just notification verbosity, not a way to turn the ~803-term vocabulary scan off at all. `dslColorOnLine()` now early-returns when disabled. Fixed the confirmed perf bug: `dslBoundedFind()` re-lowercased the same real game line from scratch on every one of up to ~803 term comparisons per line; added `dslLowerCached()`, a one-entry cache keyed by plain string equality against the current line, so the lowercase form is computed once per real line and reused for every term checked against it. Applied identically to both the git-tracked backup and the live MyDSL profile's own native XML (confirmed via source read that `build_mydsl_package.py` reads native trigger/script content from the live file, never the backup). New `test_dslcolors_toggle_and_perf.lua`, extracting the real embedded Lua directly from the native XML (same technique as `test_mapper_gmcp_and_doorverb.lua`), confirmed meaningful via targeted-revert mutation on both fixes. Census/player-profile-field/documentation asks from the same Steven note are bigger, still-open work, tracked separately in `docs/TODO.md`. Full 48-suite Lua test run + `check_known_patterns.py --all` clean.

- 2026-08-29: added `scripts/check_help_coverage.py`, a drift checker between `MyDSL_Help.lua`'s hand-maintained command table and the real, currently-registered `tempAlias()` patterns across every `MyDSL_*.lua` file -- built instead of the more invasive "annotate all ~189 tempAlias() call sites" rebuild Steven's "auto-derive sounds good" note could have meant (see `docs/TODO.md` DECISIONS RECORDED for the full reasoning). Reuses `check_text_coverage.py`'s existing `tempAlias()`-literal extractor. First real run found 19 genuinely undocumented commands: all 17 ported-EMCO `emco *` sub-commands in `MyDSL_Chat.lua`, and both `mydsl login`/`mydsl login status` commands in `MyDSL_Login.lua` (the 2026-08-26 secure-autologin module, which had never gotten a help entry at all) -- confirmed by direct grep showing zero `emco`/`login` mentions anywhere in `MyDSL_Help.lua`. Documented both in `MyDSL_Help.lua` (new "Login" module entry; `emco *` commands added to the existing "Chat" entry); re-running the checker now reports zero drift. New `test/test_check_help_coverage.py`, confirmed meaningful via targeted revert (git-stashing the `MyDSL_Help.lua` additions reproduces the original 19-item report exactly). Full Lua test run + `check_known_patterns.py --all` clean.

- 2026-08-29: login-flow design review, per Steven ("this needs design review as logging into DSL is not the same as name/password -- check the login trigger and the logs to see how the login actually works. Propose a better method for new players"). Re-derived the real sequence directly from `log/` (e.g. `log/2026-07-01#15-33-12.txt` lines 58-95) instead of trusting `MyDSL_Login.lua`'s own header description: `Password:` is the MASTER ACCOUNT's password, and `Player name:` (a separate, later prompt reached after navigating the Master Login Menu) asks which CHARACTER to play -- not a matched pair, contrary to how the header previously read. Confirmed "Reconnecting your master account due to LD" needs no dedicated alert (purely informational, appears only after a real link-death, identical Master Login Menu follows either way -- confirmed by diffing a session with it against one without). Confirmed character creation should stay explicitly out of automation scope (rare, one-time, largely irreversible). Found a real usability wrinkle in what's already live: `credentials.name` is sent verbatim at every `Player name:` prompt, but Steven's own corpus shows him logging into different characters across sessions (`kien`, `tibbins`, ...) -- a fixed auto-sent character name is wrong whenever it doesn't match that session's target character. Proposed fix (splitting the single `mydsl login on|off` toggle into independent password/character-name toggles, defaulting character-autofill off) written up in `docs/TODO.md`, not yet implemented -- it changes login automation behavior and needs Steven's sign-off first. Updated `MyDSL_Login.lua`'s own header note accordingly so it no longer misdescribes the two prompts as paired.

- 2026-08-29: ItemLore + ground-item capture design review, per Steven ("this item library and capture needs a design review"). Traced the full chain across `MyDSL_ItemLore.lua`, `MyDSL_DataLayer_ItemLore.lua`, `MyDSL_DataLayer_ScanLook.lua`, and `MyDSL_ItemReference.lua` -- all four already cross-reference correctly, with inline notes explaining every hard case already resolved (partial/full merge semantics, wand/staff-only spellInfo import, manual ground-item override). Checked the one plausible gap (no browse/search command, exact-name lookup only) and confirmed via `MyDSL_CreatureReference.lua` that Bestiary has the identical surface -- an established convention, not a defect. Conclusion: no structural changes needed. Recorded in `docs/TODO.md` DECISIONS RECORDED; the open TODO item removed.

- 2026-08-29: GroupView/TargetView quick-action button UX + interconnection review, per Steven ("review this, I want to make changing buttons easier for the user, and see how the module interconnects"). Confirmed the two modules already share one real action registry (`MyDSL.TargetView.actions` + `TV.config.custom_actions`) -- `GV.quickAction()` already looks it up directly, correctly wired, not a gap. Fixed the real, low-risk friction found: neither module had any way to discover which action keys are valid before assigning one via `focus mobset/playerset`/`group quickset` -- added `TV.listActions()`, wired to new `focus actions` and `group actions` aliases (GroupView delegates to the same function, no duplicate copy to drift). Documented in `MyDSL_Help.lua`; `scripts/check_help_coverage.py` re-run clean. New `test/test_targetview_groupview_list_actions.lua`, confirmed meaningful via targeted revert. A full clickable button-picker UI is a separate, larger project, deliberately not attempted here -- recorded as still-open in `docs/TODO.md` if Steven wants it after trying the new discovery commands. Full Lua test suite + `check_known_patterns.py --all` clean.

- 2026-08-29: `MyDSL_Login.lua` password/character-autofill split, implementing the 2026-08-29 login-flow design review's proposed fix (Steven chose "split toggle, default OFF for character autofill" from 4 presented options). `mydsl login on|off` now controls master-account PASSWORD autofill only (default unchanged: on, always safe -- one account, one password). New, independent `mydsl login character <on|off>` controls the "Player name:" autofill (default: off, since a single hardcoded character name is only correct when it matches the session's actual target character -- Steven's own corpus shows him logging into different characters across sessions). Both triggers now stay registered for the module's lifetime, each gated by its own flag at fire time, instead of the old register/unregister-the-whole-pair mechanism tied to one combined toggle. Renamed the credentials file's `name` field to `character` (clearer -- it was never the master account's own name) with a backward-compat fallback so an existing file using the old key still loads correctly. `mydsl login status` now reports both toggle states. `test/test_login.lua` extended: default-state coverage (password sends, character doesn't), the backward-compat load path, and each toggle firing independently of the other -- confirmed meaningful via targeted revert (temporarily defaulting characterEnabled to true reproduces 5 real test failures). Documented in `MyDSL_Help.lua`; `scripts/check_help_coverage.py` re-run clean. Full Lua test suite + `check_known_patterns.py --all` clean.

- 2026-08-29: correction, not an edit -- `docs/changelog-archive/CHANGELOG-2026-07.md`'s 2026-07-12 entries cite upstream PR #9334 ("reject suspicious shrinkage" in `TMainConsole::getUserWindowSize()`) as the root cause of the Mudlet 4.21/4.22 docked-UserWindow layout bug that's the whole reason this project stays pinned to 4.20.1. Claude Desktop pulled the actual PR diff directly (2026-08-29, prompted by Mudlet 5.0 shipping the same day) rather than trusting the paraphrase: PR #9334 is real and does touch `getUserWindowSize()`, but its actual change is a null-pointer guard for an unrelated bug (a docked window's `TDockWidget` getting orphaned when deleted and recreated under the same name) -- no shrinkage-comparison or size-rejection logic anywhere in the 3-hunk diff. The real root cause of the docking bug was never actually identified; the PR number was a plausible-sounding guess at the time (correctly hedged as a "root cause candidate" in the original entry, which turns out to have been the right amount of confidence). Per the archive's own rule ("nothing in the archive is ever edited after the fact"), the archived entries are left as-is -- this entry is the correction of record. Updated `docs/TODO.md`'s DECISIONS RECORDED to reflect the correction and flag the real open question it raises: Mudlet 5.0 (shipped 2026-08-29) reworked Geyser's layout internals -- the exact code path in question -- so it's genuinely unknown whether the original bug still exists. Recommended a real re-test on a disposable profile before considering an upgrade; full assessment in `~/Downloads/mudlet_upgrade_assessment.md`.

- 2026-08-30: live-test findings from the first real "MyDSL Test" reinstall session (screenshots + `notes.json` in the profile), fixed what didn't need Steven's input. (1) `MyDSL_ItemReference.lua`: clicking an item with no DB record yet redrew the PREVIOUS identify's stats under the newly-clicked item's name (`MyDSL.State.itemlore` was an unconditional fallback) -- now only used when its `.key` actually matches the item being rendered. (2) `MyDSL_WindowRegistry.lua`: `MyDSL_Help` was starting open on Steven's second monitor on a fresh reinstall -- root cause is the same shared-file `loadWindowLayout()` mechanism documented 2026-08-29, restoring a stale floating/visible state from an earlier session under the same profile name and racing ahead of the existing `characterIdentified` hide-reconciliation. Added an immediate re-hide pass for every `entry.visible == false` window right after `loadWindowLayout()` runs. (3) `MyDSL_Help.lua`: added a bare `mydsl` alias (no sub-command) that opens Help, per Steven's note. (4) Confirmed (not a bug): the Moon & Weather window's off-default position is the same shared-layout-file mechanism -- an earlier test session in this same profile name saved a moved position via `mydsl layout save`, and it legitimately came back on reinstall; not something a from-scratch install test in a *reused* profile name can actually verify defaults against. Portrait's "black title bar" and the mapper "not following movement" reports need a closer live look (mapper likely just needed Mudlet's native Map widget opened once post-install, per the `[DSL Mapper Addon]` boot message -- later screenshots in the same session show it tracking correctly) -- left open, see `docs/TODO.md`. Also ran a full `lua-language-server --check` sweep against a temporary `.luarc.json` mirroring `.vscode/settings.json` (996 problems) to answer Steven's question about the VSCode Explorer per-file issue-count badges: 605 of those were in `PNP files/` and `EMCOChat/` (vendored/reference source this project never edits), and most of the rest were real Mudlet API globals never added to the allowlist. Added `Lua.workspace.ignoreDir` for both directories and ~25 missing real globals (`hideWindow`/`saveWindowLayout`/`rex`/etc.) to `.vscode/settings.json`, cutting the sweep's count to 500 (mostly `test/*.lua`'s expected `io`/`os` usage under plain luajit, not a real gap). Full 57-suite Lua test run + `check_known_patterns.py --all` clean.

- 2026-08-30: profile cleanup, per Steven ("start streamlining the files... consolidate useful files from profiles... then delete old profiles"). Inventoried all 20 Mudlet profiles on the machine; 11 were empty July-era scratch profiles (`Test`/`Test1-7`/`old`/`dslnew`/`dslpnpupdate`, nothing but Mudlet's own `errors.txt`) and 7 were old play profiles Steven confirmed are superseded now that `MyDSL`/`MyDSL Test` are the live-play profiles (`Dark & Shattered Lands - PNP`/`PNP1`/`PNP2`, `DSL`, `DSL1`, `DSL - Kien`, `Qinrathaz-Vaelis` -- found via filename comparison that these weren't simple duplicates: PNP/PNP1/PNP2 are sequential folder-copies of one character's journey with real divergent history in each, e.g. 23 log files unique to PNP and 10 unique to PNP1 that PNP2 never had; DSL1 similarly had 5 logs Qinrathaz-Vaelis lacked). Consolidated all 1090 gameplay logs (deduped by filename) and 19 personal portrait images from all 7 into `~/Documents/DSL_MUD_Archive/` before deleting anything, confirmed via diff that no unique portraits were lost (only case-naming differences, e.g. `kien.png` vs `Kien.png`). Freed ~24GB. Updated `CLAUDE.md`'s stale "sibling Mudlet profiles" reference bullet accordingly.

- 2026-08-30: DslColors "Professor" title fix (previously only in the git-tracked reference copy, `DslColors_Core_v1_0.xml`) synced into the live `MyDSL` profile's actual native script -- `DSL_TITLE_ALIASES` and `DSL_DEFAULT_TITLE_SIGNATURE` now match between the two. Root cause of the drift: this is a native-only script (no loose `.lua` file), so a fix to the git reference copy never ships until the identical edit is made inside Mudlet's own stored XML. Rebuilt and verified `MyDSL_Full.mpackage` contains it.

- 2026-08-30: `MyDSL_PortraitView.lua` -- fixed the black bar rendering between the title bar and the portrait image (Steven, live screenshot with the bar circled). Root cause: the 2026-08-26 "Direction A+" visual pass reserved the top 10% of Portrait's content area for a `MyDSL.Windows.ensureHeader()` call, but `ensureHeader()` was never actually implemented anywhere in `MyDSL_WindowRegistry.lua` (confirmed nil project-wide) -- every other window from that pass ended up using the native title bar only, with no such gap. Portrait was the one straggler still carrying both the dead call and the now-empty reserved margin. Removed the `ensureHeader()` calls (2 call sites) and the image label's `y="10%"/height="90%"` split in favor of the native title bar + a full-height label, matching every other window's actual pattern. Rebuilt `MyDSL_Full.mpackage`.

- 2026-08-30: processed "MyDSL Test" profile's `notes.json` (Mudlet's own Notepad file, separate from the `notes_utf8.txt` convention -- never previously part of the checked-every-session routine). Real bug found and fixed: `MyDSL_DataLayer_ItemLore.lua`'s equipment/inventory/others'-equipment/container hover-click sites looked ItemLore up by the exact displayed short description, which DSL doesn't guarantee matches `identify`'s own canonical object name (confirmed real, same session's log: equipment shows "a heat resistant pair of pants", identify reports "heat resistant pants" -- neither is a substring of the other). `identify` itself was persisting correctly the whole time. Added a token-overlap tier to `MyDSL.bestFuzzyMatch()` (`MyDSL_DataLayer.lua`) and a shared `MyDSL.resolveItemLoreRecord()` helper, wired into all 4 hover-click sites. `test/test_itemlore_fuzzy_resolve.lua`, 8 assertions. Also fixed `MyDSL_LocationView.lua`'s `flattenExits()`, which alphabetized exit directions instead of compass-ordering them like the Live window's own `[Exits: ...]` line (Steven: "need to normalize those exits like live window"). Remaining items from the notes file triaged into `docs/TODO.md` (autologin confirmed not-a-bug -- no credentials file exists yet, by design; skill-based affect tracking, new DslColors titles, and Leveling areas/routes/mobs data all need more detail from Steven before they're actionable). Confirmed the asset-fetch "advise it'll take a moment, notify on completion" ask is already exactly what `MyDSL_AssetFetch.lua` does (matches the live screenshot's own output) -- no gap. Full 58-suite test run + `check_known_patterns.py --all` clean; package rebuilt.

- 2026-08-30: fixed the three items from "MyDSL Test" `notes.json` that didn't need Steven's input, per his explicit "fix the things you dont need me for, stop asking/waiting/postponing." (1) Login credential setup: built a real popup window (`MyDSL_LoginSetup`, added to `MyDSL_WindowRegistry.lua`) that auto-shows once when unconfigured -- Mudlet/Geyser has no native text-entry widget (confirmed via grep, none exists anywhere including the vendored PNP reference), so its "click here" link pre-fills the command line via `appendCmdLine()` with `mydsl login setup <character> <password>` instead of requiring a hand-edited credentials file. `test/test_login_setup.lua`, 11 assertions. (2) DslColors: found the "new titles" Steven meant -- read straight from `DSL_PeopleColors_data.lua`'s persisted `DSL_COLOR_DB.titles` (both `MyDSL` and `MyDSL Test`), 29 titles he'd already added live via `dslcolor title <category> <title>` that were never promoted to shipped defaults. Promoted all 29 into `DSL_DEFAULT_TITLE_SEEDS`/signature (release6 -> release7), same treatment "Professor" got the day before, in both the git reference copy and the live native profile; package rebuilt. (3) Leveling areas: confirmed not actually lost -- the live `MyDSL` profile's `leveling_areas.lua` is 100% seed-sourced (`mydsl leveling import` run once, nothing hand-built beyond it), so copied it directly into `MyDSL Test/MyDSL/leveling_areas.lua` rather than treating it as a rebuild task. Full 59-suite test run + `check_known_patterns.py --all` clean.
- 2026-08-30: `DslColors_Core_v1_0.xml` -- added `(THAX)`/`THAX`/`thax` to the kingdom-color lookup table, all mapping to `thaxanos` (per Steven's notes.json: "dslcolor need to map THAX to thaxanos kingdom color palette etc."). The table already had `Thaxanos`/`thaxanos` but was missing the bracketed abbreviation form real game text actually uses ("(THAX) Vexgar.") -- every other kingdom with a short form (VR/Verminasia, etc.) already had all 3 key variants; Thaxanos was the one gap. Applied to both the git reference copy and the live `MyDSL` profile's native XML; `MyDSL_Full.mpackage` rebuilt, copied to `~/Downloads/`.
- 2026-08-30: found the old native login-automation trigger Steven asked about (notes.json: "i had an old login trigger... can you find it") -- an old profile snapshot from 2026-07-17, predating the 2026-08-26 security cleanup, firing on the color prompt and chaining straight through account name/password/character-select in one command. Confirms `MyDSL_Login.lua` only ever covered credential storage, never actual automated sending. Pitched a real redesign (trigger chain on the real login prompts + first-run capture via `sysDataSendRequest`, no separate setup command) to replace the popup approach Steven rejected ("lose the autologin, it makes it more combesom") -- not built yet, needs his go-ahead. Also flagged: that old snapshot still has his real account name/password in plaintext on disk (never git-tracked) -- not yet cleaned up, needs his confirmation the old snapshots aren't needed for anything else first.
- 2026-08-30: rebuilt `MyDSL_Login.lua` end to end per Steven's go-ahead ("build the login rediesign, and dont worry about the password trigger"). Real navigation now, not just credential storage: a stage-tracked trigger chain drives color prompt -> continue -> master login menu -> account name -> password -> master menu -> character list, cross-checked against the old trigger's sequence plus this session's own corpus logs (now in `docs/DSL_CommandRef.md`'s new "LOGIN / CONNECTION SEQUENCE" section). First-run capture: when account/password aren't saved yet, the module watches (doesn't send) the next line after those two prompts and saves what was typed automatically -- no setup command, no popup. Captured password line is deleted from the console the instant it's captured. `mydsl login on|off` is now the whole sequence's switch; new `mydsl login forget` clears a bad capture. Removed the `MyDSL_LoginSetup` popup entirely (window registry entry, rendering, aliases -- all gone, per Steven's rejection of that approach). `test/test_login.lua` rewritten (33 assertions, drives trigger callbacks directly since the test mock doesn't do real PCRE matching); `test/test_login_setup.lua` deleted. Full suite + `check_known_patterns.py --all` clean; `MyDSL_Full.mpackage` rebuilt and verified to contain the new module's actual text, copied to `~/Downloads/`. Needs Steven to log in once and confirm live.
