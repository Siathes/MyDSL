# DSL Observer UI — Changelog

Format: `[Date] [Version] Description`
Claude Code appends one entry per commit. Claude.ai appends session summaries.

---

## Session: June 9, 2026 — Research & Contract Phase

**No code changes made this session.**

### What was accomplished:
- Uploaded DSL2 autosave.xml — all 13 Layer 2/3 scripts extracted and available
- Full Mudlet API education: built Parts 1 & 2 of API reference
- Built feature comparison matrix (DSL1 vs PNP vs new MyDSL)
- Built project backlog from all scattered notes
- Built DSL_CommandRef.md from actual in-game text captures
- Written Contract_DataLayer.md from actual code review
- Established three-tool workflow (Claude.ai + Claude Code + Steven)
- Confirmed git branch strategy and session rituals

### DataLayer bugs confirmed:
- 10 broken score parser patterns (wrong field names/formats)
- 2 flags parser bugs (can't detect on/off state)
- Who parser broken for hyphenated races
- Lunar, time, affects text parsers all wrong format
- Missing: no equipment section

### DSL game data confirmed:
- Prompt is 3 lines, event-driven (not per-command)
- GMCP fires after every server response
- 14 chat channels confirmed
- Moon system: 3 moons, Kien sees red+white only (neutral alignment)
- Calendar: 1 year = 13 real days 15 minutes

### Files created this session:
- `DSL_CommandRef.md` — in-game text patterns with confirmed Lua patterns
- `Contract_DataLayer.md` — DataLayer contract with all confirmed bugs
- `DSL_SessionNotes.md` — session log
- `SESSION_START.md` — this update
- `CHANGELOG.md` — this file
- `TODO.md` — actionable backlog
- `README.md` — project master index

---

## Pre-Session History (reconstructed from memory/audit)

### ~June 2026 — Layer 3 Phase A
- Ported 7 display modules from DSL1: ChatWrapper, AffectsView, LiveView,
  TickSource, TickView, PortraitView, LocationView
- DataBridge and RouteHelper added
- Chat routing triggers imported to Trigger editor
- HP/Mana/MV bars showing, affects populating, tick counting
- Mapper loaded with DSL1 map data

### ~June 2026 — Layer 2 Complete
- ThemeEngine, LayoutEngine, WindowRegistry built
- 21 windows registered and positionable
- All positions stored as percentages

### ~June 2026 — Layer 1 (DataLayer) v1.0
- Written from scratch, replacing DSL1's SourceCore
- Clean event bus, per-character data separation
- Duplicate handler prevention
- Per-section state guards (the prior initialization bug fixed)
- Git: `e6af3d1 — DSL2 clean profile — carry-over files from DSL1 with DataLayer bug fix`


---

## Session: June 14–21, 2026 — Full Layer 2/3 Phase A Contract Pass

**No code changes made this session — pure design/contract/research session.**

### Contracts written from actual extracted code:
- Contract_ThemeEngine.md — persistence, character-binding, named presets designed
- Contract_LayoutEngine.md — screenshot-corrected positions for all 21 windows
- Contract_WindowRegistry.md — MyDSL_Location rename, MyDSL_Mapper removal decided
- Contract_DataBridge.md — 6 gaps found, 3 entire sections (time/affects/score) unmapped
- Contract_RouteHelper.md — color-stripping bug found (Gap 1), routeMap dropped
- Contract_TickSource.md — 5-second warning bug confirmed (config exists, logic missing)
- Contract_TickView.md — confirmed visual warning DOES work, only audio/echo missing
- Contract_ChatWrapper.md — 5.0s forced rebuild wipes early-session chat (Gap 2)
- Contract_AffectsView.md — confirmed best-quality module, registerHandlerOnce is the template
- Contract_PortraitView.md — CharPic removal confirmed and decided
- Contract_LocationView.md — confirmed roomData() is LiveView's primary room data source
- Contract_LiveView.md — baseline only, deliberately paused for full design session

### Major architecture discovery:
- Mudlet has native UserWindow docking (docked/dockPosition/autoDock/restoreLayout)
  and native layout persistence (saveWindowLayout/loadWindowLayout with version
  numbers) that replaces most of LayoutEngine's planned custom persistence system
- Multiple windows docked to the same side auto-tab via Mudlet's Qt layer —
  confirmed by screenshot (Map/Scan/Combat tab together on the left)
- Critical caveat found: loadWindowLayout() must be called once, after all
  windows are created — calling it per-window resets every other window

### DSL1 mapper script fully audited:
- 8 confirmed DSL-specific patches on top of stock generic_mapper v2.1.8
- All center on room-description matching/cleaning for duplicate room names
- download_path disabled to protect patches from being overwritten by auto-update
- Must be carried forward to DSL2 as-is, never reinstalled fresh

### Screenshots analyzed (3 total):
- Full DSL1 layout reference — corrected all 21 window positions
- Stacked/tabbed dock confirmation — Map/Scan/Combat native tab group on left
- Tick window position — standalone, between Live and Target, Alterform slot confirmed for future

### Prompt system fully designed:
- Toggleable pretty prompt (gag 3 lines + new PromptBar overlay)
- Confirmed no extra prompt-text capture needed — alignment from score,
  day/night derived from time command (existing trigger runs it 2x/game-day)

### GMCP structure further confirmed via live captures:
- Two time-command format variants found and documented
- Affect fields (n/d/lc/m/t) cross-verified against AffectsView's correct handling
- (AR) = Arkane kingdom confirmed
- Two moons visible to Kien (red/white), black moon confirmed evil-only

### Files created/updated this session:
- 8 new Contract_*.md files (ThemeEngine, LayoutEngine, WindowRegistry,
  DataBridge, RouteHelper, TickSource, TickView, ChatWrapper, AffectsView,
  PortraitView, LocationView — final count includes earlier-session DataLayer)
- Contract_Addendum_2026-06-21.md — cross-cutting decisions superseding
  parts of LayoutEngine, WindowRegistry, RouteHelper, PortraitView contracts
- SESSION_START.md — full rewrite reflecting all 12 contracted modules
- TODO.md — full rewrite, blocking items clearly separated from design work
- CHANGELOG.md — this entry

---

## Session: June 21, 2026 (continued) — LiveView Design Finalized

No code changes — design and validation session.

### LiveView fully designed via iterative collaboration:
- Built interactive drag-and-drop mockups (Visualizer tool) so the user could
  physically arrange stat blocks rather than describe layouts in words
- Final layout: 12-row design, header (room/exits/identity/badges/time) +
  attribute rows paired with their combat stat (mirroring DSL's own score
  sheet pairing: STR+Hit, INT+Dam, WIS+Armor) + four bars (HP/Mana/Move/
  Improve) with graduated danger-color thresholds
- No compact/full toggle — single unified view per explicit decision
- Window is floating with restoreLayout=true (not docked), allowed to grow
  taller than the standard bottom-strip height; user resizes once

### Validated against real Mudlet/Qt constraints before finalizing:
- Confirmed Geyser gauges (front/back/text triple-label pattern), dynamic
  per-render color swapping, and percentage+pixel mixed positioning are all
  supported — consistent with patterns already used in TickView/PortraitView
- Found: Qt QLabel does NOT support CSS text-overflow:ellipsis — room name
  truncation must be done via Lua string cutting, not a stylesheet property
- Found: absolute pixel positioning breaks at different resolutions (same
  bug already flagged in MyDSL_Audit.md for old LocationView) — all LiveView
  children use percentage positions instead

### New gaps surfaced (added to TODO.md):
- DataLayer: Items count was never parsed at all — new field, not a fix
- DataBridge: needs hitroll/damroll/armor/items/posn added to DB.score
- DataBridge Gap 3 (DB.time): priority raised, now has a concrete consumer

### Files updated this session:
- Contract_LiveView.md — full rewrite, baseline superseded entirely
- SESSION_START.md, TODO.md — LiveView moved from "next" to "done"
2026-06-21 fix: DataLayer text parsers — HitRoll/DamRoll/Armor/Align/Pos'n/GOLD/BANK/PRACT/TRAIN/Craft/PKill patterns corrected; Items field added; Flags X/space detection fixed; Lunar rewritten for 2-line format; Time handles both HH:MM and HH:MM o'clock; Who captures race+class; Affects text uses Spell:/modifies format
2026-06-24 fix: DataBridge gaps 1-4 and 6 — DB.time and DB.affects sections added; score text fields (align/race/class/religion/profession/crafts/xp/practices/trains/bank/qpoints/hitroll/damroll/armor/items/posn) merged into DB.score without overwriting GMCP fields; DB.room.area replaced with DB.room.sector; listeners added for score.updated, time.updated, affects.updated
2026-06-24 fix: RouteHelper — appendBuffer mode for color-preserving passive routing, decho mode for explicit messages; routeMap removed (addendum); Route.clear() and Route.getConsole() added; Route.bloodbath() shorthand added; shorthand helpers now use direct DSL2 window names
2026-06-25 fix: ChatWrapper — Gap 2: 5s startup timer now guards before rebuild (revive if EMCO healthy, createInWindow only if broken); Gap 3: fallback window position corrected to x=78% w=22% h=46%; Gap 5: getWindowEntry() uses canonical MyDSL_Chat key only (removed Chat/windowId fallbacks that were always nil)
2026-06-25 fix: TickSource — Gap 1: warnTime alert fires safeRaise("MyDSL.Tick.Warning", rem) when remaining hits threshold; Gap 2: deregisterHandlers() kills old handler IDs on reload (pcall killAnonymousEventHandler); Gap 3: loop generation counter aborts stale tempTimer chains on reload; also raises MyDSL.Timers.Updated alongside Pulse each step
2026-06-25 fix: rename MyDSL_RoomPicture → MyDSL_Location in WindowRegistry (entry + 5 comments/fallback), LayoutEngine (defaults key + comment), LocationView (getWindowEntry lookup); remove MyDSL_Mapper from WindowRegistry and LayoutEngine defaults per Addendum §6; remove MyCore dead code from LocationView roomData() sources; fix MyDSL_Tick defaults to confirmed position x=0.59 y=0.79 w=0.03 h=0.21
2026-06-25 feat: PromptView — MyDSL.Prompt module with character-bound enabled toggle (save/load/onLogin), setEnabled/toggle/_cmd alias handler; handler deregistration on reload; listens for MyDSL.login.updated; two trigger specs documented in file for manual entry (MyDSL_PromptGag_Vitals, MyDSL_PromptGag_Location)
2026-06-25 fix: WindowRegistry — Gap 5: applyBorders() (23%/22%/21%) called at ensureAll() and on sysWindowResizeEvent (_resizeHandler); onLogin() calls loadWindowLayout(charLayoutVersions[name]) then applyBorders() (window reset fix); saveLayout() calls saveWindowLayout(); mydsl save layout alias with _saveAliasInstalled guard; login handler on MyDSL.login.updated with pcall deregistration
2026-06-25 fix: WindowRegistry — port DSL1 constructor patch approach: patchUserWindowConstructor() monkey-patches Geyser.UserWindow.new to inject restoreLayout=true/autoDock=true on every UserWindow; removed charLayoutVersions/onLogin()/_loginHandler/saveLayout(charVersion); loadWindowLayout() called plain (no version arg) via tempTimer(1s) and tempTimer(3s) after ensureAll(); saveLayout() simplified to saveWindowLayout() + saveProfile()
2026-06-25 fix: WindowRegistry — remove restoreLayout=true from constructor patch (conflicts with x/y/w/h at creation per Mudlet Geyser docs); add saveWindowLayout()+saveProfile() after ensureAll() to establish baseline layout at default positions before user arranges windows
2026-06-27 fix: WindowRegistry — layout persistence: baseline saveWindowLayout() after ensureAll(), loadWindowLayout() on 1s+3s startup timers, debounced auto-save on sysWindowResizeEvent (2s settle), mydsl layout save alias
2026-06-28 fix: WindowRegistry — remove saveWindowLayout at startup and auto-save on resize; single immediate loadWindowLayout() after ensureAll(); saveLayout() captures live Geyser positions into LayoutEngine before saving Qt state; 3s timer restored to match DSL1; sysWindowResizeEvent handler removed from LayoutEngine (was snapping windows to defaults on every dock operation)
2026-06-28 fix: DataLayer score parser — stats flattened (scoreBlock.str not scoreBlock.stats.str); weight/max_weight added (was missing); field names normalised to snake_case (hit_roll, dam_roll, armor_pierce/bash/slash/magic); PRACT:/TRAIN: patterns corrected from Practices:/Trains:
2026-06-29 fix: DataLayer score trigger wiring — tempRegexTrigger("^Score for ") registered in DataLayer itself (no Mudlet XML triggers existed); beginScore() installs catch-all line trigger; two-separator logic in parseScoreLine() handles opening/closing --- lines; endScore() kills catch-all trigger
2026-06-29 v1.0-phase-a-complete — Phase A smoke test passed. All Layer 1 and Layer 2 systems confirmed working. Score parser operational. One known minor issue: stance field captures trailing text — fix in next pass. Profession field missing — endScore fires before PROFESSION line.
2026-06-29 fix: score stance captures only first word (%S+ not .+); profession field restored via three-separator logic (_saw_profession flag gates endScore); Phase A fully complete with zero remaining issues — stance="Offensive" ✅ profession="Pickpocket" ✅
2026-06-30 fix: rewrite parseLunarLine and verify parseTimeLine for actual DSL output — beginLunar pre-sets has_bonuses=false on all three moon sub-tables; parseLunarLine uses three-capture pattern, strips "in " prefix from position, renames visibility→position, fixes %s+ whitespace matching in bonus patterns, fixes hours_remaining pattern for "19 1/2 Hours" format, sets has_bonuses=true when bonus block parsed; parseTimeLine verified correct (handles both HH:MM and HH:MM o'clock formats)
2026-06-30 fix: wire lunar block triggers in DataLayer Section 10 — permanent tempRegexTrigger on "^The (red|white|black) moon is" calls beginLunar(), parses first line, installs catch-all; catch-all feeds each line to parseLunarLine(), detects blank line to call endLunar() and kill itself; guard prevents beginLunar() re-entry when catch-all already active; weather trigger deferred with TODO comment
2026-06-30 feat: add MyDSL_MoonWeather Phase B HUD widget — Adjustable.Container with lockStyle="padding", AutoSave/AutoLoad; three Geyser.Label image slots (left/center/right) + one text label; focal moon derived from score.align via case-insensitive find (default red); phaseToFile+phaseAbbrev+phaseBonus tables; applyMoonSlot() with PNG/Unicode fallback; buildFocalText() shows phase·position + wiki bonus + regen/cycles/hours (has_bonuses=true only); buildTimeRow() shows ☀/✦ indicator + clock + day + date; four event handlers (lunar/weather/tick/login .updated); gag triggers for moon/bonus lines; mydsl moon toggle/on/off/font aliases; MW.init() safe-reload pattern kills old handlers before re-registering
2026-06-30 fix: add lockStyle=padding to MoonWeather container in WindowRegistry — registry entry gains lockStyle="padding" field; Container creation block passes entry.lockStyle to Adjustable.Container:new() (nil for all other containers, so no behaviour change elsewhere)
2026-06-30 fix: moon slot sizing (pixels), black moon dim (#222222/18pt/not-visible text), time row event (MyDSL.time.updated), gag toggle default OFF with setGag()+aliases
2026-06-30 refactor: single-label HTML table layout in MoonWeather — one Geyser.Label at 100%x100%, moonSlotHtml() returns HTML, render() builds 3-row table, no Geyser pixel math
2026-06-30 fix: time row event/field names in MoonWeather — MyDSL.State.tick.time is clock string not period; isDay now derived from ampm; debugc logging added to buildTimeRow()
2026-06-30 fix: wire parseTimeLine() trigger in DataLayer Section 10 — root cause of time row "-- -- --"; tempRegexTrigger("^It is ") now calls parseTimeLine(getCurrentLine()); expanded buildTimeRow() debug logging to include State.tick block
2026-06-30 fix: correct time row field names, event name, data source — buildTimeRow() now reads MyDSL.DB.time (not State.time); uses db.clock/db.is_day/db.day_name/db.month; display order corrected to day→clock→date; debug logging updated to dump DB.time; Contract_MoonWeather.md Implementation Notes corrected
2026-06-30 fix: remove debug logging from buildTimeRow() — 16 debugc() lines removed; function is clean
2026-06-30 docs: add all contract and session docs to git tracking — 19 previously untracked docs/*.md files now versioned
2026-06-30 docs: update Contract_DataBridge to reflect current state — Gaps 2/3/4/6 marked fixed; field mappings rewritten to show DB.time, DB.affects, full DB.score text fields, correct DB.room.sector; Gap 1 confirmed not a bug; Gap 7 (no DB.equip) added; contract status table updated
2026-06-30 fix: time row ordinal number and day/night indicator in MoonWeather — ordinal() returns suffix only; call site now prepends db.day_num ("25th" not "th"); isDay derived locally in buildTimeRow() from db.hour+db.ampm instead of relying on db.is_day (eliminates stale-data timing risk)
2026-06-30 fix: time row ordinal display and remove unreliable is_day — confirmed both time formats work (o'clock and plain); confirmed day_num stored as integer by parseTimeLine(); is_day removed from DB.time (Algoron day/night does not follow 12h clock); buildTimeRow() now shows fixed neutral ✦ (#888888); clock text fixed to #cccccc; Contract_MoonWeather.md updated with is_day deferral note
2026-06-30 milestone: MoonWeather confirmed working in-game — three moon slots, phase/bonus text, time row all verified; tagged v1.1-moonweather-complete
2026-06-30 note: score parser stance+profession bugs confirmed already fixed (commit 468ee77 / Jun-29) — SESSION_START.md was stale; no changes needed
2026-06-30 feat: wire sunrise/sunset and weather triggers, real day/night indicator — State.time.is_night added (default false); sunrise/sunset triggers confirmed from live session (Steven 2026-06-30); weather trigger uses broad pattern with keyword guard in parseWeatherLine(); DataBridge DB.time.is_night added; MoonWeather buildTimeRow() restores ☀/✦ from is_night
2026-06-30 fix: correct night trigger to 'The night has begun.' and add GMCP clock fallback for is_night — removed wrong 'The sun slowly disappears in the west.' pattern; State.time.is_night now starts nil (not false) so DataBridge can detect first-load; DataBridge computes gmcpIsNight from gmcp.tick.time as fallback before any trigger fires; explicit if/else avoids Lua boolean-false bug in and/or idiom
2026-07-01 feat: wire day/night period from prompt parser — parsePromptLine() added to DataLayer (Section 9d2); fires on every prompt line 2 '==-Night Time - 5:00am :: ...'; captures period string, sets State.time.period + is_night on every server event; DataBridge priority updated: period > sunrise/sunset trigger > GMCP clock; fixes day/night indicator unreliability vs rare trigger events
2026-07-01 refactor: compact stacked layout, living clock display, day_name fix — moonSlotHtml sizes reduced (focal 32pt/32px, sides 18pt/20px); buildTimeRow() rewritten as 3-line stacked layout (indicator+period, clock, date); date line strips 'the ' month prefix and uses day_name directly from State.time; row heights 50/20/30%%, cellspacing=0; debug lines added for day_name field confirmation
2026-07-02 fix: clock anchor via State.time, remove half-hour snap — MW.clockAnchor(tickStr) replaced with MW.setClockAnchor() reading MyDSL.State.time directly (gmcp may be nil in anonymous handlers); anchor_h24/anchor_min consolidated to anchor_game_min (total DSL minutes 0-1439); gmcp.tick.time still used as fallback for the minutes component only; MW.clockStr() uses anchor_game_min with %02d format so clock visibly counts forward every second; setClockAnchor() called from tick/time handlers, onTickUpdate, onTimeUpdate, and init()
2026-07-02 fix: clock hour rollover — only anchor on time command not on tick — removed MW.setClockAnchor() from tick handler and onTickUpdate(); tick fires every ~41s which was resetting anchor_game_min to the last typed time, preventing hour rollover; anchor now only set from onTimeUpdate()/time handler (player types 'time') and init() startup; clockStr() math rolls hours correctly without interference
2026-07-02 fix: clock drift — derive DSL rate from live TickSource average — replaced hardcoded 60/82.5 rate with 30/tick_avg where tick_avg=MyDSL.DB.tick.average (smoothed real-time interval from TickSource); each GMCP tick advances exactly 30 DSL minutes so this gives the true rate; fallback to 40s before TickSource stabilises; eliminates ~3.8% systematic drift from the old constant
2026-07-02 fix: anchor clock to gmcp.tick.time every tick, eliminate drift — MW.setClockAnchor() now accepts optional tickStr parameter; tick handler and onTickUpdate() pass gmcp.tick.time (exact DSL clock, no drift); init() also tries gmcp first; onTimeUpdate() still calls with no arg (State.time fallback for minute precision); clock stays perfectly synchronised without relying on rate approximation
2026-07-02 fix: parse minutes in parseTimeLine, time command no longer resets clock — DataLayer: added min capture group to parseTimeLine() pattern (:%d+ → :(%d+)), stores State.time.minute=integer; MoonWeather: setClockAnchor() fallback now uses t.minute or 0 (was hardcoded 0); onTimeUpdate() and time handler only call setClockAnchor() when anchor_real is nil (no tick anchor yet), preventing `time` command from resetting clock position mid-session
2026-07-02 style: reorder time/period line, gold bonus text, minimize title — buildTimeRow() condensed from 3 lines to 2: clock+indicator+period merged onto one line ("1:34 am  ✦  Night"), date line unchanged; period label abbreviated ("Night Time"→"Night", "Day Time"→"Day"); bonus colors in buildFocalText() changed from green/red to gold (#ffcc44) for all non-zero values; container:setTitle(" ") added to _buildUI() to minimize title bar chrome
2026-07-02 style: attempt to remove outer container border — added container:setStyleSheet() in _buildUI() (transparent background, border:none, no padding/margin) wrapped in pcall in case Adjustable.Container does not expose the method; label border-radius changed 4px→0px for flush fill
2026-07-02 fix: remove outer border via adjLabelstyle constructor param — replaced post-creation setStyleSheet() pcall (doesn't work on Adjustable.Container) with adjLabelstyle="background-color:rgba(0,0,0,0); border:none; padding:0px;" in the Container:new() constructor; requires profile reload to take effect since _buildUI() only runs on first init()
2026-07-02 milestone: MoonWeather feature-complete — border removed (adjLabelstyle), clock anchors to gmcp.tick.time every tick (zero drift), minutes parsed from time command, date re-anchors via existing dawn/dusk triggers firing time command (no calendar math), all visual tweaks done (gold bonuses, merged clock/period line, minimized title). Tagged v1.2-moonweather-final
2026-07-02 feat: live moon phase countdown + regen gold color — added MW._lunar anchor table, setLunarAnchor()/cyclesNow()/countdownStr(); onLunarUpdate() and init() seed the anchor; buildFocalText() line 3 now shows "Regen+10%  57cy · 39m" with regen in gold (#ffcc44) when non-zero and live-decrementing countdown; static hours_remaining removed
2026-07-02 fix: lunar countdown anchor + remove stale debug lines — setLunarAnchor() no longer uses focalMoon()/alignment; now scans all three moons for the one with has_bonuses=true (only the aligned moon gets bonuses from `lunar`), fixing nil countdown when score not yet loaded; removed two debugc day_name lines from buildTimeRow() (fire every second, confirmed working)
2026-07-02 fix: wire setLunarAnchor into lunar event handler — _registerHandlers() lunar.updated callback only called render(); setLunarAnchor() was never being called from the wired handler so anchor_real stayed nil and countdownStr() always returned nil; fixed by adding setLunarAnchor() to the reg() callback (init() already had the call)
2026-07-02 fix: wire lunar anchor + remove redundant has_bonuses gates — lunar handler and init() already wired (previous commit); removed has_bonuses guard in setLunarAnchor() scan (cycles_remaining non-nil already implies bonus block parsed) and in buildFocalText() regen/countdown line (same reason)
2026-07-02 fix: countdown shows game-hours not real-world minutes — countdownStr() was dividing cycles×tick_avg/60 (real minutes); fixed to cycles×30/60 (DSL game-hours, 30 game-min per tick); display now matches `lunar` output "72 (36 Hours)" → "71cy · 35h"; tick_avg lookup removed as no longer needed; hours step at whole-cycle boundaries (~40s) which is acceptable
2026-07-02 fix: countdown half-hour precision matching game output — each cycle = 0.5 game-hours; countdownStr() now rounds to nearest half-hour and formats as "27 1/2h" or "27h" to mirror game's "27 1/2 Hours"/"27 Hours"; examples: 54cy→"54cy · 27h", 53cy→"53cy · 26 1/2h", 52cy→"52cy · 26h"
2026-07-02 fix: countdown cycles and hours use consistent floor rounding — previous code floored cyc_int but rounded total_half_hours, causing inconsistency (45.8cy→"45cy · 23h" instead of "45cy · 22 1/2h"); fixed by deriving hours from cyc_int: hours=floor(cyc_int/2), has_half=(cyc_int%2)==1
2026-07-02 fix: anchor lunar countdown at parse time not handler time — endLunar() now stamps State.lunar.parsed_at=os.time() inside the update() call (before emit fires); setLunarAnchor() uses parsed_at as anchor_real instead of os.time() at handler invocation; eliminates the ~40s event-chain delay that caused widget to always show 1 cycle less than game
2026-07-02 milestone: MoonWeather complete — all alignments verified (neutral/red, good/white, evil/black), live countdown working, 1-cycle offset known and deferred; next window: Scan/RightHere
2026-07-02 feat: add scan and creaturelore parsers to DataLayer — Section 3: State.scan and State.creaturelore guards; Section 9o: beginScan/parseScanLine/endScan with isMobName helper and byName/rightHere aggregation; catch-all ends on blank line/"Players near you:"/group header; ScanView gag flag delegated to parseScanLine; Section 9p: beginCreatureLore/parseCreatureLoreLine/endCreatureLore; parses alignment, wealth, sex, hp; merges into CreatureLore DB (nil-guarded); Section 10: scanAround, scanDir, loreStart tempRegexTriggers
2026-07-02 feat: add MyDSL_ScanView — Scan and RightHere display windows; renderScan() shows [Right Here] (deduplicated via byName with ×count) and [Nearby] (ordered rows); renderRightHere() shows clickable cechoLink entries calling MyDSL.Target.set(); setGag() manages header-line gag triggers; body gagging delegated to DataLayer; safe-reload pattern; mydsl scan gag/ungag aliases
2026-07-02 feat: add MyDSL_TargetView — target display and action buttons; MyDSL.Target.set/clear/toggle/doAction/captureConsider API; render() draws [M]/[P] toggle, 6 action button cechoLinks in 2 rows, consider output area; config persisted to getMudletHomeDir()/MyDSL/targetview_config.lua; consider capture triggers; mydsl target/mobset/playerset aliases; safe-reload kills old handlers/triggers/aliases
2026-07-02 feat: add MyDSL_CreatureReference — creature lore display window; listens for MyDSL.creaturelore.updated; onLoreUpdate() looks up DB then State fallback, renders, auto-shows window; render() outputs race/align/hp/xp/rooms/immunities/resists/vulns/drops/last-lored; formatNumber() adds comma separators; mydsl lore <name>/hide/show aliases; window hidden by default
2026-07-02 fix: guard nil State.scan on first load — DataLayer State.scan now initialised with full schema (rows={}/rightHere={}/byName={}); ScanView renderScan() splits nil-rows vs empty-rows check into two separate early returns; renderRightHere() guards not scan.rightHere before header cecho and before next() call; prevents crash on profile load before first scan
2026-07-02 fix: use Geyser MiniConsole object methods instead of global cecho — global cecho/cechoLink/clearWindow calls with string window name render raw color codes as literal text in Geyser MiniConsole objects; TargetView render() and ScanView renderScan()/renderRightHere() now use mc:clear()/mc:cecho()/mc:cechoLink() object methods; nil guard added at top of each render function
2026-07-02 fix: use decho for plain text in ScanView and TargetView MiniConsoles — mc:cecho() with <#rrggbb> hex tags rendered raw text in Geyser MiniConsole (cechoLink was working; plain cecho was not); all mc:cecho() calls replaced with mc:decho() using <r,g,b> RGB format and <r> reset; color variable values in renderScan() converted to RGB triplets; cechoLink display strings left unchanged (already working)
2026-07-02 fix: convert remaining cecho to decho in TargetView — toggle and button cechoLink calls still used <#rrggbb> hex colors in display text; TV.actions color values converted from hex to RGB triplets (204,68,68 / 136,204,170 / 170,170,255); type_color in render() converted to RGB; toggle_text and btn_text format strings use <r> reset; mc:cechoLink() replaced with mc:dechoLink() for both toggle and action buttons
2026-07-02 refactor: scan window uses appendBuffer via RouteHelper — renderScan() removed from ScanView; Scan window now fills passively as each body line arrives: DataLayer beginScan() clears SV.ui.scanConsole, catch-all calls selectCurrentLine()/copy()/appendBuffer() after parseScanLine(); SV.ui = {} added as public namespace exposing scanConsole for DataLayer; render() now calls only renderRightHere(); cechoLink useCurrentFormat changed to true (suppress underline)
2026-07-02 style: target buttons use dechoPopup, larger font, action colors — TV.actions colors updated (glance/consider/lore/look→204,204,204 near-white; heal→68,204,68 green); fontSize=11 in MiniConsole constructor + setFontSize(11) on every init; toggle dechoLink useCurrentFormat=true (suppress underline); action buttons replaced with dechoPopup(TARGET_MC, label, {gameCmd}, {hint}, false) — sends command directly on click, only renders when target is set
2026-07-02 fix: use dechoLink and decho RGB format in renderRightHere — cechoLink with <#rrggbb> hex colors in display text rendered raw codes in Geyser MiniConsole; mob/player/count color variables converted to RGB triplets (204,136,68 / 136,170,255 / 255,204,68); text format string uses <r> reset; mc:cechoLink() replaced with mc:dechoLink() (useCurrentFormat=true); newline folded into text string, removing separate mc:decho("\n") call
2026-07-02 style: use decho+setLink for unstyled clickable buttons in TargetView — dechoPopup added visible blue underline that cannot be suppressed; replaced with mc:decho(colored label) + mc:setLink(luaCmd, hint, false) pattern which attaches click handler without changing text appearance; toggle likewise changed from dechoLink to decho+setLink; button callbacks use MyDSL.Target.doAction(key) which already guards nil target
2026-07-02 fix: use dechoLink for clickable Target buttons — mc:setLink() does not fire click handlers on Geyser MiniConsole in Mudlet 4.20.1; replaced toggle [M]/[P] and all 6 action buttons with mc:dechoLink(text, luaCmd, hint, false); inter-button space folded into text arg for cols 1–2; mc:decho("\n") retained for row breaks
2026-07-02 fix: proper DSL command quoting for multi-word target names — added normalizeName() (strips articles a/an/the, commas, dots, normalises spaces) and commandArg() (apostrophe→last-word fallback, multi-word→single-quoted, single-word→bare); all 7 non-flee cmd functions in TV.actions now call commandArg(t.name); "a gnome student"→murder 'gnome student', "philosopher's assistant"→murder assistant
2026-07-02 milestone: Scan/RightHere/Target confirmed working in live combat — murder 'gnome student' landed, flee confirmed, full pipeline scan→righthere→target→action working end-to-end; tagged v1.4-scan-target-combat-confirmed
2026-07-03 feat: wire group triggers and catch-all body in DataLayer — beginGroup() now installs tempRegexTrigger(".*") catch-all (mirrors beginScan pattern); catch-all feeds lines to parseGroupLine(), calls endGroup() on blank line, delegates body gagging to MyDSL.GroupView.config.gagGroup check; endGroup() kills groupBody trigger before update(); Section 10: added MyDSL._triggers.groupStart on "^.+'s group:$"
2026-07-03 feat: add MyDSL_GroupView — group member display window — GV.render() reads State.group.members; class tag [War/Mob/etc] colored blue(player)/dim-yellow(mob); name truncated to 20 chars colored tan(mob)/near-white(player); HP% colored green/yellow/orange/red by threshold; mana% in blue and mv% in light-green shown only when <100; setGag() installs header gag trigger; body gagging delegated to DataLayer catch-all via config.gagGroup flag; "mydsl group gag/ungag" aliases
2026-07-03 fix: PCRE regex in group header triggers — tempRegexTrigger uses PCRE not Lua patterns; %' is wrong in PCRE (stray backslash); changed "^.+%'s group:$" to "^.+'s group:$" in DataLayer groupStart trigger and GroupView gagHeader trigger; the Lua string:match() in scanBody catch-all is unaffected (Lua %' is valid there)
2026-07-03 feat: expand TV.actions library with 10 healing/curative/buff spells — cure_light/refresh/cure_serious/cure_critical (green 68,204,68); cure_blindness/cure_disease/cure_poison/cure_fatigue/cure_bugbite (lavender 170,170,255); sanctuary (gold 255,215,65); all use commandArg(t.name); available to mobset/playerset immediately
2026-07-03 feat: add Clear button to TargetView line 1 — dim-red dechoLink "[Clear]" (170,68,68) inserted between [M]/[P] toggle and target name; only rendered when a target is set; calls MyDSL.Target.clear()
2026-07-03 feat: GroupView overhaul — mana%/mv% always shown (removed <100 conditionals); name is now dechoLink calling GV.setTarget(idx) with tooltip "Click to target: name"; quick-action buttons after mv% from GV.config.quickActions (default heal+rescue), reuse TV.actions entries, no mob/player filtering; added GV.setTarget(idx) and GV.quickAction(idx,key) functions; added "mydsl group quickset <k1> <k2>" alias; Contract_GroupView.md created; Contract_TargetView.md updated
2026-07-03 fix: PCRE \s/\S in tempAlias patterns — tempAlias uses pure PCRE (no Lua-% translation); %s/%S in three alias patterns were literal and never matched; fixed mobset/playerset in TargetView and quickset in GroupView to use \\s+/\\S+ (Lua string literal for PCRE \s+/\S+)
2026-07-03 fix: PCRE regex sweep — all remaining Lua-pattern escapes in tempRegexTrigger patterns removed; confirmed by live test (scan sw never populated Scan window) + Mudlet wiki; 7 patterns fixed across 3 files: DataLayer scanDir(%a+%.)→[a-zA-Z]+\\., sunrise/sunset(%.)→\\., weather([^%.]+%.)→[^.]+\\., loreStart(%s)→\\s; ScanView gagDir(%a+%.)→[a-zA-Z]+\\.; TargetView considerLine2(%.%.%.  )→\\.\\.\\. ; docs/MyDSL_MudletAPIReference.md created with confirmed PCRE rule + full bug table
2026-07-04 feat: add order_attack to TV.actions — "order all murder <arg>"; label "Order All"; red (204,68,68); opt-in only via mydsl target mobset/playerset, not added to default mob_buttons or player_buttons; Contract_TargetView.md updated
2026-07-04 fix(3 bugs confirmed live): (1) commandArg() now always returns last word only — DSL keyword matching only works on a single word regardless of quoting, confirmed via cast heal 'wild bear'/stallion/fire elemental all returning "They aren't here." while bare last-word succeeded; debug logging removed from doAction/quickAction; (2) GroupView rescue button now hidden for Mob rows — rescue only works player→player, confirmed rescue bear fails every time, order bear rescue kien succeeds; (3) DataLayer parseScanLine rightHere[key] now gets its own independent counter instead of a reference to byName[key] — fixes RightHere showing full-scan count (×6) instead of room-only count (×2); Contract_TargetView/GroupView/ScanView and MudletAPIReference updated
2026-07-04 feat: MyDSL_CombatView.lua — Phase B Combat window; DataLayer extended with State.combat, Section 9q (severity ladder 26 entries PNP-tuned, condition ladder, parseCombatDamageLine/AvoidLine/ConditionLine/DeathLine/EndLine/ProcLine), Section 10 unified damage trigger + 5 avoidance triggers + condition/death/flee/rescue triggers + 14 proc triggers (Frost/Flaming/Shocking/Vampiric/Stunning/ManaDrain/Holy/Unholy + Poison 3-step) + round-flush handler on MyDSL.time.updated; hp_raw added to char_data GMCP handler for rage detection; CombatView.lua with render()/renderSummary()/renderRage(), 7 aliases (clear/history/gag/ungag/show/hide); NOTE: requires dofile() wrapper in Mudlet Script editor (same pattern as ScanView/GroupView/TargetView)
2026-07-04 fix(5 post-review): three-way comparison (PNP source / contract / shipped code) found: (1) compound-noun proc forms ("life drain"→H vampiric, "shocking bite"→L lightning) never got flagged — NOUN_FLAG_MAP lookup added after noun capture in parseCombatDamageLine; vampiric also increments rage.vamp +2.5; (2) State.combat.last_updated never set — added to initializer (=0) and stamped os.time() before raiseEvent in round-flush handler; (3) combatCondition trigger missing gag check — gag_combat deleteLine() added matching pattern of combatDead/combatFlee/combatRescue; (4-5) config defaults all show_*=true gag_combat=false were wrong — corrected to all show_*=false echo_to_main=true gag_combat=true matching PNP's actual tested out-of-box behavior (display opt-in, combat gagged by default); Contract_CombatWindow.md Public API section updated
2026-07-05 fix(combat): replace goto/::continue:: in CombatView.render() — Mudlet uses Lua 5.1/LuaJIT which has no goto/::label:: (added in 5.2); file threw syntax error at dofile() time and never loaded; refactored per-entry loop body into local renderRoundEntry(mc, rd) with return in place of goto; grep confirmed no other MyDSL Lua file uses this pattern; MyDSL_MudletAPIReference.md updated with permanent Lua 5.1 version note and local-function pattern
2026-07-05 fix(combat): dodge/parry/block triggers only matched third-party phrasing ("Mob dodges Name's attack") and missed the you-as-subject/your-attack forms ("You dodge Mob's attack.", "Mob dodges your attack.") because the old regex hardcoded "dodges"/"parries"/"blocks" (3rd-person verb form only) and required a literal "'s attack"; replaced all three with DSL_PNP_Battle.lua's verbatim tested patterns (dodge/parry/block[s]? + your|Name's alternation) ported to our PCRE double-backslash convention; parseCombatAvoidLine(evader, verb, attacker) now reads the 3 capture groups directly instead of re-parsing getCurrentLine() with its own Lua patterns (sense triggers unchanged, still whole-line parsed); cross-check against PNP condition/death triggers found a confirmed-live gap (DSL-Logs show "You have some small wounds"/"You are in excellent condition"/"You look pretty hurt" -- second-person verb forms) that neither PNP nor our CONDITION_PATTERNS table handles at all -- self-condition currently never registers; not fixed here, flagged for follow-up
2026-07-05 audit: full PNP cross-check beyond evasion triggers (per Steven's request) — verified parseCombatAvoidLine cleanup (single 3-arg def, no duplicates), reworded combatDodge comment for accuracy; confirmed via DSL-Logs that self-condition uses second-person verbs ("You have some small wounds"/"You are in excellent condition"/"You look pretty hurt") vs third-person for others -- gap flagged in 2026-07-05 evasion-trigger entry above, still unfixed; searched DSL_PNP_Character.lua and DSL_PNP_Affects.lua for finishing-blow/execute mechanics -- none found, but DSL-Logs reveal "<mob> hits the ground ... DEAD." is a very common death form (188 occurrences in one session log alone) that NEITHER PNP nor our combatDead trigger (" is DEAD!!$") handles at all -- likely means most player-delivered killing blows never fire MyDSL.combat.ended; re-verified all 14 flag/proc trigger regex against DSL_PNP_Battle.lua lines 476-489 character-for-character (all 14 match) but found a deeper issue: PNP's flag handler never trusts names captured by the flag trigger itself (uses last_attacker/last_target/last_noun from the most recent damage line instead) specifically because those captures are often a WEAPON name, not the wielder -- confirmed live via DSL-Logs ("A whisper thin blade of satiny steel draws life from...", "is knocked to the ground by a grand arcanium polearm."); our procFlameBurn/procShockLightning/procVampDraw/procStun (and likely procFrostFreeze) pass that captured name straight into normalizeKey() as the attacker key, so entry.by_attacker[attackerKey] lookup silently fails and the flag is dropped every time -- not fixed here, architecture decision needed (adopt PNP's last-attacker-tracking model); staleness check found Contract_ScanView.md and Contract_TargetView.md both still show pre-fix PCRE code samples (scanDir %a+%./considerLine2 %.%.%. ) even though live code was already corrected 2026-07-03 -- Contract_GroupView.md is current; added "read PNP source directly" caveat to Contract_CombatWindow.md and MyDSL_PNP_Reference.md; docs/claude_export_2026-07-05/ created with state report + current file copies for Steven to upload to Claude.ai project
2026-07-05 fix(combat, 3 confirmed issues from prior audit): (1) added combatDeadGroundHit trigger + parseCombatDeathLine now also matches "<mob> hits the ground ... DEAD." alongside "<mob> is DEAD!!" -- confirmed via a full session log with 188 kills of this form and zero "is DEAD!!"; snapshotFight() already no-ops on an already-cleared target so no double-snapshot risk if both fire; (2) parseCombatProcLine now checks isKnownCombatant(attackerKey) (you/group member) -- weapon-flag procs whose "attacker" capture is actually a weapon name (confirmed: "A grand arcanium hoopak draws life from Rylae.", "is knocked to the ground by a runehammer.") now get their own by_attacker[weaponKey]["(proc)"] pseudo-row instead of being silently dropped; deliberate simplification, not full wielder resolution -- documented in Contract_CombatWindow.md; (3) procFrostFreeze/procVampDraw/procStun char classes now include " (double-quote) and strip it via new stripQuotes() helper before normalizeKey -- fixes quoted weapon names like "Nadrik's Honor" breaking the match; all three verified with luajit against real captured text. Also added a CONFIRMED note to MyDSL_MudletAPIReference.md that templates_by_freq.txt/templates_with_examples.txt are first-pass only -- both files are missing every death-ground-hit and weapon-flag-proc example despite dozens of real occurrences in log/, so absence there isn't evidence of absence; Contract_CombatWindow.md updated with all three fixes, the pseudo-attacker-key design note, and an updated Data Model example showing the new "(proc)" row shape
2026-07-05 docs: full project staleness audit after removing Claude.ai from the workflow -- spot-checked every Contract_*.md against live code; found Contract_TickSource.md's 3 listed gaps (warnTime, handler dereg, loop generation) already fixed by commit b16ec52 and never updated, Contract_ChatWrapper.md's 3 of 5 gaps (5s forced rebuild, fallback window position, fragile window-key lookup) already fixed and never updated (2 gaps -- hardcoded tab CSS, non-char-bound settings -- confirmed still open); TODO.md's DataBridge gap list (6 items) all confirmed fixed, RouteHelper's routeMap-removal already done and its decho/appendBuffer "gap" was never a real bug, DataLayer's two Phase-A score issues (stance/profession) confirmed fixed but TODO.md still listed as open; TODO.md's entire Phase B section said Combat/Scan/Group/Target were "not started" when all four were built -- full rewrite; SESSION_START.md rewritten (workflow section replaces old three-tool framing, Contract Status table updated); DSL_SessionNotes.md deduplicated (June 28-29 entry was pasted twice) and given a reconstructed summary entry for 2026-07-01 through 07-04 (no session notes existed for that stretch -- work is in CHANGELOG but was never logged here) plus a 2026-07-05 entry; SYNC.md's Module Inventory/Phase B Progress/Recommended Next Actions updated in place (namespace/event-convention sections untouched, still accurate); README.md rewritten to drop the three-tool/upload-folder framing entirely; DSL_CommandRef.md's stale TODO checklist corrected (scan/group/consider/lunar/time/weather were already captured elsewhere but never checked off; equipment/inventory genuinely still missing); MyDSL_MudletAPIReference_Part2.md was confirmed absent from the repo mid-audit, then Steven added it back (in two near-duplicate versions -- kept the corrected one, which itself fixed a stale "resize handler still needs retiring" section describing a 2026-06-25 fix as still-pending)
2026-07-05 docs: addressed three concerns raised by Steven -- (1) audited character-binding: confirmed no functional Kien hardcoding in live .lua code (all "Kien" hits are comments or legitimate per-character save data), but found 2 previously-unflagged gaps -- LayoutEngine window positions (MyDSL_layout.lua) and TargetView button config (MyDSL/targetview_config.lua) are both single shared files, not character-bound, contradicting the project's own recorded decision; added to TODO.md alongside the already-known ChatWrapper/WindowRegistry gaps; (2) confirmed cross-session file access already works via .claude/settings.local.json's Read(//home/owner/**) blanket grant (git-ignored but persists on disk across sessions on this machine); documented all sibling Mudlet profiles (PNP/PNP1/PNP2/DSL1/DSL-Kien/etc.) and ~/Downloads as known reference locations; (3) formalized "read PNP files/ and log/ directly, don't trust distilled summaries" as a standing practice, not a one-off. CLAUDE.md rewritten: removed Kien-primary-character framing that implied character-specificity, added explicit universal-character principle, added Reference Material section, added Character-binding status section, fixed stale "do not start Phase B" blocking language (Phase B is done) and stale "scripts not on disk" note. Also saved 4 cross-session memory entries (workflow change, verify-against-source lesson, universal-character requirement, reference locations) so this persists even before CLAUDE.md gets read.
2026-07-05 chore: post-restart commit review with Steven -- committed all combat fixes/doc audit work + EMCOChat (vendored dependency) + PNP files/ (46-file source reference, actively used for porting/cross-checking) + colors.xml (People/Friends name-coloring module, candidate for future MyDSL incorporation); removed MyDSL_Layer2/MyDSL_Layer2/ (stale nested duplicate, never tracked) and docs/claude_export_2026-07-05/ (dead weight after Claude.ai removal); .gitignore expanded for AdjustableContainer/ (confirmed auto-generated window-position cache, not modules), mpkg/+mpkg.packages.json (generic package-manager tool, not project-specific), DSL_PeopleColors_data* (accumulated data, not the module), loose Mudlet runtime/profile-connection files, and loose asset files; added two items to TODO.md surfaced from Steven's notes_utf8.txt (GroupView heal-on-pet bug, TargetView aura-based auto-target idea) -- notes_utf8.txt itself left untouched, it's Steven's actively-used in-game/feature notes file, not something to commit or ignore
2026-07-05 fix(combat): weapon-flag proc lines were never gagged from main console -- confirmed live via Steven's own cecho note during testing showing a raw "draws life from" Vamp-proc line staying on screen; none of the 17 proc trigger handlers ever called deleteLine(), unlike every other combat trigger which all gag correctly; added shared gagIfCombatGagged() helper, wired into all 17. Reviewed Steven's post-testing logs and cecho notes: no sustained combat occurred this session (no murder/damage verbs/deaths in any of the 16:21-18:08 logs), so evasion/death-form/group-fight-tracking fixes remain unconfirmed by real combat; one positive signal found (a solo "group" listing populated correctly). Confirmed and documented an important methodology limitation: Mudlet's HTML session logs never capture custom UserWindow/MiniConsole content (verified by grepping for "Fight summary"/"Right Here:" across every log including the session with a screenshot proving the fight-summary rendered at that exact moment -- zero matches, ever) -- logs can only confirm raw text/trigger-firing, never window display; that needs a screenshot. Also flagged (not yet resolved): a separate, pre-existing "itemstats" item-identification trigger system (319 hits in current/autosave.xml, unrelated to PNP files or our own code) does a bare cecho() with no leading newline, a plausible mechanism for silently breaking our $-anchored combat regexes when an item name appears inline in a combat line -- not conclusively proven this session, needs a deliberate test.
2026-07-05 feat: per-window logging for Combat/RightHere/Group/Target -- Steven asked if we could make custom windows log since Mudlet's session logs can't capture them; confirmed via wiki/manual search that startLogging() is main-console-only with no built-in per-MiniConsole/UserWindow equivalent. Added MyDSL.logWindow(category, text) in DataLayer (Section 2), mirroring any window write into MyDSL/logs/<category>/<YYYY-MM-DD>.log (plain text, decho color tags stripped, one file per day, matching the already-scaffolded-but-unused MyDSL/logs/{chat,combat,events,raw}/ directories from earlier in the project). Wired into all mc:decho()/mc:dechoLink() call sites in CombatView (combat), ScanView's renderRightHere (righthere), GroupView (group), and TargetView (target); created the righthere/group/target log subdirectories to match. Contract_CombatWindow.md documents the mechanism.
2026-07-05 fix: made the new per-window debug logs (MyDSL.logWindow, added this session) character-bound -- MyDSL/logs/<category>/<CharName>/<date>.log instead of a flat shared file per category, using MyDSL.Char() (falls back to "Unknown") + the same safeFileName() sanitizer as AffectsView. Steven's call: a shared file across characters would get confusing once more than one character is tested in the same day, which already happened this session (Vrokt + others). Verified the recursive mkdir -p path creation works for the new nested structure.
2026-07-05 fix(combat): round-flush handler was wired to the wrong event -- confirmed root cause of Steven's live-test report ("still only seeing the combat report not the actual combat in combatwindow") after fighting on Vrokt; MyDSL/logs/combat/Vrokt/2026-07-05.log showed only the Fight summary block, zero round-by-round lines, proving the earlier show_damage_by_me/show_damage_to_me/echo_to_main removal was necessary but not sufficient. Root cause: MyDSL._handlers.combatRoundFlush listened on "MyDSL.time.updated" (fires only when the player types "time") instead of "MyDSL.char.updated" (raised on every gmcp.char_data packet -- the actual once-per-round vitals-refresh signal); round_data accumulated correctly the whole fight but never got flushed to CV.render() until either a manual "time" or a kill via the separate snapshotFight() path, which is exactly why fight summaries worked while the live round log never appeared. Fixed by switching the event name.
2026-07-05 fix: font sizes for Combat/RightHere/Group per Steven's screenshots -- Combat 10->9, RightHere had no explicit fontSize at all (fell back to Mudlet's tiny default) -> now 9, Group 10->8 (row of class tag + name + hp/mana/mv + 2 buttons was overflowing the window width).
2026-07-05 feat: wire up "Players near you:" -> MyDSL_PlayersNear routing + gag -- Steven asked "where does that stand?"; investigation found MyDSL.Route.players() already existed fully working in MyDSL_RouteHelper.lua but was never called from anywhere, and MyDSL_PlayersNear was a registered window with nothing ever routing into it (the existing scan catch-all only used the "Players near you:" line as an end-of-scan-block signal, never gagged or routed it). Added MyDSL.beginPlayersNear()/endPlayersNear() in DataLayer Section 9o.2 (mirrors beginScan/endScan exactly) plus a permanent "^Players near you:$" trigger; captures the header + each "<Name><padding><Room>" line (confirmed exact shape from log/) via Route.players(nil) (appendBuffer, preserves original colors) and gags each from main console, ending on the blank line. Contract_ScanView.md corrected (previously claimed the opposite). Note: triggered by Steven's own external "autowhere" alias (runs `where` every ~20s), not part of this project.
2026-07-05 fix: who-list parser (parseWhoLine) was silently corrupting kingdom/name fields -- first finding from the new systematic bottom-up integrity audit (per Steven's request to re-verify the project layer by layer against the reuse-PNP/EMCO mandate, starting at DataLayer). Cross-checked against DSL_PNP_People.lua's tested regex and DSL_CommandRef.md's own documented format, then confirmed live in log/: clan/org codes are in (parens) -- (NT), (VR), (Abaddon) -- not [brackets] as the old code assumed; brackets are for kingdom names only. Old entry.clan was therefore always nil, and leftover parenthetical text like (WANTED)/(VR) shifted every following word by one position, corrupting kingdom/name for any WANTED- or clan-tagged who-list entry. Rewritten to extract every ()/[] group in order and classify status tags (WANTED/Hostile/AFK -- confirmed live in bare/bracket/paren forms, DSL isn't consistent) vs org/kingdom text; verified against 5 real log examples. Dropped an unconfirmed "quiet" field never found in log/ or DSL_CommandRef.md. Also confirmed (separately) that our GMCP-first vitals approach is correct as-is, not a reinvention gap -- PNP's Statusbar module is text/prompt-based only because it predates/works around limited GMCP.
2026-07-05 docs: consolidated all confirmed combat patterns (damage/evasion/condition/death/procs) into DSL_CommandRef.md's new COMBAT section -- this file exists specifically to prevent rediscovering a pattern twice, but combat work all session had never been written back into it. Also refined the WHO COMMAND section with the AFK-delimiter-inconsistency finding from the parseWhoLine fix. Vendored DSL_Helpfiles/ (919 official in-game help texts) from Steven's Claude.ai /mnt uploads -- usage/syntax reference, not raw output format, so log/ remains authoritative for exact trigger text. CLAUDE.md and README.md updated to point at both as standing reference material.
2026-07-05 fix: font tuning (Combat 9->8, History added at 8 via new per-window override in RouteHelper) + RouteHelper-routed windows (History/PlayersNear/Scan/Bloodbath) now get MyDSL.logWindow() coverage, closing a gap where only CombatView/GroupView/ScanView-RightHere/TargetView had it. Confirmed via log/ before/after comparison that the PlayersNear routing fix from the prior commit is genuinely working live (127 untouched "Players near you:" occurrences pre-reload, zero post-reload) -- a screenshot that appeared to show it not working was just a stale pre-reload moment. Investigated Steven's Skills/Spells-to-Combat-window request: confirmed no PNP precedent exists (DSL_PNP_Battle.lua has zero spell/skill handling, this would be new ground like our Poison addition) and real skill-action text exists ("You disarm Jhawsh!", "You kick!") but needs a dedicated cataloging pass before it can be responsibly scoped -- not enough confirmed patterns yet, logged as its own TODO item rather than a rushed implementation. Also discovered ~/Downloads/Shattered-Archive-release-dev.zip, an open-source DSL-specific MUD client/tooling monorepo -- flagged as worth checking before Layer 4 reference-library work, not audited yet.
2026-07-05 feat: made per-window logging toggleable -- Steven didn't want "players near you" logged (fires every ~20s, pure noise). Added MyDSL.LogConfig in DataLayer (enabled master switch + disabled_categories per-category opt-out), checked at the top of MyDSL.logWindow(); playersnear disabled by default. New aliases: "mydsl log on/off" (master), "mydsl log <category> on/off" (per-category, e.g. re-enable playersnear if ever wanted). Contract_CombatWindow.md documents the mechanism.
2026-07-05 docs: Claude.ai knowledge cross-check confirms clean-slate on 5 open items (chat routing duplication, roller-vs-PNP comparison, Skills/Spells-to-Combat as a distinct feature, the command-surface retrofit plan, Layer 4/Shattered Archive) -- Claude.ai had no prior design work on any of these, so no research to recover, all genuinely fresh work for Claude Code. Recorded in TODO.md so future sessions don't go looking for prior context that doesn't exist. Also fixed a real internal contradiction the cross-check surfaced: Contract_CombatWindow.md's "What This Module Does NOT Do" section still said combat tracking ignores ambient/non-group fights, directly contradicting the "Scope filter" section's own 2026-07-05 update that removed that filter on purpose (to match PNP's unfiltered behavior and surface group-member fights). Corrected.
2026-07-06 feat(combat): rewrote the display engine to port PNP's actual logic verbatim, replacing our from-scratch condensed-table format -- per Steven's direct request ("i would like to reverse our process... use pnp source and plug in what we want to do... make it work like pnp, then discuss the additions"). Traced DSL_PNP_Battle.lua's handle_damage/handle_evasion/handle_condition/handle_flag/calc_dam_verb/output_damage/battle_format functions in full and ported the ~50% that's pure logic (dam_info severity/decoration table, battle_format token substitution, per-swing sentence construction, round-summary aggregation) close to verbatim, replacing the ~50% that's PNP-infrastructure-specific (raw windowManager/sidebar/toggle system, PNP's own event bus) with our existing Geyser/event-name/tempRegexTrigger equivalents. Verified the ported format logic byte-for-byte against real confirmed combat examples via a standalone test script before considering this done (caught and fixed one bug this way -- an extra space accidentally inserted between the decorated verb and target name). MyDSL_CombatView.lua: Combat window is now a running per-swing log (CV.appendSwing(), never cleared) instead of a per-round condensed redraw, matching PNP's battle_console exactly; CV.config now mirrors PNP's actual defaults table; added "mydsl combat mode raw|condensed|gag" alias implementing the 3-way toggle directly on PNP's own gag_combat/summarize_damage flags. MyDSL_DataLayer.lua: round-aggregate summaries now go to main console gated by summarize_damage (independent of gag_combat, confirmed this matches PNP's actual output_damage() exactly); weapon-flag proc attribution now uses PNP's real last_attacker/last_target/last_noun technique instead of the pseudo-attacker-row workaround from the previous pass -- isKnownCombatant()/stripQuotes() removed as dead code. Contract_CombatWindow.md flagged with a staleness banner (full rewrite pass still needed, not done this round). Needs live-test confirmation -- none of this exercised against a real fight yet. Deliberately not touched: the persistent multi-fight "Fight summary" block (renderSummary(), our own addition, no PNP equivalent) -- kept as-is per Steven's own framing to discuss additions separately after the PNP-parity pass is confirmed working.
2026-07-06 docs: rules/docs contradiction sweep, per Steven's request to check for issues left over from before migrating to direct Claude Code conversations. Found and fixed real ones: deleted root SESSION_START.md (a stale duplicate predating Phase B entirely, containing a self-referential "Read SESSION_START.md" bootstrapping instruction that would trap a reader in stale content -- CLAUDE.md itself always correctly said docs/SESSION_START.md, so this wasn't actively read by instruction, just a landmine for anyone browsing the root directory); deleted root DSL_UI_Philosophy.md (exact byte-for-byte duplicate of docs/DSL_UI_Philosophy.md); fixed CLAUDE.md's wrong file path for MyDSL_Audit.md (said "in docs/ if present", it's actually at profile root, never in docs/); removed 5 phantom file references from docs/README.md's file index (claude_export_2026-07-05/ deleted mid-session-2026-07-05 without updating README; 2026-06-07_09-42-43.xml/MyDSL_WorkflowPlan.md/MyDSL_FeatureComparison.md/MyDSL_Backlog.md confirmed via full-repo search to not exist anywhere, likely phantom references inherited from the old Claude.ai project-folder index and never actually migrated); clarified docs/DSL_SessionNotes.md's misleading "June 9, research phase" header on what's actually a running log through 2026-07-05.
2026-07-06 docs: major documentation restructure -- deleted 27 files (19 per-module Contract_*.md, Contract_Addendum_2026-06-21.md, docs/SESSION_START.md, docs/SYNC.md, docs/DSL_SessionNotes.md, docs/README.md, docs/DSL_UI_Philosophy.md, root DSL_UI_Philosophy.md/SESSION_START.md from the earlier contradiction sweep, root MyDSL_PNP_Reference.md), per Steven's request to restructure around what actually works for direct Claude Code collaboration instead of the old Claude.ai-design-relay model these files were built for. Principle: docs should only hold what git history and the code itself can't already tell you. Kept: CLAUDE.md (standing rules, updated to describe the new leaner structure), docs/TODO.md (rewritten from a 615-line growing history into a ~180-line current-only punch list -- resolved items pruned, CHANGELOG/git already have that record), docs/CHANGELOG.md (unchanged, already the right shape), docs/DSL_CommandRef.md (confirmed regex/output patterns for DSL itself -- high value because it's about an external stable system, not our own changing code), the three MyDSL_MudletAPIReference*/WindowManagement.md files (same reasoning, about Mudlet/Geyser), templates_by_freq.txt/templates_with_examples.txt, MyDSL_IdeaBacklog.md, and root MyDSL_Audit.md (DSL1 historical audit, safe to keep since DSL1 no longer changes). Folded DSL_UI_Philosophy.md's genuinely distinct principles (move text don't replace it; automate to assist not to play; stale data beats spam) into CLAUDE.md's Philosophy section before deleting it. Verified the window-docking model in the now-deleted Contract_Addendum against live code before carrying anything forward -- found the addendum's detailed per-window docked=true/dockPosition model was itself superseded by a simpler global constructor-patch approach (patchUserWindowConstructor() injecting restoreLayout=true+autoDock=true for every window) that the addendum was never updated to reflect; recorded the verified-current fact in TODO.md's DECISIONS RECORDED section instead of copying the stale claim forward. Fixed 8 dangling references to the deleted files across DSL_CommandRef.md, MyDSL_MudletAPIReference.md, and two Lua source comments (MyDSL_DataLayer.lua, MyDSL_MoonWeather.lua), pointing each at the actual .lua source instead.
2026-07-06 audit: log-corpus regression test extended project-wide -- extended the trigger-pattern extractor (previously combat-only, 53 patterns) to parse every tempRegexTrigger/tempTrigger call across every MyDSL_*.lua module, including [[long-bracket]]/[=[...]=] Lua string literals and MyDSL_ChatTriggers.lua's route(tabName, pattern) helper indirection, then ran all 82 extracted patterns against the full 996,034-line log corpus. Confirmed real bugs (all added to TODO.md with detail): (1) MyDSL_ChatTriggers.lua channel routing is almost entirely non-functional -- of ~20 route() patterns only auctions: matches real DSL text; DSL formats gossip/shout/tell/pray/whisper as "Name verb 'message'" (quote-based), not "Name verb:" (colon-based); clan-says/kingdom-says are also the wrong verb (real: "X clan gossips '...'"); QUEST:/[OOC]/[OCLAN]/[OKING] don't match anything (only [OMB] found in corpus); (2) MyDSL._triggers.weather's pattern ^[A-Z][^.]+\.$ matched 131,876 of 996,034 lines (13%) including login-screen prompts, not just weather; (3) TargetView's considerLine1/2 confirmed still wrong (invented text, not real DSL "X looks like an easy kill." format, and not in PNP source either); (4) reconfirmed the combat/proc bracket-prefix zero-match findings from the prior combat-only pass. Investigated Steven's caveat that logs are heavily modified before trusting any of this: searched all of DSL2+PNP for scripts that insert the [51]/[ Coliseum Wall ] text; found nothing, then pulled raw unprocessed HTML directly and confirmed DSL itself sends "[ Wall Name ] Name verb 'message'" -- genuine server text, not client-added, for the room-tag prefix. Steven then corrected the level-tag half of this: PNP files/DSL_PNP_Highlighter.lua lines 261-264 (replaceNames()) DOES construct "[" .. info.level .. "] " .. name and inject it in place via replace()+cinsertText() for recognized clan members when its show_clanner_level config is on -- client-side decoration, not server text as originally concluded. Whether this actually corrupts what DataLayer's own triggers see live (vs. only appearing in saved logs) depends on trigger-priority ordering between Highlighter's trigger and ours, which is unresolved -- neither sets an explicit priority today.
2026-07-06 feat: added MyDSL_RawCapture.lua -- a new diagnostic-only module, one read-only catch-all trigger (pattern ".", priority 100, no deleteLine()/replace()) that logs every line to MyDSL/logs/rawcapture/<CharName>/<date>.log untouched by any other trigger, specifically to resolve the Highlighter-priority ambiguity above empirically. Off by default (toggle: "mydsl rawlog on"/"mydsl rawlog off") since it logs literally every line of the session. Needs Steven to place its dofile() first in Mudlet's Script editor load order (GUI-configured, not doable from here) and needs live confirmation that priority 100 actually beats Highlighter's trigger -- flagged in TODO.md, not yet tested in-game.
2026-07-06 audit: date-stratified the log/ corpus per Steven's question ("are the logs clean enough... latest logs will have the cleanest version because they come from dsl2"). Confirmed DSL2 itself never loads PNP's Highlighter at all (grepped every active MyDSL_*.lua file, zero references -- PNP files/ is read-only reference material, never dofile()'d into the running profile). Split log/ by date: the bracket-decoration and Coliseum-wall room-tag formats from the two most recent findings are >99% confined to Jan-May 2026 files (predating DSL2's own scripts, which start 2026-06-06 per git history) and almost certainly came from the actual sibling "Dark & Shattered Lands - PNP" Mudlet profile (has its own separate log/ going back to Nov 2025), not DSL2. Checked all 157 June+July 2026 (DSL2-era) log files: zero genuine occurrences of either format (9 apparent bracket-decoration hits were false positives on unrelated genuine DSL features -- item charge tags like "-[12] Identify", board-post numbers like "[1077] Scorn:"). Downgraded the procStun/procFrostFreeze/procShockLightning finding in TODO.md from "confirmed real bug" back to "unconfirmed, low priority" -- the mechanism is real and worth keeping documented, but isn't actually occurring in DSL2's own current play. Updated MyDSL_RawCapture.lua's header to reflect this (kept as generic future-proofing insurance, not an active concern). Conclusion for Steven: recent/future DSL2 logs are confirmed to be the clean corpus to test against, not just assumed.
2026-07-06 audit: full re-run of the trigger/alias/keybinding regression test against a proper DSL2-era-only corpus (built from the 247 dated session logs June 6 2026 onward, excluding the 42 EMCO per-tab export files which are post-routing output, not raw server text -- 328,643 lines total). Confirmed complete extraction coverage this time (82/82 real regex triggers across every MyDSL_*.lua file that registers any). Findings: (1) MyDSL_ChatTriggers.lua's confirmed-working count is 0 of ~20 route() patterns, not 1/20 as first reported -- the "auctions:" pattern that looked like the one working case had all 405 of its matches from pre-DSL2 data, 0 in the clean corpus; (2) three previously "confirmed working" proc/affect triggers (procFlameSear, procHolyWrath, procPoisonTick) plus AffectsView's Song: format turned out to have zero DSL2-era occurrences too (their matches -- 98/31/22/25 -- were entirely pre-DSL2), downgraded to unconfirmed alongside their siblings; the "sibling procs prove the mechanism works" reasoning from the original combat audit no longer holds since the cited siblings themselves lack DSL2-era confirmation. Checked keybindings (permKey/tempKey/setKey): none registered anywhere in DSL2 -- clean, nothing to flag. Extracted and tested all 71 tempAlias patterns against typed-command lines in the corpus (confirmed local echo is logged, so this is testable) -- 69 of 71 have never been typed in any DSL2-era session, but this is usage telemetry, not a bug signal (most are niche one-time config commands, expected to be rarely retyped). Cross-referenced every typed "mydsl .../toggle ..." line against the server's very next line to find real syntax gaps: found "mydsl help" typed 6 times across different sessions (always got "Huh?" back -- no such alias exists anywhere) and "mydsl history font 9" typed twice (also "Huh?" -- every other window has a "mydsl <module> font <N>" alias, History doesn't). Both added to TODO.md as concrete, user-requested gaps.
2026-07-06 fix(critical): found via real-code emulation (not regex-testing) that MyDSL_DataLayer.lua would crash on a genuinely fresh Mudlet start -- built a minimal Mudlet-API mock (tempRegexTrigger/tempAlias/cecho/Geyser stubs/etc, in scratch, not committed) specifically so the real file could be dofile()'d into a fresh Lua state exactly like Mudlet's first-ever load, instead of only checking whether patterns match text. First attempt crashed immediately: "attempt to index field '_aliases' (a nil value)" -- MyDSL._aliases was referenced by the log-toggle/help aliases (line ~86) before ever being initialized as a table (that init lived at line 205, added 2026-07-05, well after first use). This would have aborted the rest of the file's load on a true fresh start, meaning every trigger registered after that point -- all of combat, weather, chat, everything -- would silently never exist. Only masked in practice because Mudlet's Lua state persists across a same-session "reload this script," so MyDSL._aliases already existed from a prior load and the bug never surfaced during normal iterative editing. Fixed by moving _triggers/_aliases initialization to Section 1 immediately after the MyDSL = MyDSL or {} namespace guard; removed the now-redundant later re-initialization. Verified: file loads clean in a fresh Lua state via the mock harness. Needs Steven to confirm with an actual full Mudlet restart (not a script reload) before closing, since that's the only scenario that reproduces the original crash.
2026-07-06 plan: kicked off a comprehensive PNP+EMCO cannibalization pass per Steven's direction ("we are now going to cannibalize and take all we can from emco and the rest of PNP incorporate it cleanly into MyDSL UI, instead of patch or rebuilding items"). Ran a fork to inventory all 46 PNP files + EMCO's real alias surface against every existing MyDSL module and every open TODO item; independently re-verified its two highest-stakes claims myself directly against current/autosave.xml before writing the plan (approved, saved at ~/.claude/plans/soft-twirling-thunder.md). Headline finding: Mudlet's own native trigger tree has a complete, tested "Chat Routing" trigger group (dormant, isActive="no", but real tested reference material) with patterns for every channel -- mined directly from XML rather than guessed from logs a second time. Also confirmed EMCO's real native alias list (addtab/color/gaglist/gag/lock/notify/remtab/title/ungag/unlock/unnotify/update/usage -- no show/hide/font at all), correcting a wrong claim in CLAUDE.md that ChatWrapper had "reinvented" EMCO aliases that don't actually exist. Confirmed "Roller" isn't a MyDSL module -- it's a native alias literally named "roller", not a .lua file -- so the open "Roller behaves differently than PNP's" bug needs an actual read-and-diff before it can be fixed. Plan phases: A (ChatTriggers/CLAUDE.md correction, done this session below), B (AffectsView's unported .bar/.flags/.gag/.respell sub-features), C (Roller), D (new Character-assist module: disarm/spellup/standup), E (equipment/eq parser), F (Highlighter+DslColor merge, per Steven's follow-up message -- supersedes the plan's original assumption that Highlighter was out of scope). Explicitly out of scope: Statusbar (GMCP-first already correct) and ~20 PNP-client-infrastructure files superseded by Geyser/WindowRegistry/LayoutEngine/ThemeEngine.
2026-07-07 audit(Phase B): reassessed AffectsView completion, found the plan's initial scoping (from an earlier inventory fork's summary) was wrong -- read all four DSL_PNP_Affects.bar/.flags/.gag/.respell.lua sub-files in full, then re-read the actual current MyDSL_AffectsView.lua before assuming anything needed porting, rather than trusting the summary. Found .bar.lua's persistent color-coded active-affects grid is functionally superseded by the existing A.display()/echoActiveRows() (same red/yellow/green remaining-cycles thresholds, Geyser window instead of PNP's raw icon labels, plus clickable recast links PNP never had); .respell.lua is already ported near-verbatim as A.respell()/A.spellup() (same exclusion syntax, tracked-list merge, recast dispatch), confirmed A.recast() only reachable via user-initiated aliases/clicks. .flags.lua's prompt-integration isn't ported but its unique value is negligible given GMCP-first PromptView already replaced PNP's text-prompt system and the underlying tracked/highlight data need is already covered elsewhere -- not scoping a redundant variant. Confirmed .gag.lua should NOT be ported at all: it auto-sends the "affects" command with no user action whenever a buff lands, gagging the auto-triggered output -- a direct violation of CLAUDE.md's passive-observation mandate. No code changes this phase; the gap wasn't real once checked against current source.
2026-07-07 feat(Phase E): ported the passive-capture half of DSL_PNP_Character.equipment.lua into MyDSL_DataLayer.lua Section 9r -- beginEquip/parseEquipLine/endEquip, new MyDSL.State.equipment (mirrors to MyDSL.Data[char].equipment via the existing update() helper). Deliberately did not port PNP's useItem(), which sends wear/wield/get commands -- reserved for a future display-module button, not Layer 1 (matches this file's own "never sends commands to the game" header). Confirmed real equipment-line format against 154 unique lines across 113 full blocks in the clean corpus, including multi-flag items and item names containing their own parenthetical stat blocks (e.g. "(W)-1S,2D (R) 4Con,2H"). Ported PNP's slot-name normalization (strip worn/used/as/on/around/about/nearby/weapon, auto-number finger/neck/wrist to 1/2) faithfully -- confirmed correct against every real slot string seen (floating nearby, held, secondary weapon, sheathed, used as light, wielded, worn about body/waist, worn around neck/wrist, worn as quiver/shield, worn on arms/feet/finger/hands/head/legs/torso). Verified via the real-code emulation harness: all 113 real equipment blocks parse with zero errors.
2026-07-07 audit: full Mudlet-objects inventory per Steven ("incorporate what you are calling native into our bubble of work, you should have a full picture of ALL the things going on in mudlet"). Ran a fork to enumerate every native TriggerGroup/Alias/Key/Script in current/autosave.xml (142 TriggerGroups, 613 Triggers, 43 Scripts, 66 Keys, 25 top-level Aliases, 0 Timers) and cross-reference against MyDSL_*.lua for dead windows and dead/orphaned code. Confirmed a second copy of the already-flagged duplicate-routing bug's root cause detail (Toasts/History Captures + Trumpet Sounds and History, byte-for-byte identical children, both active). Found 4 registered-but-unfed MyDSL windows (Inventory/Equipment/AsciiMap/Banner) -- checked against MyDSL_IdeaBacklog.md before concluding anything, confirmed all four match planned-but-unbuilt ideas already in that backlog (not abandoned dead code like the removed Bloodbath was). Found and removed 5 genuinely dead functions in MyDSL_MoonWeather.lua (onLunarUpdate/onWeatherUpdate/onTickUpdate/onTimeUpdate/onLoginUpdate -- refined the fork's count of 4 to 5 via a direct whole-codebase grep) -- superseded by inline closures in _registerHandlers() during an earlier refactor, never deleted. Found 17 fully-written DataLayer functions across 7 complete capture pipelines (whok/whoc/inv/map/affectsText/improve/unread) that no trigger anywhere calls -- more precise than the existing "not yet audited" TODO hedge, these are confirmed unreachable, not just unchecked; updated that TODO entry accordingly. Catalogued native-only object categories with no MyDSL equivalent yet for future reference: Random Affects (11 stat-flavor messages), Spells category triggers, Weather Conditions (more precise than current weather regex), all 66 Keys, ~20 misc convenience aliases, Atmosphere's AGL/Areas/Sailing sub-groups. Confirmed DslColors_v1.0 is real/active/currently-loaded (seen directly in a fresh log) -- the DslColor half of the planned Phase F merge is live, not stale. Adopted "Mudlet objects" as the working term for triggers/aliases/timers/keybinds/scripts collectively, per Steven's request for one.
2026-07-07 fix: removed MyDSL_Bloodbath window per Steven -- not needed, bloodbath chat already goes to OOC and bloodbath announcements already go to History via a native mechanism. Removed the window from MyDSL_WindowRegistry.lua/MyDSL_LayoutEngine.lua and the dead MyDSL.Route.bloodbath() helper from MyDSL_RouteHelper.lua (it existed but was never called from anywhere -- same dead-window pattern as the "Players near you" gap fixed 2026-07-05). While checking this, found current/autosave.xml has two natively-active trigger groups ("Toasts/History Captures", "Trumpet Sounds and History"), both isActive="yes", both routing the exact same chat channels to the exact same destinations -- confirmed every single child trigger's script is byte-for-byte identical between the two groups. This is almost certainly the real root cause of the already-open "Clan gossip duplicates in chat window" bug -- updated that TODO entry with this concrete lead. Not fixed this pass (disabling a live native trigger group is a real config change, left for Steven to decide).
2026-07-07 docs: did a full validity scan of both MyDSL_MudletAPIReference.md and _Part2.md per Steven's direct request ("did you do a full scan... we have already had misleading issues before") -- previously had only spot-checked individual claims as they came up. Found two more real, confirmed inaccuracies in Part 2 beyond the Debug-window/EMCO-alias corrections already made same day: (1) Section 18 claimed "our existing chat routing triggers use permRegexTrigger" and stated a project principle of using permRegexTrigger/permAlias for all chat/gameplay triggers -- both false, directly contradicted by every single MyDSL_*.lua module touched this entire session, all of which use tempRegexTrigger/tempAlias with a per-module _triggers/_aliases table and a kill-and-recreate-on-reload pattern, zero exceptions; (2) same wrong permAlias framing repeated in Section 21. Corrected both sections to state the actual established pattern instead of quietly deleting the wrong claims. Also flagged (not confirmed wrong, just unverified) Section 19's EMCO Internals -- written from general knowledge, not checked against the actual vendored EMCOChat/emco.lua source, and at least one specific detail (append()'s internal call chain) doesn't match what was directly read from that source earlier this session (real implementation is self:xEcho(), not selectCurrentLine()+copy()+appendBuffer()). Section 1's core PCRE-vs-Lua-patterns claim and the Lua 5.1/no-goto claim both re-confirmed as solid -- both have live, repeated, direct empirical confirmation independent of any wiki citation, not just a web claim.
2026-07-07 docs: corrected the Debug-window/GMCP entry added to MyDSL_MudletAPIReference.md this same day, per Steven's request to double-check it -- the earlier version stated the icon/keyboard-shortcut/"Statistics button" details as flatly confirmed based on one WebSearch summary; a second search directly contradicted the first (disagreement over whether Ctrl+8 opens the debug console or the error console specifically, and whether "error console/statistics generator/debug console" are three separate features or one window). Both WebFetch attempts on the actual wiki pages returned HTTP 403, so neither claim could be checked against primary source. Rewrote the entry to separate what's actually witnessed (Steven's own captured session showing the GMCP-announcement text and a real display(gmcp) dump) from the unconfirmed web-sourced UI-navigation details, flagging the correction explicitly rather than quietly fixing it.
2026-07-07 audit: answered Steven's "what is the bloodbath window" question by checking source instead of guessing -- found MyDSL_Bloodbath is fully registered and positioned (MyDSL_LayoutEngine.lua: bottom overlay for bloodbath/event messages) with a ready MyDSL.Route.bloodbath() helper in MyDSL_RouteHelper.lua, but that helper is never called from anywhere -- same dead-window pattern as the "Players near you" gap found and fixed 2026-07-05. "Bloodbath" is DSL's PvP arena/tournament event channel (real chat format confirmed via the Phase A ChatTriggers work: "Bloodbath: '<message>'"), currently routed to the Chat window's OOC tab instead of this dedicated window. Added to TODO.md as an open gap, not fixed this pass.
2026-07-07 feat(logging defaults): per Steven -- combat/chat/history logs are useful to review later so they stay on by default; every other per-window log (group/righthere/target/scan/bloodbath/playersnear) is debug-only, off by default, opt-in when actually debugging that window. Updated MyDSL.LogConfig.disabled_categories in MyDSL_DataLayer.lua accordingly. Chat was never wired into this system at all -- it doesn't go through MyDSL.logWindow(), EMCO already has its own real per-tab logging (enableAllLogging()/disableAllLogging(), writing to log/MyDSL_EMCO_Chat/YYYY/MM/DD/<Tab>.html, on by default already) -- special-cased "mydsl log chat on/off" in the existing generic logCategoryToggle alias to call EMCO's real methods directly instead of duplicating chat content into a second file/format, keeping the same command shape as every other category. Verified via the emulation harness: correct default state for all 9 categories, "mydsl log chat off" correctly calls EMCO's disableAllLogging(), "mydsl log combat off" still uses the generic table-based path. Also researched Mudlet's Debug window (ladybug icon/Ctrl+8) per Steven's question -- confirmed via wiki.mudlet.org it shows live GMCP events and lua display(gmcp) dumps the full table, exactly the mechanism behind the "System Message: gmcp event... display(gmcp)" text found in logs earlier; added as a confirmed fact to docs/MyDSL_MudletAPIReference.md rather than bulk-downloading the manual (recommended against downloading -- it's a large wiki that would go stale, the existing curated-facts pattern in that file is the better fit).
2026-07-07 fix(MyDSL_DataLayer.lua logWindow()): Steven asked to sample today's per-window logs to check how logging turned out -- found GroupView's and TargetView's logs came out fragmented, one word/segment per line ("[Mob]", "A throughbred stalli" on its own line, " 100%hp" on another, etc.) instead of one coherent row per group member. Root cause: those two views build a single visual row from several separate decho/dechoLink calls (class tag, name, hp%, mana%, mv%, action buttons, then a final lone "\n" call), and logWindow() used to write one full log line per CALL regardless, unconditionally appending "\n". Combat/RightHere/History never hit this since those modules already pass one complete line per call. Fixed generally at the shared logWindow() level instead of patching each view: buffers per category now, only flushes a line when a "\n" actually appears in the accumulated text, so multi-call rows reconstruct as one line with one timestamp while single-call callers behave exactly as before. Verified via the emulation harness: GroupView's exact fragmented-call pattern now produces one coherent log line per row; Combat-style single-call lines unaffected. (Separately noted, not a bug: the "A throughbred stalli" truncation itself is GroupView's intentional 20-char name limit, `m.name:sub(1, 20)`, just cutting this particular 22-char name mid-word -- cosmetic, low priority, not fixed this pass.)
2026-07-07 fix(critical regression, AffectsView): Steven's live testing found affects counting down in ~2-3 real seconds instead of ~40 -- root cause: "MyDSL.Tick.Updated" does not mean "one real game tick happened", MyDSL_TickSource.lua's T.publish() raises it unconditionally from every caller including T.updateTimer()'s own ~0.25s internal progress-bar loop, not just the real once-per-tick function, with no reason string passed through to distinguish them. Fixed A.onTickUpdated() to guard on MyDSL.DB.tick.ticks actually changing (an integer only the real per-tick function increments) before decrementing, instead of decrementing on every event firing. Verified via the emulation harness: 20 simulated fast-pulse firings with no real tick change now cause zero decrements; each actual tick-count change causes exactly one.
2026-07-07 fix(MyDSL_CharacterAssist.lua): fixed the blind/no-light/can-see vision check using real data instead of a guess, after Steven did live in-game blind testing and captured a real GMCP dump ("lua display(gmcp)" while actually blinded, plus finding the native "Gag promt line1"/"Gag promt line 2" triggers in current/autosave.xml that confirmed DSL sends TWO distinct prompt lines, one of which -- the "[HP|M|MV] [stance | alignment | language | flags]" one -- DSL2 currently gags unconditionally with zero parsing). The GMCP dump confirmed gmcp.room_data.room becomes the literal string "darkness" -- exactly the value PNP's own text-prompt parsing checked -- and that MyDSL_DataLayer.lua's gmcp.room_data handler already stores this directly as MyDSL.State.room.name, no new trigger needed. Switched checkVision() from the earlier unverified MyDSL.State.room.sector substring guess to checking MyDSL.State.room.name == "darkness" directly. Also confirmed simpler exact-text alternatives in the same session ("You are blinded!"/"You can see again.") though not needed as primary. Re-verified via the emulation harness across all three vision states with real room-name transitions.
2026-07-07 audit: investigated the "prompt gag trigger 2" Steven mentioned -- found current/autosave.xml has TWO native prompt-gag triggers, not one: "Gag promt line1" (pattern "^\[\d+/\d+HP \| \d+/\d+M \| \d+/\d+MV \] \[ .* \| .* \| .* \| .* \]$") and "Gag promt line 2" (the "==-...::[room]::[exits]-==" one MyDSL_DataLayer.lua's parsePromptLine already handles). Line1 is currently gagged with zero parsing anywhere in MyDSL -- pulled real examples from the corpus and confirmed its 4 pipe-separated fields are: combat stance (Offensive/Normal/Defensive), alignment (neutral/evil), current speaking language (Common/Dragon/Yinnish -- directly explains the "(Language)" tags on chat lines found earlier this session), and a parenthetical status-flags slot (confirmed "(Flying)" appears there; never saw "(Blind)" in this specific corpus but the room-name-based check above makes that moot). This is a real, currently-unused data source -- not acted on yet, noted for a future pass since it wasn't needed for the immediate vision-check fix.
2026-07-07 decision: closed the .gag.lua discussion per Steven -- not built. The countdown fix (below) made it moot rather than the original philosophy objection deciding it: durations no longer need any server re-query to stay accurate now that they decrement locally per real game tick, so .gag.lua's entire reason for existing doesn't apply to DSL2 regardless of whether the auto-refresh approach would have been acceptable on its own terms.
2026-07-07 feat(Phase D): new MyDSL_CharacterAssist.lua, ported from DSL_PNP_Character.disarm/spellup/standup.lua after Steven reviewed and explicitly approved each feature (unlike every other cannibalization phase, this one does send real game commands): rearm (both the auto-trigger on any of 4 weapon-disarm/2 shield-disarm forms, and a manual "rearm" alias, matching PNP's own both-together design), standup (auto ~+stand on knockdown, no manual alias, matching PNP), spellup (setspell/bless/fireproof/stop spellup/resume spellup/ignore, full per-item cast/remove/re-equip loop with a 20s failsafe). Built useItem(slot, command, location) -- PNP's own command-sending primitive -- in this module specifically, not in MyDSL_DataLayer.lua which stays passive-only; reads MyDSL.State.equipment.slots (Phase E) without ever writing it. Derives item keywords by stripping filler words from the item name rather than porting PNP's "set keyword" manual-override alias (v1 auto-derive only). One unconfirmed piece: the blind/no-light/can-see vision check uses MyDSL.State.room.sector as a best-effort "dark" substring proxy since DSL2 has no equivalent to PNP's text-prompt room field it originally checked -- couldn't verify against real data, GMCP is never in text logs, flagged in-file. Verified via the emulation harness with real DataLayer-built equipment state: rearm (combat+full), rearm-shield, standup, the worn-duplicate "2.keyword" disambiguation, and a full "fireproof all" run (start -> per-item advance -> completion) all produce exactly the right command sequences, zero errors. Deliberately not built: DSL_PNP_Affects.gag.lua's auto-refresh -- reopened for discussion per Steven rather than staying excluded on my own unilateral call; noted that GMCP's affect_data only fires reactively per existing docs, and that the specific symptom motivating the reopen (frozen timers) turned out to have a separate root cause needing no server command at all.
2026-07-07 fix(AffectsView): fixed the affects-timer countdown per Steven's report that timers "didn't seem to work correctly." Found two real bugs: entry.duration was set once by GMCP and never decremented locally, so it stayed frozen between add/remove events instead of counting down; and the "time"/"both" timer-mode display looked up MyDSL.DB.timers.affects[name].text, a table nothing ever populated, so it silently always fell back to cycles-only. Checked whether GMCP itself already pushes fresh data periodically (also relevant to the .gag.lua question) -- confirmed via docs/DSL_CommandRef.md's existing note that gmcp.affect_data only fires reactively (typing "affects", or a spell start/end), not as a periodic push. Fixed with a purely local, zero-server-command approach instead, matching PNP's own DSL_PNP_Affects.bar.lua decrement_affects() (hooked to its onTick there): new A.onTickUpdated() decrements every active affect's duration by 1 on "MyDSL.Tick.Updated" (fires once per real game tick), while GMCP add/remove events stay authoritative and always overwrite it on any real change. Real-time minutes/seconds now computed directly from MyDSL.DB.tick.average (same field MoonWeather's clock already uses for this) plus MyDSL.DB.tick.remaining for smooth between-tick counting. Verified via the emulation harness: real affect ticked 10->7 over 3 simulated ticks, zero errors, permanent-duration affects correctly skip real-time conversion.
2026-07-07 feat(Phase C): ported Roller as new MyDSL_Roller.lua. Read the native "roller" trigger in current/autosave.xml in full -- it's a character-creation stat-reroll auto-rejecter (sums Str/Int/Wis/Dex/Con, auto-sends "n" below a hardcoded goal of 241, pauses for manual review at/above goal, never auto-accepts), not a combat dice-display as the vague open TODO item implied. DSL_PNP_Roller.lua does the same core thing plus real value-add the native version lacks: adjustable goal, running min/max/avg/stdev stats across all rolls, reset -- ported that near-verbatim, reusing PNP's own set goal/roll stats/reset roll command vocabulary, keeping the native trigger's already-tuned 241 goal as the default over PNP's generic 230. PNP's own trigger pattern (no brackets, double space) and the native one (brackets, single space) disagree and neither could be verified against any real log (character creation is one-time, not captured in available logs) -- made the pattern tolerant of both instead of guessing one over the other. Verified via the real-code emulation harness: both format variants match, goal-boundary logic and all three commands run error-free.
2026-07-07 audit: rescanned all logs/screenshots/notes for new gameplay activity. One log "error" hit turned out to be a false positive (Mudlet's normal "Socket got disconnected" message after Steven typed quit and reconnected, not a Lua bug) -- useful evidence for his reload-safety question, answered directly rather than added to TODO (see notes_utf8.txt: a simple character quit/reconnect is safe and doesn't reset Mudlet's Lua state, unlike a full application restart -- most MyDSL state persisting across it, like tracked spells and window layout, is intentional, not a bug). Investigated "affects window treating skills like stealth and riot as spells" with real data: riot is confirmed genuinely a spell (real "Spell: riot ... cycles" text, correctly tracked), but stealth is confirmed a %-based skill (via the skills listing) with no "Spell: stealth" line anywhere in tonight's logs -- likely can never register as active via the affects-duration mechanism, so it sits permanently in AffectsView's "missing" list. Added both findings to docs/TODO.md, split cleanly since only one is actually a mismatch. Also logged a new TargetView design request (alternate combat-opener command, e.g. waylay instead of murder, for characters like Vrokt) as a scoped-but-not-built design idea.
2026-07-07 feat/re-scope: Phase F (Highlighter+DslColor) fully re-scoped after reading every source involved -- read DSL_PNP_Highlighter.lua/.custom.lua (445 lines), DSL_PNP_People.lua (672 lines), colors.xml (4904 lines/95 triggers), and the live native DslColors_Core_v1_0 script (3220 lines) in full. Found colors.xml is not currently loaded (zero overlap with current/autosave.xml's live trigger tree) and calls functions that don't exist in the live engine -- a stale pre-v1.0 snapshot, nothing to merge from it. Found the live DslColors_Core_v1_0 is already a complete superset of PNP's Highlighter+People combined: a real dslcolor command surface (friend/enemy/neutral, war/peace, show, list, census, team, killed/killedby/downed/downedby + combat notes, name override, term, title, palette, ignore/save/reload/test) backed by a persistent DSL_COLOR_DB with per-person records (race/class/kingdom/clan/robe/dragon/title/team/first_seen/last_seen/room/crafts/combat history) and friend-enemy/war-peace relation tracking at both person and org level. Porting PNP's Highlighter/People now would just build redundant, inferior duplicate functionality -- moved both files to explicitly-out-of-scope alongside DSL_PNP_Statusbar.*. Built and syntax-checked the first real integration instead of just scoping: MyDSL_TargetView.lua now shows a [Friend]/[Enemy] tag next to the current target's name, read live (read-only, pcall-guarded) from DSL_COLOR_DB.relations.people -- setting relations still goes through the real "dslcolor friend/enemy <name>" command, no duplicate alias added. Updated docs/TODO.md's Phase F entry, the DslColor design-idea entry, and the out-of-scope list to match.
2026-07-07 audit: found and confirmed the itemstat A/B comparison Steven asked about, directly in the main session log. He ran eq/in with the native itemstat triggers on, then disabled them and ran eq/in again -- same items, and every single stat suffix vanished (e.g. "a ring of wizardry -[35] 30M,-1S" became plain "a ring of wizardry"). Confirms the itemstat system isn't only "type an item name as a command" -- it also live-decorates eq/in output lines with "-[level] stat,..." suffixes, entirely client-side. Surfaces a real dependency: MyDSL_DataLayer.lua's Section 9r equipment parser captures that suffix as unparsed text and only works today because the native itemstat trigger is what puts it there -- if itemstats get disabled permanently before Layer 4 ships a replacement, MyDSL.State.equipment would silently lose all stat data. Documented in docs/TODO.md's existing itemstats-interference item. Also reviewed 6 new screenshots (18:15-19:07) -- confirmed CreatureReference/lore working live against multiple real mobs (throughbred stallion, wild bear, gnome architect/factory worker), a third consider-tier ("The perfect match!") not previously recorded, and real affects-countdown/prompt-line-1 data matching what the logs already showed; no new bugs found in the screenshots themselves.
2026-07-07 audit: investigated Steven's new note ("im not seeing the debufs i cast on the target, like weaken and slow?"). Traced tonight's logs around his own "c weaken"/"c slow" casts and confirmed two real, distinct success messages: weaken -> "<target> looks tired and weak." (checked against the existing 7-phrase HP-condition ladder at MyDSL_DataLayer.lua:1723, not a match, genuinely separate), slow -> "<target> starts to move in slow motion." Per an in-game help newbie lookup found in an earlier log, these are "Maladictions" that persist until cured/cancelled rather than on a short fixed timer -- no MyDSL code currently tracks or displays either. Added as a new TargetView design item (enemy-side counterpart to AffectsView's player-side buff tracking), not scoped yet -- needs a wider catalog of maladiction spells and their end-text before building.
2026-07-07 audit: rescanned all logs for new info since the last pass. Found a real help-file lookup Steven did mid-session for the healing spell group -- confirmed the actual power-ordered hierarchy (cure light -> refresh -> cure serious -> cure critical -> heal -> mass healing, syntax cast '<name>' <character>, classes ASSASSIN/BATTLERAGER/CLERIC/CRUSADER/DRAGON/GIANT/DRUID/PALADIN/PRIEST/SHADOWKNIGHT/WARRIOR) -- folded into the GroupView multi-heal-tier design gap in TODO.md. Also found Steven's own lua cecho() marker in the log pinpointing the order-all-kill failure moment: surrounding text shows the game answered with a plain "Ok." and no attack, narrowing the bug toward a follower-targeting/state issue rather than a broken alias pattern (not confirmed). No new errors in any of today's three session logs; combat/chat still actively logging live; group/righthere/target/scan logs unchanged since the last scan (still need re-enabling this session if wanted).
2026-07-07 audit: folded Steven's live-test notes (notes_utf8.txt "claude notes" section) into docs/TODO.md. GroupView heal-on-Mob-rows resolved as not a bug -- Kien (has heal skill) healing a bear via the Group window worked live; the earlier failure was Tibbins, who simply has no heal skill at all. New design gap surfaced by the same test: more than one heal-tier skill exists and the Group window's [Heal] button needs to use whichever one the character actually has, not a hardcoded command. New items added: TargetView shows an attack-capable "kill" option on your own group members/followers (real safety issue, not cosmetic); native (oac) order-all-kill failed twice in live testing (first elf in combat, then a greaser from the target window); CombatView/History font sizes requested (8/7, should route through the existing per-window font-persistence pattern); action-button blue color contrast flagged as a real accessibility problem (unreadable without hovering for the tooltip); a design direction for more IC/human-speak command vocabulary; and the Skills/Spells-to-Combat idea clarified with three specifics (echo to main too, Combat should get "everything" like PNP's did, remove raw attack-spam in favor of real actions taken).
2026-07-07 audit: confirmed both native prompt gag triggers ("Gag promt line1"/"Gag promt line 2", both isActive="no" under MyDSL_GameplayTriggers) have served their verification purpose and can be re-enabled. Extracted real "==-...::...::...==" prompt-line text from fresh post-reload logs (e.g. "==-Dawn - 6:00am :: [darkness] :: [ESW]-=="), hand-traced parsePromptLine()'s pattern against it and confirmed it correctly captures the period name; the room-name and exits brackets on that line duplicate GMCP fields already captured elsewhere. Prompt Line 1's bracket format (stance/alignment/language/flags) was already confirmed from corpus data earlier and is likely fully redundant with GMCP too -- no parser built, none currently planned. Updated docs/TODO.md's "Parse Prompt Line 1" entry to record both as safe-to-regag rather than open questions.
2026-07-07 correction: retracted the "confirmed duplicate native trigger group" finding from the 2026-07-07 full inventory. That finding claimed "Toasts/History Captures" and "Trumpet Sounds and History" were two trigger groups with byte-for-byte identical children routing chat channels (including clan gossip), named as the likely root cause of the open "clan gossip duplicates" bug. Found wrong while trying to write exact GUI steps for Steven to disable one, by properly parsing current/autosave.xml with xml.etree instead of trusting the prior text-scan summary: both are single leaf Triggers (not groups) under a "Notifications" folder, with completely different scripts/regexes -- one matches PvP kill-toast phrases, the other kingdom-join/retirement/achievement announcements, neither matches gossip/chat text. Also confirmed the native "Chat Routing" group (which does contain three distinct, non-duplicate gossip triggers) is itself isActive="no" (dormant), ruling it out too. Nothing was disabled based on the wrong claim. "Clan gossip duplicates" reopened as fully unsolved -- needs live repro, not another guess. Corrected all three places in TODO.md that repeated this claim.
2026-07-07 fix(chat command-surface): closed the `emco <verb>` TODO item after tracing the actual object graph instead of implementing the previously-drafted fix blind -- that draft would have broken the live chat window. Confirmed emco show/hide operate on demonnic.container (the original native Adjustable.Container, permanently hidden by MyDSL_ChatWrapper.lua's C.hideOldPrebuilt() since MyDSL replaced it with its own WindowRegistry-integrated C.window), so mydsl chat show/hide are not a duplication and were left alone. Confirmed emco font/fontSize/blink/blankLine/timestamp read demonnic.chat fresh per-call, which MyDSL reassigns to its own live object -- so emco fontSize <n> and mydsl chat font <n> really do call the same ch:setFontSize() on the same object, a real but low-stakes duplication (left as-is since mydsl chat font also persists to MyDSL's own settings file). Added code comments at MyDSL_ChatWrapper.lua's C.setFont() and the show/hide alias registrations recording this so it isn't miscorrected again. Also traced (not fully verified) that emco save/load use a module-level upvalue only refreshed by a function MyDSL never calls, so they likely act on the stale original object rather than the live chat -- flagged, not asserted as confirmed. Updated CLAUDE.md, MyDSL_MudletAPIReference.md, and TODO.md to match.
2026-07-07 audit: resolved the last open EMCO-internals uncertainty in MyDSL_MudletAPIReference_Part2.md Section 19. Confirmed EMCOChat/emco.lua is byte-for-byte identical (same md5) to the genuine upstream ~/Downloads/EMCO-2.9.0.zip's src/resources/emco.lua -- unmodified real source, not a fork. Read EMCO:append()/EMCO:xEcho() directly (lines 1660/1781): append(tabName, excludeAll) calls self:xEcho(tabName, nil, 'a', excludeAll), and xEcho itself internally does getCurrentLine() gag-check -> selectCurrentLine() -> copy() -> deselect() -> resetFormat() -> console:appendBuffer(). Both previously-discussed descriptions were correct, just describing different call frames of the same chain -- no remaining uncertainty on this point.
2026-07-07 correction: re-corrected the EMCO `emco show/hide/font` claim a second time -- the 2026-07-06 "fix" (which said no such alias exists) was itself wrong. Root cause: that check read only ~800 chars past the `<name>emco</name>` tag in current/autosave.xml and never reached the `<regex>` tag, which sits after a long multi-branch `<script>` body -- so it concluded "not found" from an incomplete read, not a real absence. Re-checked properly (reading the full `<Alias>` block) and confirmed the bare `emco` alias is isActive="yes" with regex `^emco (save|load|font|fontSize|blink|blankLine|timestamp|show|hide)(?: (.+))?$`, matching ~/Downloads/EMCO-2.9.0.zip's own aliases.json exactly. So emco show/hide/font/fontSize/blink/blankLine/timestamp/save/load are all real, live commands, and MyDSL_ChatWrapper.lua:622-625 (mydsl chat show/hide/font) does genuinely duplicate them -- reopened as a real command-surface bug in TODO.md. Corrected CLAUDE.md, MyDSL_MudletAPIReference.md, and TODO.md to match, flagging the mistake explicitly in each rather than silently re-editing.
2026-07-07 fix(Phase A, third pass): per Steven, voice types and languages both affect real chat text and weren't handled generally by the second pass's narrow "(?: \([^)]+\))?" language-tag group. DSL_Helpfiles/voicetype.txt confirms 21 distinct voice types (Soft, Raspy, Low toned, Growlingly, Husky, ...) with irregular phrasing ("says softly"/"says in a raspy voice"/"says in a low toned manner" -- no single template), and real corpus data shows a "(to Name)" target tag can combine with a "(Language)" tag in either order (e.g. "Ariaenys says in a musical tone (to You) (Dragon) 'message'"). Rather than enumerate all 21 phrasings, replaced the narrow group everywhere with a general "[^']*'" zone (anything up to the opening quote) -- correctly covers any combination of voice-phrase/target-tag/language-tag/none without needing each one's exact wording. Verified via the clean corpus: counts stayed sane (Local says 2,759 -> 2,959, not unbounded) and all 139 real voice-type "says" lines are now covered (0 missed, some were missed by the narrower group before). Voice-type modifiers only appear on "says" in the corpus so far, applied the general form everywhere anyway as free future-proofing.
2026-07-06 fix(Phase A): rewrote MyDSL_ChatTriggers.lua a second time using the native-XML-sourced ground truth above -- corrects several things the first same-day rewrite got wrong from log-corpus guessing alone: clan gossip's real verb is "clan gossips" (two words, not the guessed "cgossips"), group chat is "tells the group" (not "group-says"), OOC is "Name OOC: 'message'" (not a bracket tag), congratulate's verb is "grats" (not "congratulates"), Kingdom is a literal "Kingdom: '" prefix (not a verb), plus three previously-unknown channels (Radios, Bloodbath, Quest). Also fixed a real bug in the first rewrite: its yell/shout patterns required the third-person -s form even for "You", so "You yell '...'"/"You shout '...'" (first-person) never matched -- native XML confirms both forms are real. Corrected several wrong tab-destination assignments (auctions/grats/radios/ask/newbie/bloodbath/quest all route to OOC, not City). Kept the confirmed-from-logs optional (Language) tag group on every pattern since that's independently verified real and better than the native reference. Every pattern re-verified against the clean corpus. Corrected CLAUDE.md's wrong emco show/hide/font claim and the matching TODO.md command-surface entry.
2026-07-06 audit: extended the real-code emulation to ChatTriggers and the render/display layer (CombatView/GroupView/ScanView/TargetView), completing the sweep. Loaded the real EMCOChat/emco.lua library into the mock (added string.trim/table.contains/deepcopy etc as needed) -- it loads clean, but a full EMCO:new() construction needs deep internal layout state (tabHeight, Geyser.HBox chains) that's really EMCO's own internals, not DSL2 code, so descoped that part as low-value. Instead verified what actually matters: fired each of the 6 confirmed-real ChatTriggers patterns' real handler against a real matched corpus line with a lightweight recording demonnic.chat stub -- all 6 correctly called append() with the right destination tab (Tells/City/Local), zero errors (first attempt fired every handler regardless of match since the mock's tempRegexTrigger doesn't gate on pattern -- test-harness gap, fixed by invoking only the specific handler Python had already confirmed matches each line). Then ran a full real fight sequence through the actual DataLayer parse functions in registration order (damage/avoid/condition/death, all real confirmed text) with raiseEvent upgraded to record instead of no-op -- confirmed the real previously-untestable local snapshotFight() fires via "MyDSL.combat.ended" with a real snapshot, fed that into the real CV.renderSummary() -- clean. Also verified CV.renderRage/CV.appendSwing (incl. nil/no-arg edge cases) and GroupView.render()/ScanView.render()/ScanView.renderRightHere()/TargetView.render(), each built from real state via their own real begin/parse/end functions -- all clean. One test mistake caught along the way (not a real bug): MyDSL.Target (data namespace) is not MyDSL.TargetView (render namespace) -- called the wrong one first, got a nil-value error, corrected. This closes out the real-code-emulation effort for DataLayer's parsing layer, ChatTriggers routing, and the four view modules' render layer -- all confirmed error-free against real captured data. AffectsView/MoonWeather/CreatureReference render layers not covered this pass -- not flagged as broken, just not yet exercised.
2026-07-06 audit: completed the real-capture parse-function stress test for MyDSL_DataLayer.lua (resumed after the daily-limit break, using the same scratch mock/harness). Fed real matched captures/lines from the clean 328,643-line corpus directly into every remaining parse function (bypassing trigger dispatch): parseCombatAvoidLine (273 dodge/parry/block matches plus the sense-trigger single-arg call shape), parseCombatConditionLine (628), parseCombatDeathLine (94), parseWhoLine (23,056 broad-net bracket-shaped candidates), and all 14 of parseScoreLine/parseFlagsLine/parseTimeLine/parsePromptLine/parseWeatherLine/parseWhokLine/parseWhocLine/parseGroupLine/parseUnreadLine/parseInvLine/parseMapLine/parseAffectsTextLine/parseImproveLine/parseScanLine against the entire corpus, not just pre-filtered matches. Zero real runtime errors found. Two functions (parseLunarLine, parseCreatureLoreLine) initially threw on nearly every call ("attempt to index local 'moon' (a nil value)", "bad argument #1 to 'insert' (table expected, got nil)") -- investigated rather than assumed either way: confirmed both are always preceded by beginLunar()/beginCreatureLore() in the real trigger flow (lunarBegin/loreStart handlers), re-ran with that setup against the full corpus, zero errors -- test-harness gap, not a real bug. This completes the DataLayer portion of the real-code emulation effort; ChatTriggers route() destinations and the Geyser-dependent render/display layer remain for a future pass, and GMCP-dependent logic can't be exercised this way at all since GMCP was never in the text logs.
2026-07-06 fix: traced the actual current-state pipeline before fixing anything (route() -> demonnic.chat:append(tabName) -> EMCO's xEcho('a' mode) copies the current triggering line, color preserved, no gag) then fixed all five confirmed gaps from today's audit. MyDSL_ChatTriggers.lua: full rewrite of every route() pattern, verified against the clean corpus per-pattern via a standalone script (not just eyeballed) -- CONFIRMED real text for tells (both directions)/local says/gossip/yell/whisper, all using "verb(?: (Language))? 'message'" (quote-terminated, optional language tag -- confirmed present on tells/gossips/yells alike) instead of the old invented colon convention; INFERRED (same convention, no direct example yet but DSL_Helpfiles/ confirms the command family) for shout and cgossip; dropped kingdom-says/auctions/NEWBIE/congratulates/QUEST/prays/group-chat/OOC entirely rather than replace one guess with another -- "prays" in particular turned out to be a travel-spell cast message ("Aothien prays for transportation!"), not a chat line at all, so the original pattern would have actively misrouted gameplay text into a chat tab if it had ever matched. MyDSL_DataLayer.lua: fixed parseWeatherLine()'s keyword filter from plain substring matching to a word-boundary Lua pattern (%f[%a]word%f[%A]) -- substring matching meant "sun" matched inside "Sunday", confirmed via corpus search to be actively corrupting MyDSL.State.weather with the log-session-start banner every time a new log file opened; verified the fix via luajit against the full 328,643-line corpus (false positives rejected, real weather kept, would-update count dropped from 33,801 to 687). MyDSL_TargetView.lua: considerLine1/considerLine2 (invented text, matched nothing) replaced with considerEasyKill/considerNoMatch built from the only two confirmed-real consider-output tiers found in the corpus ("X looks like an easy kill."/"X is no match for you.") -- other difficulty tiers DSL likely has are left for future confirmation rather than guessed. Added MyDSL.help() + "mydsl help" alias to MyDSL_DataLayer.lua (static command list) and MyDSL.Route.setHistoryFont() + "mydsl history font <n>" alias to MyDSL_RouteHelper.lua, closing both confirmed alias gaps from the prior audit. All four edited files syntax-checked via luajit loadfile(). None of this confirmed in-game yet -- per project workflow, TODO.md items stay open until Steven confirms live.
- 2026-07-07: TargetView no longer renders/sends murder or order-all-attack against your own group members or followers (checked by name against the live group roster); fixes the "clicked bear from group, got kill option" safety bug. Verified via emulation test, needs live confirm.
- 2026-07-07: RightHere now also refreshes on look (and any room reprint from movement), not just scan -- new beginLook/parseLookHereLine/endLook in MyDSL_DataLayer.lua, anchored on the room's own "[Exits: ...]" line, handles wrapped status-text continuation lines. Verified against a real captured room block, needs live confirm.
- 2026-07-07: Combat/History font sizes wired into real per-window persistence (mydsl combat font <n>, mydsl history font <n>), character-bound like TargetView/AffectsView; Combat default stays 8, History default dropped to 7. Verified via emulation tests simulating a full restart, needs live confirm.
- 2026-07-07: GroupView member-name truncation now uses AffectsView's cutText() convention (trailing ~ marker) instead of a flat mid-word sub(1,20) cut. Verified against the real 22-char mob name that exposed the bug, needs live confirm.
- 2026-07-07: found and fixed the likely root cause of "order all kill greaser did not work from target window": MyDSL_TargetView.lua's murder/order_attack actions always sent the murder verb (DSL's PKILL-only player-attack command per DSL_Helpfiles), never kill/hit, even against mob targets -- confirmed via the real native (oac) alias script (a trivial order-all-kill passthrough, not the bug source itself) and DSL_Helpfiles/"kill kill command hit.txt". Both actions now pick kill vs murder by t.is_mob; MyDSL_GroupView.lua quickAction() fixed to pass is_mob through too. Verified via emulation test, needs live confirm.
- 2026-07-07: investigated the long-standing "chat capture bug" (grabs first letter of following line, blamed on a GMCP S echo) -- checked every gmcp.* event handler in MyDSL_DataLayer.lua, none touch the raw text stream/selectCurrentLine/deleteLine at all, so no code-level mechanism found. Left open, redirected to MyDSL_RawCapture.lua for a live repro next time it happens rather than guessing further with no reproducible case.
- 2026-07-07: fixed the low-contrast blue action-button complaint -- MyDSL_TargetView.lua's Rescue/Cr.Blind/Cr.Disease/Cr.Poison/Cr.Fatigue/Cr.Bugbite buttons (and GroupView's quick-action Rescue, same shared color) changed from pale blue-violet 170,170,255 to a more legible soft cyan/teal 120,210,220; left the separate mob/player type-indicator blue alone since that's a category color, not an action. Needs live confirm.
- 2026-07-07: character-bound the three remaining shared-file settings gaps -- MyDSL_ChatWrapper.lua chat_settings.lua, MyDSL_LayoutEngine.lua MyDSL_layout.lua, MyDSL_WindowRegistry.lua MyDSL_windowstate.lua all now per-character files, matching the recorded all-settings-character-bound decision. Also added MyDSL.Layout.resetAll() + "mydsl layout reset" alias, and wrapped both saveState()/Layout.save() in pcall with a debug log on failure. All three files syntax-checked.
- 2026-07-07: ThemeEngine setOverride() now validates the key against MyDSL.Theme.defaults instead of silently accepting anything; logs and returns false on an unknown key. Verified via emulation test.
- 2026-07-07: chat capture bug (grabbed first letter of following line) confirmed fixed per Steven live testing. No specific code-level cause was ever pinned down; closing on live confirmation rather than a diagnosed root cause.
- 2026-07-07: fixed a real "Unknown" character-binding gap per Steven's question -- every character-bound module (ChatWrapper, LayoutEngine, WindowRegistry, TargetView, CombatView, History font) loaded settings at script-boot time, which happens before login on a fresh Mudlet start, so they'd get stuck on "Unknown"'s bucket/defaults for the whole session even after the real character logged in. MyDSL_DataLayer.lua's gmcp.login_data handler now raises MyDSL.character.identified; all affected modules re-load+apply on it. AffectsView needed no fix, already self-heals via checkCharacterProfile() on every render. Verified end-to-end via emulation test (boot under Unknown -> fire event -> real saved settings load correctly), all 7 files syntax-checked.
- 2026-07-07: cataloging pass for TargetView debuffs -- weaken/slow now confirmed with 4 independent real examples each (different mobs, identical template), ready to build. blindness/poison/plague confirmed as real cast-spells via DSL_Helpfiles but have zero confirmed on-target success text anywhere in the 93-file DSL2-era log corpus (only self-directed "You are blinded!" found); no wears-off text found for any of the five maladictions. Documented in TODO.md, nothing built yet.
- 2026-07-07: added mydsl who <name> as a real passthrough to the native DslColors dslcolor show <name> command (calls dslColorCommand() directly, not send(), since send() would just hit the server as an unknown command rather than re-trigger a native alias). pcall-guarded, verified via isolated logic test. Needs live confirm.
- 2026-07-07: skill/spell action-text cataloging pass for the "Skills/Spells -> Combat window" idea (216-file DSL2-era log corpus, found a previously-missed log/2026-0/ subdirectory). Found the item's two seed examples ("You disarm Jhawsh!", "You kick!") do not actually exist in DSL2-era logs -- likely misremembered. Confirmed real replacements instead: waylay, riot, frenzy, swarm, castigation, dragon roar, an 8-variant killing-blow flavor-text pool, and a bash-specific evasion form -- all added to docs/DSL_CommandRef.md. Real gap found: warrior/thief skills (kick/bash/trip/rescue/circle) have zero first-person confirmed text in this corpus, recommend a live test instead of more log-grepping. Nothing built yet, prep only.
- 2026-07-07: fixed MyDSL_CharacterAssist.lua disarm2/disarm3 triggers to make the comma before "and sends it flying" optional, after the skill/spell catalog pass found a confirmed real third-person analog with no comma (PNP's own source pattern had one, likely a typo) -- confirmed disarm1 is an exact match against real DSL2-era text, upgraded from unconfirmed. Verified pattern change against both comma/no-comma forms.
- 2026-07-07: pruned docs/TODO.md from 1325 lines back down to a real punch list (260 lines) -- consolidated ~20 scattered "fixed, needs live confirm" essays into one checklist, cut resolved-item narrative that CHANGELOG.md/git log already record permanently, kept only open/actionable items and genuinely load-bearing reference facts (itemstat sequencing risk, native inventory scale). No content was lost -- full detail for anything summarized here remains in git history.
- 2026-07-07: mydsl test smoke test caught a real gap on its first live run -- 4 modules (MyDSL_ChatTriggers.lua, MyDSL_CharacterAssist.lua, MyDSL_Roller.lua, MyDSL_RawCapture.lua) exist, are correct, and are committed, but were never added as dofile() Script entries in Mudlet's Script editor, so they've never actually run in the live profile -- confirmed via current/autosave.xml, every other module has its own Script entry, these 4 don't. This means several "needs live confirmation" TODO items (chat routing, CharacterAssist rearm/standup/spellup/vision, Roller) could not have been properly tested regardless of what was tried. Documented as a new BLOCKING section in docs/TODO.md with the exact 4 dofile lines needed. Also confirmed the "UI collapsed on Kien login" report is an expected one-time side effect of tonight's character-binding fix (no MyDSL_layout_Kien.lua existed yet, so reflowAll() snapped to LayoutEngine defaults) -- resolved by running mydsl layout save once.
- 2026-07-07: Phase 3 answers from Steven -- GroupView confirmed working live (resolved as a side effect of session fixes, not isolated to one). Killed the 4 dead placeholder windows (Inventory/Equipment/AsciiMap/Banner) from WindowRegistry/LayoutEngine per "kill until we need" -- Phase E's equipment capture logic itself is untouched. Gathered full context on the 7 unwired DataLayer capture pipelines (which tie to killed windows, which are redundant with working GMCP, which have no consumer at all) for Steven's decision, not yet deleted.
- 2026-07-07: Phase 3 item 2 resolved -- Steven agreed to delete whok/whoc/unread/inv/map/affectsText (6 unwired DataLayer pipelines, no consumer), and to keep+build improve since MyDSL_LiveView.lua already had a complete Improve bar waiting on real data. Added MyDSL.parseImproveStatusLine() for the real "currently improving X (N%). (M online minutes to improvement)" status line (confirmed pattern, added to DSL_CommandRef.md), wired both improve triggers for the first time, mapped MyDSL.State.improve -> MyDSL.DB.improve in DataBridge (pre-formatted text field), fixed a real event-name case mismatch in LiveView (listened for "MyDSL.Improve.Updated", DataLayer actually raises "MyDSL.improve.updated"). Verified end-to-end via emulation: real text -> parse -> event -> DataBridge sync -> LiveView render, bar text confirmed as "Imp sneak 91% (12m)". Needs live confirm.
- 2026-07-08: layout collapse recurred on Kien login -- confirmed same root cause as 2026-07-07 (mydsl layout save was never actually typed either session, checked both logs), not a new bug. Built proper auto-save per Steven's request: MyDSL.suppressLayoutAutoSave() time-window guard + ~2s debounce on sysWindowResizeEvent, specifically designed against the failure mode a prior Claude.ai-era attempt hit (sysWindowResizeEvent fires on every dock/undock, not just user drags). mydsl layout autosave on|off toggle added. Verified via test/mudlet_mock.lua with real timer/event dispatch across 4 scenarios (boot suppression, post-suppression schedule, debounce collapse, toggle). Needs live confirm it does not reintroduce old docking symptoms.
- 2026-07-08: confirmed the "clan gossips... never made it to chat window" report is the same still-unfixed BLOCKING dofile issue from 2026-07-07, not a new chat-routing bug -- MyDSL_ChatTriggers.lua still has no dofile() entry (re-checked current/autosave.xml), so nothing routes any chat to EMCO tabs right now. No code change; documented the concrete symptom in docs/TODO.md as evidence, pointed back at the existing fix.
- 2026-07-08: reverted layout character-binding and auto-save per Steven's direct request ("revert the layout save and per character layout save... use lua saveWindowLayout() cause its not woking right and i dont want to have that fight again"). MyDSL_LayoutEngine.lua's SAVE_FILE back to fixed MyDSL_layout.lua (not per-character); removed the character.identified -> Layout.load()+reflowAll() hook entirely; MyDSL_WindowRegistry.lua's saveLayout() simplified to just call native saveWindowLayout()/saveProfile(), dropping the custom percentage-capture-and-file-write steps. Deleted the orphaned MyDSL_layout_Kien.lua the reverted auto-save had created. Window visibility state (MyDSL_windowstate_<Char>.lua) stays character-bound -- only layout was reverted. Verified via test/mudlet_mock.lua that saveLayout() now only calls native functions and character.identified no longer touches layout.
- 2026-07-08: confirmed 3 of the 4 BLOCKING dofile entries were added by Steven (ChatTriggers, CharacterAssist, Roller) -- re-checked current/autosave.xml. Only MyDSL_RawCapture.lua (a low-priority diagnostic tool) still has no entry; nothing critical remains blocked.
- 2026-07-08: layout revert confirmed live by Steven ("layout seems to be functioning as it should now") -- closed. Also: Steven moved MyDSL_RawCapture.lua out of MyDSL_Full to the top of the script tree (first to run, ahead of any other script/trigger) -- correct per the tool's own design intent, but not yet reflected in current/autosave.xml (likely needs a profile save); flagged to re-check next session.
- 2026-07-08: confirmed via screenshot (Screenshot_20260708_155236.png) that MyDSL_RawCapture.lua is now the first item in the whole native script tree, checked/enabled, correct dofile line -- exactly right placement. Still not in current/autosave.xml on disk though (Mudlet's live in-memory Script Editor state, not yet saved) -- flagged that a restart before clicking "Save Profile" would likely lose the change.
- 2026-07-08: MyDSL_RawCapture.lua confirmed fully saved to disk and correctly first in the script tree (dofile count 24, up from 23) -- but only after checking the actual newest timestamped file in current/ (2026-07-08#15-55-17.xml), since current/autosave.xml itself was stale (still 0 hits even after Save Profile + close). Updated CLAUDE.md's Key Paths section with the corrected gotcha: check the most-recently-modified current/*.xml file, not the fixed autosave.xml filename, when verifying native Script/Trigger/Alias changes -- confirmed as a real, repeatable trap (this is the second time in two days autosave.xml specifically was stale relative to the true latest saved state).
- 2026-07-08: reopened the chat-capture "S" bug with much stronger evidence -- checked log/MyDSL_EMCO_Chat/2026/07/08/OOC.html directly, every single routed chat line (16+ consecutive) has a standalone "S" line right after it, 100% reproducible. Circumstantial link to frequent "System Message: gmcp event... display(gmcp)..." debug lines visible in the main console (source not found in any of our scripts or the native trigger tree -- likely a Mudlet Settings-level GMCP debug toggle). Recommended two next steps: check Settings for that toggle, and use mydsl rawlog on (RawCapture is finally active) to see the true raw text. Not fixed -- documented in docs/TODO.md rather than guessing further.
- 2026-07-08: fixed a real CharacterAssist gap -- standup only ever matched "knocking you senseless", but Steven hit a completely different real knockdown message ("<actor> bumps into you causing you to lose your balance." / "You sit down.", confirmed via log/2026-07-08#15-57-13.html) that was never covered, so auto-standup silently never fired. New trigger added on the bump line specifically, verified against the real captured text via test/mudlet_mock.lua.
- 2026-07-08: checked PNP standup source for a solution to the bump-knockdown gap -- PNP only has the same "knocking you senseless" trigger we already had, no additional coverage; confirms the fix already made is genuinely new ground, not a missed port. Refined the chat-capture "S" bug diagnosis per Steven's live observation (GMCP echoes tied to the Errors window, artifact persists even when it's closed): traced the leak to EMCO's append()/xEcho() itself, which uses selectCurrentLine()+copy() (a live widget-selection operation) rather than a plain string capture -- the regex pattern match is not the problem, tightening it would not help. Proposed fix (not yet applied, pending Steven's live experiment result): capture the line via our own immediate getCurrentLine() and use decho() instead of append(), trading per-line original color for one consistent per-channel color. Logged "consolidate all native Mudlet objects into one package" as a new DEFERRED design item per Steven.
- 2026-07-08: chat capture "S" bug confirmed fixed, per Steven's live experiment -- root cause was a second, duplicate set of chat-capture triggers running alongside MyDSL_ChatTriggers.lua, not an EMCO/Mudlet-core timing race as initially suspected. Disabling the duplicate set fully resolved it. This is now the leading new theory for the long-unsolved "clan gossip duplicates" report too, added as a lead there. Also restored deleteLine() in MyDSL_ChatTriggers.lua's route() (deliberately left out for testing per a code comment, "add back once routing confirmed") -- chat lines were staying duplicated in the main console after routing to EMCO tabs; not an EMCO gag setting, just this file's own unfinished state. CharacterAssist standup fix confirmed live by Steven, closed.
- 2026-07-08: clan gossip duplicates confirmed fixed by Steven -- same root cause as the chat "S" bug (the duplicate chat-capture trigger set), not a separate unsolved issue. Closed.
- 2026-07-08: reopened chat "S" bug + clan gossip duplicates -- both had been closed same-day on Steven's live impression, but directly checking log/MyDSL_EMCO_Chat/2026/07/08/All.html (while investigating the RightHere-on-look bug) shows 54 standalone "S" lines and 25 consecutive duplicate lines across 153 total segments in that one file, both persisting right up to the most recent entries. Not reverting the deleteLine() restoration (still correct/needed regardless) but flagging that whatever was tested at the time didn't actually confirm the fix -- needs a fresh live check.
- 2026-07-08: fixed a major RightHere-on-look gap -- corpus frequency check found "stands here" (2740 occurrences) is more common than "is here" (1928) for describing stationary NPCs, meaning most mobs in most rooms were never captured since this feature was built. Also fixed item-line and mid-listing-blank-line handling (both were incorrectly ending capture early) and multi-tag paren stripping. Verified via emulation against a real 7-entity room: 0 captured before, 7 after.
- 2026-07-08: reverted the decho()-based chat routing change per Steven -- misread his request. He wants the known extra-line artifact detected/suppressed directly (e.g. via EMCO's own gag-list mechanism), not a rewrite of the capture mechanism, and does not want any chat changes right now until he's tested the current reverted+duplicate-triggers-deleted state. route() is back to append()+deleteLine(). Sibling-profile log scan came back a dead end -- 95-99% debug noise, the 3 PNP profiles are identical mirror copies of each other, no new room-presence verbs found; recommend not repeating this.
- 2026-07-08: split the EMCO chat "S"/duplicate bug and the "follower extra line" bug back into two separate TODO entries -- per Steven, these are unrelated and I had wrongly combined them. No real data captured yet for the follower-line issue; waiting on a concrete example before investigating.
- 2026-07-08: resolved the "follower extra line" confusion -- Steven confirmed it's the same area as the RightHere-on-look mid-listing blank line I already found and fixed, quoting my own diagnosis line back as the thing he meant. Merged the TODO entry back into the RightHere-on-look fix instead of leaving it as a second open item.
- 2026-07-08: found round 1 of the RightHere-on-look fix wasn't actually sufficient -- notes_utf8.txt flagged "look command does not update the righthere window with mobs" and "blank lines are still present after the sommoned/followers bear wolf elementals" as still happening post-fix. Checked a fresh log (2026-07-08#17-15-32.html, captured after round 1 was loaded) and found the REAL second bug: charmed/summoned pets' idle-action lines use wildly varying verbs DSL-side ("sloshes around here.", "prances about here.", "paces back and forth here.", "looms here.", "burns hotly in the room.", "follows their client." with no here/room anchor at all -- confirmed via corpus grep over every "(Charmed)" line in the logs), none of which matched parseLookHereLine's verb set, so the first charmed pet listed in a room silently ended capture and dropped every real entity after it. This -- not the blank line -- is what "the space after certain followers" actually meant. Fixed: broadened parseLookHereLine's name match with an article-anchored generic "here"/"in the room" fallback, reordered the fixture check ahead of it to avoid item lines ("X lies here.") being miscaptured as mobs, and added isCharmedStatusLine() as a safety net so any "(Charmed)"-tagged line is treated as skip-don't-end regardless of verb phrasing, covering future never-seen verbs too. Verified via emulation against the real captured sequences from both rounds -- all entities now captured, capture never terminates early.
- 2026-07-08: found the ACTUAL root cause of RightHere-on-look, round 3 -- Steven reported it was still not updating live even after rounds 1 and 2. Both those fixes were real but moot: the trigger that calls beginLook(), anchored on "^\[Exits: .*\]\s*$", has never matched a single real line, ever -- corpus-checked all 172 distinct "Exits:" lines across the whole log history and every single one has a leading space before the bracket that the anchor's start-of-line "^\[" rejects. My emulation testing in rounds 1/2 called MyDSL.beginLook() directly, which bypassed this trigger entirely and could never have caught it. Fixed the anchor to tolerate leading whitespace; hardened the same bug class in scanAround's "Looking around you see:" anchor too (9 of 412 real occurrences have a leading double-space). Also ran a broader 718-line corpus sweep of the round-2 fallback and confirmed it correctly (and necessarily) catches static room-landmark lines too ("A large beautiful fountain flows here.", "A member of the Minotaur Guard stands watch here.") -- these sit directly in the real presence-listing, same bug class as charmed pets, just far more common. Found and fixed one new false positive from the broadened fallback: "You see a long and sharp Protection Sword here." was being miscaptured as a mob; added "You see ... here" (9 real occurrences) to isLookFixtureLine(). Verified the full anchor-to-body chain end to end via emulation this time, exercising the real installed trigger IDs rather than calling internal functions directly -- closes the exact testing gap that let this hide through two prior "fixed and verified" rounds. Documented one known remaining lower-priority gap: a handful of proper-named NPCs use non-standard verb phrasing with no article to anchor on ("Sorbus the Hermit is sitting here...").
- 2026-07-08: Steven reported RightHere still not updating even after the round-3 anchor fix, with a fresh log showing more never-before-seen charmed-follower phrasing ("A wild wolverine is foaming at the mouth.", "A wild cougar is here growling at you."). Re-checked the exact anchor+body chain against this new log via emulation -- correctly handled, capture never terminates early. Also ruled out: dofile path divergence (native XML's dofile path resolves to the identical file via diff/inode check), and window visibility (Kien's persisted windowstate has MyDSL_RightHere = true). No further bug found from static analysis alone. Added `mydsl righthere dump` to MyDSL_ScanView.lua -- a diagnostic alias that echoes the raw scan.rightHere table straight to the main console, bypassing the window, to distinguish a capture problem from a display problem. Waiting on Steven to run it live and report back.
- 2026-07-08: `mydsl righthere dump` came back 0 entries/capture inactive on every real look. Fixed a bug in the dump alias itself first (used cecho() with decho's RGB-triple color syntax, printed the literal tag instead of rendering). Added a temporary debug trace directly to the trigger chain (anchor fire, beginLook() entry, capture success, endLook() entry) since static analysis had exhausted itself; Steven ran one more real test with it in place. Found the ACTUAL root cause immediately: endLook() was firing on the exact same "[Exits: ...]" line that had just fired lookExits and installed the capture body catch-all, every single time. Root cause: lookBody's ".*" catch-all gets installed by lookExits's callback while that same line is still being processed, and Mudlet evaluates newly-registered triggers against the current line in the same pass -- so the brand-new lookBody immediately re-fires on the very Exits line that created it, which isn't recognized as fixture/mob/charm/continuation, so it falls through to "unrelated event, end capture" and closes immediately, before any real content line is ever seen. This is why rounds 1-3 were all individually correct but the feature never actually worked, ever, in real gameplay. Fixed with one line: lookBody now explicitly treats a line matching the Exits-line shape itself as a keep-capturing no-op. Verified via emulation reproducing the exact self-retrigger scenario -- capture survives, all real entities get captured. Removed the temporary debug traces.
- 2026-07-08: consolidated the RightHere-on-look TODO.md entry -- pruned rounds 1-3's now-superseded writeup (round 3's anchor fix was individually correct but wasn't the real bug) down to a pointer, keeping only the round-4 actual-root-cause entry current. Full history stays in git log/this file, per the project's "prune TODO, CHANGELOG keeps the permanent record" rule.
- 2026-07-08: closed 8 confirmed-live checklist items per Steven's notes_utf8.txt updates: TargetView Friend/Enemy tag, mydsl who passthrough, full-Mudlet-restart crash fix, ChatTriggers basic routing (separate from the still-open "S"/duplicate cosmetic bug), mydsl help/history font, TargetView charmed-pet button guard, kill-vs-murder verb fix, GroupView name truncation, and the LiveView Improve bar populating. Clarified one non-bug: Steven noted the Improve bar's displayed time "does not count down" -- confirmed this is the documented deliberate design (shows the last snapshot rather than a live-ticking countdown, per "stale data beats spam"), not a defect.
- 2026-07-08: diagnosed a new bug from Steven's notes -- "the quiet tag/indicator when def, ungags the prompt line." Confirmed via corpus grep: quiet mode prepends a literal "[Quiet] " tag to the vitals prompt ("[Quiet] [1605/1605HP | ...] [...]"), which breaks the native prompt-gag trigger's start-anchored pattern (requires "[" immediately followed by digits). Found the real live trigger in current/*.xml -- named "Gag promt line1" (real typo), pattern `^\[\d+/\d+HP \| \d+/\d+M \| \d+/\d+MV \] \[ .* \| .* \| .* \| .* \]$`. This is a native Mudlet trigger (MyDSL_PromptView.lua's own header documents it as GUI-created, not script-managed), so the fix has to be applied by Steven in the Trigger Editor, not by editing a .lua file: add an optional `(?:\[Quiet\] )?` at the front. Verified the corrected pattern matches both quiet and non-quiet real prompt text. Also flagged in passing: the live trigger's script is a bare deleteLine(), not gated by MyDSL.Prompt.enabled the way MyDSL_PromptView.lua's own header comment describes -- a doc/reality drift, not fixed, just noted.
- 2026-07-08: quiet-mode prompt gag fix confirmed live by Steven ("doesnt gag prompt now after new pattern added") -- closing. Also closed the GroupView/TargetView fragmented-row log fix, confirmed live via the same notes update.
- 2026-07-08: 5 screenshots of a busy multi-room gnome corridor confirmed RightHere-on-look's round-4 self-retrigger fix works -- capture actually runs and populates now, a first for this feature. But the screenshots also surfaced two more real bugs. (1) parseLookHereLine() unconditionally overwrote scan.rightHere[key] with a fresh count=1 table on every match, so a room with 2 identical gnomes always showed count 1 instead of 2 -- fixed to match parseScanLine's existing increment-if-exists pattern for the same table. (2) One screenshot showed RightHere completely empty in a room with 4 real gnomes -- traced to "(Glowing) (Humming) A Parrying dagger floats above the ground.", a never-seen item verb phrase with no "here"/"in the room" anchor at all that fell through every check straight to "unrelated event, end capture"; added "floats above the ground" to isLookFixtureLine(). Both verified via emulation against the exact real room sequences from the screenshots. Also confirmed via the newest log that `eq` produces real, parseable equipment text (worn-location + item name pairs) -- not implementing the equipment-capture feature itself, just confirming the format for when that TODO item gets picked up.
- 2026-07-09: reopened the quiet-mode prompt gag bug -- misread Steven's 2026-07-08 reply "doesnt gag prompt now after new pattern added" as confirmation of success; on reread, plus a new note ("The gag1 trigger is broken") and a screenshot showing the raw vitals prompt flooding the main console unfiltered, it was clearly still broken. Read the live trigger XML directly and found the real cause: the corrected pattern I gave Steven got appended after the old one instead of replacing it, producing one garbled two-anchor regex that can't match anything. Gave Steven precise replace-not-append instructions. Also confirmed via corpus check that only "[Quiet]" ever appears as a prompt tag (no separate "[Deaf]" variant), so the one pattern covers it -- the "quiet is temporary when deaf" note appears to describe a real DSL mechanic (a temporary deafen status effect from a trap also triggers the "[Quiet]" prompt tag) rather than a second tag format to handle.
- 2026-07-09: fixed a real, systemic link-rendering bug -- Rescue/cure-spell buttons (and every other clickable link in TargetView/GroupView) were rendering as Mudlet's default blue/underlined hyperlink instead of their configured colors, confirmed via screenshot. Root cause: dechoLink()'s useCurrentFormat parameter (misnamed "underline" by the local helper functions) was being passed false at all 5 call sites in MyDSL_TargetView.lua/MyDSL_GroupView.lua, telling Mudlet to ignore the embedded decho color codes. Only MyDSL_ScanView.lua's RightHere links passed true and rendered correctly -- that's why only RightHere ever looked right. Fixed all 5 call sites to pass true.
- 2026-07-09: generalized RightHere-on-look's presence-line safety net a third time (round 6). isCharmedStatusLine() only recognized a literal "(Charmed)" tag as proof of a skippable entity, but a screenshot showed two ordinary NPC lines with no tag and no "here" anchor ("A dark elven commoner stands around looking bored.", "The dark elven scout slips in and out from the shadows unheard.") emptied out RightHere for that room too -- third confirmed live instance of the same underlying problem. Generalized to isUnparsedPresenceLine(): every confirmed example has started with an article once tags are stripped, so treat that shape alone as sufficient, subsuming the old Charmed-only check. Verified via emulation against the real screenshot text.
- 2026-07-09: closed considerEasyKill/considerNoMatch text checklist item -- confirmed by Steven as a non-issue ("not sure why this is being tested, but the server sends the echo fine").
- 2026-07-09: confirmed via screenshot (Screenshot_20260709_172053.png) that the link-color fix works -- MyDSL_Target's action buttons now show varied colors instead of uniform blue/underline. Closed the action-button-contrast TODO item.
- 2026-07-09: confirmed mydsl log <category> on works end to end -- Steven ran `mydsl log righthere on` then look/scan a few times; verified MyDSL/logs/righthere/Qinrathaz/2026-07-09.log was written with matching content (including count badges). Closed the logging-defaults-rework TODO item.
- 2026-07-09: Steven asked me to verify notes_utf8.txt actually saved his last commit's worth of edits, worried it hadn't updated while he was logged in -- diffed the last committed version (4ad8bc3, committed 17:17:15) against the working file and found only his one new line added, nothing lost. His entire most-recent play session (the new screenshot, the mydsl log test) started in a fresh log at 17:18:57, after that commit, so the checklist he saw was current. Reassured, no actual bug -- notes_utf8.txt is a plain file on disk, unaffected by whether Mudlet is connected to the game.
- 2026-07-09: Steven applied the replace-not-append fix for the quiet-mode prompt gag trigger -- confirmed correct via the live trigger XML, a clean single pattern with no more duplication. Checked the next real-gameplay log for direct proof but it turned out to be a full character-creation session (a brand-new goblin/hobgoblin character named Uldek), never reaching an existing character's prompt, so it doesn't actually confirm the fix -- it may just never have been exercised. Did find a real, separate gap from that session though: newbie/freshly-rolled characters use a completely different short prompt format, "[Quiet] <20hp 100m 100mv>" (angle brackets, no max values), not covered by the current trigger pattern at all -- confirmed leaking unfiltered 4 times. Documented a suggested alternation pattern to cover both formats; not applied yet, Steven's call. Still need a log from an established character to actually confirm the original fix.
- 2026-07-09: Steven asked how to change the roller's goal from 241, then reported "roller is not pausing to let me input." Traced the character-creation log in detail and found MyDSL_Roller.lua (ported 2026-07-07) and a native trigger literally named "roller" were both reacting to every stat-roll line independently, each with a separate goal variable that could drift out of sync -- confirmed via a literal artifact (the native trigger's old-style `</cyan>` closing tag doesn't parse in Mudlet and leaks out as visible text) showing duplicated output on the first ~14 rolls. If the two goals disagree, one trigger can decide "pause" while the other, on a stale goal, decides "reject" and sends n anyway, silently overriding the pause. Turned out Steven had already independently noticed something was off and manually deleted the native "roller" trigger mid-session (confirmed: its fingerprint vanishes in the log at exactly that point) -- the actual "not pausing" moment he'd flagged was him manually using ~ to stop the roller himself to keep a good roll, not a real bug. From the deletion point on, MyDSL_Roller.lua alone handled every roll correctly. Closed the Roller TODO item -- confirmed working, `set goal <n>` is the one command that matters now.
- 2026-07-09: added MyDSL_PromptSetup.lua per Steven's explicit request -- wants his chosen `prompt <format>` string applied automatically to brand-new characters instead of typing it by hand each time. Checked PNP's approach first as directed: PNP's DSL_PNP_Statusbar.lua `set_prompt` turned out to be a local Mudlet-side display-overlay setting, not DSL's actual server-side `prompt` command, so not directly reusable. CLAUDE.md's "never send an automatic game command, must be user-initiated (alias or click)" rule directly conflicts with a naive "fire the moment character creation finishes" design, so asked Steven to pick a trigger mechanism -- he chose detect + clickable link over a fully-automatic send. Anchor choice: rejected the obvious "WELCOME TO DARK & SHATTERED LANDS (DSL)" banner after checking PNP's own DSL_PNP_Character.lua, which names that trigger "Login Trigger" and confirms it fires on every login, not just character creation. Used "The Gray Mist of Nothingness" (the character-birth cutscene room) instead -- corpus-checked as appearing in the one fresh-character-creation log and zero established-character logs. New module needs its dofile() Script entry added by Steven before it can run (same pattern as ChatTriggers/CharacterAssist/Roller earlier), then a real character-creation test.
- 2026-07-09: put MyDSL_PromptSetup.lua down for a later addition per Steven -- moved its two TODO.md entries (dofile wiring + live test) into DEFERRED as one consolidated item. Code stays committed and untouched, just not wired in yet.
- 2026-07-09: started the combat TOP PRIORITY pass. Fixed a real, confirmed bug: self-condition never registered because DSL phrases your own condition in second person ("You have some small wounds"/"You are in excellent condition"), a different verb conjugation from the third-person form our trigger and PNP's both only matched. Confirmed via corpus for 3 of 7 condition-ladder rungs; the rest fixed via the same validated grammatical transformation. Added self-phrased alternatives to both CONDITION_PATTERNS and the combatCondition trigger's own regex (which wouldn't have fired on these lines at all before). Verified via emulation: all 7 rungs now register correctly. Side finding: some self-condition corpus lines had a garbled "nasty's...And...(18.5)" suffix that turned out to be our own severity-score decorator leaking a formatting bug into the log, not raw DSL text -- flagged, not investigated further this pass.
- 2026-07-09: verified two more TOP PRIORITY combat items against real corpus text -- both death-message forms (`is DEAD!!` / `hits the ground ... DEAD.`) and all 4 evasion trigger patterns (dodge/parry/block/sense, both grammar directions including you-as-evader with the embedded possessive apostrophe). No bugs found in either -- both closed as code-verified, just needing a real live fight for final end-to-end confirmation. Checked "quoted weapon names" too but found zero real occurrences of a quoted weapon name in any actual combat line in the whole corpus -- the damage trigger's capture groups genuinely don't include `"`, a plausible latent bug, but left unfixed since there's no real text to verify a fix against.
- 2026-07-09: Steven pointed at log/Archive.zip and the sibling Mudlet profiles' logs for more combat data. Archive.zip had 25 entries not present as loose files (all .txt, 06-30/07-01) plus errors.txt -- errors.txt turned out to be pure map-loading audit reports, no real Lua errors; the new .txt logs added no new evidence for the still-unconfirmed procs. Also found "DSL2 Current state.tar.gz" (untracked, dated June 6) containing a MyDSL/data/ directory with db_creatures.lua/db_items.lua/db_people.lua/db_rooms.lua etc -- looks like an earlier or parallel attempt at the not-yet-started Layer 4 reference library, but its MyDSL/logs/ directories were empty and db_patterns.lua/db_logs.lua were empty stubs -- a dead end for this specific search, though worth remembering the tarball exists next time Layer 4 gets picked up.
- 2026-07-09: sibling-profile logs (the 3 PNP variants, DSL1, Qinrathaz-Vaelis) turned out NOT to be the dead end the 2026-07-08 scan concluded -- that scan was specifically about room-presence verbs in one giant debug-heavy file; a targeted grep for combat proc phrases across all of them found real, extensive combat text. 11 of 15 "unconfirmed" procs confirmed with real occurrences: procFrostFreeze, procFrostTouch, procFlameBurn, procFlameSear, procShockLightning, procShockShocked, procVampDraw, procVampDrain, procStun, procManaDraw, procHolyWrath, procHolyFlash, procPoisonSetup, procPoisonOnset, procPoisonTick. Still zero evidence anywhere for Sharp, procUnholy, procManaSelf, and combatSense1/2.
- 2026-07-09: found a real, high-impact discovery via that same sibling-log data -- every single combat line fought in the Coliseum is broadcast with a leading location prefix ("[ The Center of the Coliseum ] a wild bear's wrath misses Rylae."), confirmed across dozens of real lines for both regular damage and several procs. Since nearly every combat trigger was `^`-anchored directly to the attacker name, this meant regular combat tracking has never picked up any Coliseum fight. Initially "fixed" by adding an optional non-capturing bracket-prefix to every affected trigger regex plus a stripLocationPrefix() helper -- then REVERTED same-day per Steven: Coliseum/AGL combat is explicitly out of scope for this tracker, it's planned as a separate future module (a large window with 4 floating cardinal sub-windows matching the wall echoes). Reverted all the trigger-regex and helper changes back to the original strict anchoring, added a comment on combatDamage explaining why it's deliberately excluded, and logged a new DEFERRED item for the future AGL module noting the groundwork (confirmed bracket format, confirmed which procs apply) is directly reusable there, just inverted. Also confirmed via Steven that the Sharp weapon flag never echoes anything (pure damage bonus) -- closed as "working as intended," not a gap. Caught and fixed a real self-inflicted mistake made mid-investigation: an edit briefly deleted the `local DAMAGE_VERBS = "..."` declaration entirely, which would have made the whole file fail to load -- caught immediately by the routine post-edit syntax check, never shipped. Verified the revert via emulation: Coliseum lines are correctly excluded again, normal combat tracking is unaffected.
- 2026-07-10: checked combatSense1/2 (sense-based evasion) next. Found DSL_Helpfiles/danger sense.txt -- the underlying ability is real ("sense what your opponent is going to do... dodge or deflect their blow", bard/bard-reclass only), not invented, narrowing the open question to just the exact echo wording. Searched every available log source on this machine -- DSL2's own corpus, log/Archive.zip, all 3 PNP sibling profiles, DSL1, Qinrathaz-Vaelis, and every remaining stub profile -- zero hits anywhere for the trigger's expected wording or any close variant. Helpfiles document usage, not raw output, so this doesn't confirm the exact text either way. Likely explanation: bard-only skill, no bard has used it during any logged session across this whole machine. Found and ruled out a look-alike red herring while searching ("[51] Herbert deflects [51] Archal's charge with a shield." -- a different, real mechanic, not Danger Sense). Left open -- would need an actual bard playing/logging to ever confirm.
- 2026-07-10: Steven shared a Google Sheets "Leveraged Rankings" link and 6 DSL forum AGL match threads, asking if they'd help find the still-missing combat patterns. The sheet turned out to be just a win/loss scoreboard with informal victory descriptions ("ROCKED", "toasted", "DESTROYED"), no raw game text. The forum threads were members-only ("Access Denied") -- Steven provided a browser cookie export (~/Downloads/cookies.txt) to log in with; fetched all 6 with it successfully. Got ~7,000 lines of real AGL tournament combat text across 6 full fights. Searched all of it for the remaining unconfirmed items: still zero hits for procUnholy, procManaSelf, and combatSense1/2's wording. Found two near-misses that turned out not to be matches ("Paklop's energy drain hits Lohla" -- a regular damage line, not procManaSelf; "Zecnys stops using unholy robes" -- an armor name, not procUnholy). Did confirm quoted item names are a real DSL convention (a potion "inscribed \"Stoning\"", seen repeatedly) but still zero occurrences of a quoted *weapon* name in any damage line, now against a much larger sample -- lowered priority on that one accordingly. Saved all 6 fight transcripts as clean text into log/AGL/ (gitignored like the rest of log/, same as every other session log) since they're real reference material for the future AGL/Coliseum module even though they didn't answer the immediate question. Documented the new location in CLAUDE.md's reference-material section.
- 2026-07-10: Steven asked to check the AGL/combat forums for more logs. Found the forum's AGL board (FID=128, "AGL/AWG/CWARS/ACFL") goes back 88 pages -- fetched an additional 18 fight transcripts spanning 05/24/26 through 06/30/26 (24 total now saved in log/AGL/, ~9,000 lines combined). Searched everything for the same still-open items -- zero hits anywhere for procUnholy, procManaSelf, combatSense1/2's wording, or a quoted weapon name in a damage line, across the full 24-fight sample. This is a much stronger negative result than before but still not proof of absence -- 88 pages of history exist and this was a representative sample, not exhaustive. Updated TODO.md's counts/framing for all 3 affected entries.
- 2026-07-10: found and fixed RightHere-on-look round 7, a real bug hiding in Steven's note "the righthere window is recording my ch[a]r[a]cter on olyndros" -- turned out to describe a screenshot showing RightHere completely empty in "The Hill-lands" despite a charmed mount and 2 hill dwarves clearly present. Root cause: the room's static landmark lines are sometimes indented ("     A twisted and gnarled pine tree grows crookedly here."), and both parseLookHereLine() and isUnparsedPresenceLine() ran their ^-anchored checks against the raw, untrimmed line, so the leading whitespace meant the checks never matched and capture ended on the very first indented line. isLookFixtureLine() never had this problem since its checks are unanchored substrings. Fixed by trimming both functions' input before pattern-matching. Verified via emulation against the exact real sequence -- all 4 entities now captured correctly.
- 2026-07-11: matched damage-flag colors to PNP exactly, per Steven's note ("change the colors of the damage flags to the same as PNP for community recognition"). Read PNP's real dam_info/flag_info tables (DSL_PNP_Battle.lua) and color_table RGB values (DSL_PNP_Support.lua) and replaced every guessed color in DAM_INFO/FLAG_COLOR (MyDSL_DataLayer.lua) with PNP's exact values. Also discovered and added something we were missing entirely: PNP's GHASTLY-through-UNSPEAKABLE severity tier bakes a letter-by-letter alternating red/white color effect directly into the decorated verb text, which our old implementation rendered as one flat color instead. Added a new alternateLetters() helper that builds this at load time and an info.decorated field consumed by both calcDamVerb() (round-summary) and parseCombatDamageLine() (per-swing feed). Verified via the emulation harness: UNSPEAKABLE renders the correct alternating-color letter sequence, ordinary tiers render PNP's exact green/yellow/red. Needs a live fight to visually confirm.
- 2026-07-11: found and fixed a real GroupView bug from Steven's note "follower not showing in group for vaelis (untrained guardhand)". Checked the actual log (log/2026-07-11#07-41-33.html) and found the real group block: "Vaelis's group:" / "[ 1 Mob] An untrained guardhand ..." / "[ 1 Mag] Vaelis ...". DSL right-justifies the level in a fixed-width field, so level-1 members get a leading space before the number that higher levels (e.g. "[51 War] Olyndros ...") don't -- parseGroupLine()'s pattern required a digit immediately after "[", so it silently failed on any line with that leading space, which meant the WHOLE group vanished (Vaelis herself included, also level 1), not just the follower -- Steven just happened to notice the follower's absence first. Fixed by adding %s* after the opening bracket. Verified via emulation against the exact real 3-line block (guardhand/Vaelis/a no-space control line) -- all 3 now parse correctly.
- 2026-07-11: consolidated timers per Steven's "can we pull all timers off one tick" question, and fixed "improve timer not counting down." Audited every tempTimer() in the profile -- only 2 self-perpetuating loops existed: TickSource's own 0.25s T.loop() (left alone, TickView needs it for smooth bar animation) and MoonWeather's separate tempTimer(1, loop) clock chain. Added a new MyDSL.Timers.Slow event (throttled to 1/sec inside T.updateTimer()) and moved AffectsView's redraw, LiveView's render(), and MoonWeather's clock onto it -- all three were either redrawing 4x/sec for a countdown that only ever shows whole seconds, or (MoonWeather) running an entirely separate timer to get the same once/sec cadence this already produces internally. Deleted MoonWeather's startClockTimer()/stopClockTimer() entirely. Built the actual Improve fix: new improveLiveText() in MyDSL_LiveView.lua recomputes "M online minutes to improvement" live from elapsed real seconds since last_updated (confirmed via DSL_Helpfiles/improve improvement.txt that this is wall-clock online time, not a DSL tick interval, so deliberately not using TickSource's tick-average conversion) -- now actually counts down instead of freezing at the last-read value. Found and fixed a real bonus bug in the same plumbing: MyDSL_DataBridge.lua's sync() was rebuilding MyDSL.DB.tick from MyDSL.State.tick (which never holds remaining/average/percent, only a time string) on every sync() call -- including ones fired by the same gmcp.tick event TickSource itself reacts to, clobbering TickSource's just-computed smoothed average back to a bare 40 default on every real tick. Fixed by having DataBridge alias the already-correct MyDSL.DB.tick instead of rebuilding it -- TickSource is the sole authority for that table. Verified via emulation: the 1/sec throttle (32 fast pulses -> 8 slow pulses over a simulated 8s span) and the Improve countdown math (mid-countdown + past-due-floors-at-zero cases) both confirmed correct.
- 2026-07-11: found and fixed the real root cause of the recurring EMCO chat "S"/duplicate-line bug, after Steven asked to look into it again and whether chat should be captured via script or "the append demonnic with deleteline." MyDSL_ChatTriggers.lua's route() patterns were completely unanchored (no leading ^) with a quote-crossing ".+" prefix, despite the file's own comments already documenting the real native pattern as anchored -- meaning a verb word appearing INSIDE a different message's quoted dialogue could independently match a second unrelated route() pattern (e.g. "Vaelis tells you 'watch what she says about it'" matched both Tells and Local). Confirmed via Python re against the full 328,643-line clean corpus: 15 real lines matched more than one pattern under the old code, each causing a double append()+deleteLine() fire -- the confirmed mechanism for both a stray-character artifact and an unrelated line getting eaten from the main console. Fixed by anchoring every pattern to line-start and switching every prefix from ".+" to "[^']+"/"[^']*" (can't cross a quote). Merged 3 same-tab pairs that were separately double-matching identical text (gossips/clan gossips, Kingdom:/OOC Kingdom:, dropped a redundant "ask" from the answers/newbie catch-all). Caught one regression before shipping via the same corpus check: the first anchored Kingdom pattern required no speaker name, but all 23 real corpus examples have one ("Sofie Kingdom: '...'") -- fixed to make the name prefix optional. Final result: 0 multi-matches (down from 15), 0 lost coverage. Also incidentally found a separate, unrelated cosmetic bug while checking logs -- literal "&lt;br&gt;" text visible in EMCO's own HTML chat logs, traced to a real bug in vendored EMCOChat/demontools.lua's decho2html() (inserts a literal "<br>" string that then gets HTML-escaped along with everything else) -- left alone since it's vendored, unmodified library code and log-file-only, not visible in the live UI. Answered Steven's architecture question: append+deleteLine already runs through real Mudlet triggers (tempRegexTrigger is the same mechanism the Trigger Editor GUI uses), so "script vs. trigger" wasn't the actual bug axis -- "one line matching N independent triggers at once" was. Did not move to EMCO's native gag-list (checked EMCO:matchesGag() -- Lua patterns only, no alternation, and suppress-only, can't route to a different tab). Also added the explicitly requested gag=false option to route(), applied to say/tells/shout/yell per Steven's ask, so those still route to their tab but also stay visible on the main console.
- 2026-07-11: verified 2 more TOP PRIORITY Combat items while Steven plays to gather logs. (1) Weapon-flag proc attribution via last_attacker/last_target/last_noun: traced parseCombatProcLine() against PNP's handle_flag() directly and found we actually fixed a real bug in PNP's own source along the way -- PNP's drowning/freeze guard compares its `flag` parameter to the literal string "freeze" AFTER already reassigning it to the single-letter code, so that comparison can never be true in PNP itself; our version compares the letter code directly and works correctly. Verified end-to-end via emulation: a damage line sets last_attacker/last_target/last_noun, a following proc line correctly attaches its flag to both the persistent active-fight entry and the round-summary entry. Also reproduced a known, inherent (not new) limitation: in a busy multi-combatant fight, an unrelated swing landing between the real proc cause and its echo will misattribute the flag to whoever swung most recently -- this is PNP's own exact technique (one shared last-event slot, not per-combatant), so it's faithful behavior, not a regression; flagged for Steven's call rather than building a per-noun-tracking improvement unprompted. (2) PNP-faithful display rewrite: confirmed already fully built (mydsl combat mode raw|condensed|gag exists, maps directly onto PNP's own gag_combat/summarize_damage flags) -- this TODO item was stale, just never checked off. Both items closed.
- 2026-07-11: built fully configurable action buttons for TargetView and GroupView, per Steven's answer to 3 open decision questions ("all the buttons should be user configurable... even non standard commands like scan, look, get item etc... manually be able to set all buttons, even the group buttons... there should be a clear button and the mob/player toggle"). Found most of the mechanism already existed (mydsl target mobset/playerset with 6 named slots, a live mob/player [M]/[P] toggle link, mydsl group quickset with 2 slots) -- the real gaps were arbitrary custom commands (buttons could only reference a name from the fixed TV.actions Lua table), no reset-to-default, and a real bug: GroupView's quickset had zero persistence, silently resetting to {"heal","rescue"} on every reload. Added MyDSL.TargetView.defineAction() + "mydsl target action <key> \"<label>\" <color> <command>" (supports %t for the target's name, or no placeholder for stateless commands like scan) -- custom actions are stored in TV.config.custom_actions and merged into the shared TV.actions table every reload, so GroupView's quickset picks them up automatically since it already reads from the same table, no duplicate mechanism needed. Added "mydsl target mobset/playerset reset" and "mydsl group quickset reset", restoring true hardcoded defaults from a freshly-literal TV.defaults/GV.defaults table (deliberately not derived from the live config, so reset can't just restore an already-customized value). Gave GroupView the same configFile()/loadConfig()/saveConfig() per-character persistence pattern TargetView already had, fixing the real quickset-doesn't-persist bug found along the way. Verified via emulation: stateless and %t-substituted custom actions produce the right command, reset restores true defaults without mutating the defaults table, GroupView's quickActions round-trip correctly through reset. This also closed out the original GroupView-heal-tier and TargetView-murder-alt-command TODO items as moot -- Steven can now just point any button at whatever command a character actually knows, no auto-detection machinery needed. Also resolved (no code needed): AffectsView's stealth-tracking mismatch -- confirmed it's per-character saved data, not a code default, and the matching "mydsl affects untrack stealth" alias already exists.
- 2026-07-11: found and corrected 2 stale TODO.md entries while looking for more independently-verifiable work. "Equipment capture -- not built yet" was wrong -- MyDSL.beginEquip()/parseEquipLine()/endEquip() (MyDSL_DataLayer.lua Section 9r) are fully implemented and wired to the real "You are using:" trigger, apparently built in an earlier pass and never checked off. Verified via emulation against the real confirmed corpus format: slot normalization (finger1/finger2, wrist1/wrist2, neck1), leading flag-parens stripped correctly from decorated items, "(nothing)" handled, and a stat-block parenthetical inside an item's own text correctly NOT mistaken for a leading flag. Still needs a live `eq` to confirm end-to-end, and MyDSL.State.equipment isn't wired to any display window yet. Also removed a "MyDSL_RawCapture.lua still needs its dofile() entry" line that directly contradicted the LOW PRIORITY section's own "fully confirmed saved to disk 2026-07-08" writeup a few lines above it -- never pruned after that was resolved. Separately confirmed CharacterAssist's rearm/spellup/blind-vision TODO entry is accurate as written (genuinely built, correctly still pending live test) -- no change needed there.
- 2026-07-11: per Steven ("lets pause the capture logs phase and the associated issues... we will return to them when we have more data to scan"), consolidated the 4 TODO items that all hit the same wall -- every available log source on this machine already searched (DSL2's own corpus, all 3 PNP sibling profiles, DSL1, Qinrathaz-Vaelis, log/Archive.zip, 24 AGL fight transcripts) with zero real occurrences of the needed text -- into one new "PAUSED -- needs more captured log data" section instead of leaving them scattered across TOP PRIORITY Combat. Moved (not duplicated) from their prior locations: quoted weapon names in damage lines, procUnholy/procManaSelf, combatSense1/2's exact wording, and AffectsView's A.ids.triggers.song format. No further searching on these until new logs/notes come in from actual gameplay.
- 2026-07-11: confirmed both the quiet-mode prompt gag fix and the EMCO chat "S"/duplicate-line fix live, per Steven's real gameplay logs. Quiet-mode: log/2026-07-11#09-23-09.html (an established character, Vaelis) shows Steven directly testing it and leaving himself a marker (`lua cecho("quiet was toggled and gagged properly")` right after toggling quiet on) -- matches the project's established cecho-log-marker convention. Independently confirmed via the same log: zero long-format or angle-bracket vitals lines leaked anywhere across the full session. EMCO chat: checked log/MyDSL_EMCO_Chat/2026/07/11/All.html -- all 11 standalone "S" artifacts cluster in the file's pre-fix portion (before ~08:31:17); the ~1h45m of continued play logged after the 09:19:54 fix landed shows zero "S" artifacts and zero real duplicate lines (one apparent repeat turned out to be a merchant NPC's stock response firing 4 times with 4 distinct real timestamps, correctly not a bug). Both items closed in TODO.md.
- 2026-07-11: found real combat activity in the same live session (Vaelis + a charmed guardhand vs. a blue jay) and used it to confirm the per-swing main-console damage display works end-to-end for real -- lines like "Your slash scratch Blue jay (2.5)" exactly match battleFormat()'s decorated output shape, and condition tracking ("A blue jay has some small wounds and bruises.") parsed correctly on repeat. Found and fixed a real bug along the way: `mydsl combat mode raw|condensed|gag` (MyDSL_CombatView.lua) never actually touched show_damage/show_miss, only gag_combat/gag_non_damage/summarize_damage -- but the damage-line show/hide decision in parseCombatDamageLine() is gated entirely by show_damage/show_damage_by_me/show_damage_to_me/show_miss (confirmed against PNP source too -- gag_combat never governs the damage line itself there either). So "raw" mode's own promise never actually worked -- it behaved identically to "condensed" for the one thing its name is about. (The raw swings seen in the log weren't from the mode alias -- no combat-mode command was ever typed that session; show_damage had apparently been left true in memory from an earlier untracked toggle, since CombatView's saved config only persists fontSize.) Fixed: raw mode now also sets show_damage=true/show_miss=true; condensed/gag now also set them false. Verified via emulation that all 3 modes now produce the correct show/hide decision for both hits and misses.
- 2026-07-11: worked through a large batch of new notes/screenshots (new character Uldek, new mage Vexgar, plus a high-level Vrokt fight) and found 4 more real things. (1) Confirmed the damage-flag-color fix live at last: Screenshot_20260711_103732.png shows Vrokt's fight rendering "Your pierce do GHASTLY things to..." with the real letter-alternating red/white effect and "=== OBLITERATE ===" bracket decoration -- closes that TODO item for good. (2) Found and fixed MyDSL_PlayersNear vacuuming unrelated broadcasts into the window -- same fragile catch-all-until-blank-line shape as every RightHere-on-look bug this session; a bare "S" (the Stunning proc's raw wire text) and a full condition sentence both got mixed in with real player entries. Added isPlayersNearBodyLine() (real body lines never end in punctuation and always have a wide column gap; broadcasts don't) -- verified via emulation against the exact contaminated sequence. (3) Found and fixed LiveView's health bars stuck at "-- (0%)" for Uldek: parseScoreLine() correctly parses "Hitpoints: 20 of 20", but MyDSL_DataBridge.lua's sync() never forwarded hp/max_hp/mana/max_mana/move/max_move from MyDSL.State.score into MyDSL.DB.score at all, even though LiveView's own L.data() already has a `live.hp or score.hp` fallback specifically for this case -- the fallback had nothing to fall back to. Fixed the missing mapping, verified via emulation end-to-end. (4) Found and fixed CreatureReference showing raw hex codes instead of colors: MyDSL_CreatureReference.lua was the only file anywhere using cecho()'s invalid "<#RRGGBB>"/"<reset>" tags instead of the "<r,g,b>"/"<r>" convention every other module uses successfully -- converted all 9 hex values and switched cecho->decho. Also investigated (not fixed, evidence doesn't support a code bug): the rabbit/weasel "not here" report looks like real wandering mobs leaving before the click resolves, not a name-matching bug (log shows a "walks <direction>" line for the exact mob immediately before its own "not here"); the Vexgar magic-missile report couldn't be reproduced from any available log; the "multiple mob health echos" report looks like the same already-open, deliberately-deferred renderSummary()-vs-PNP discussion item, not a new bug.
- 2026-07-11: started the command-surface retrofit (docs/TODO.md, held until combat was confirmed live -- lifted per Steven, combat's display/colors/group/procs all confirmed today). Confirmed PNP's real bare "toggle <module>" command directly in the native XML/source (DSL_PNP_Support.lua's Toggle Alias, "^toggle ([\w\.]+)...", each PNP module wiring its own name -- "battle", "affects", "moons", "ticktimer" -- into a shared dslpnp.toggle() helper). Added the same bare alias to CombatView/AffectsView/MoonWeather/TickView, reusing WindowRegistry's already-correct generic MyDSL.Windows.toggle() instead of inventing new visibility tracking. Found a real, separate gap along the way: CombatView had no show/hide/toggle function or alias at all (every other module already had one) -- added CV.show()/hide()/toggle(). Existing mydsl <module> aliases are untouched, this is purely additive. Verified via emulation: all 4 modules load cleanly with their toggle alias correctly registered. Also closed 2 of the retrofit's open verification questions: emco save/load confirmed (not just traced) to operate on the stale, hidden, original demonnic.container, not MyDSL's real chat window -- no fix needed since MyDSL_ChatWrapper.lua already has its own working mydsl chat save/reload settings persistence; and confirmed mydsl chat's actual sub-commands (status/save/reload/show/hide/clear/font/wrap/timestamp/rebuild/revive/echo/test) don't overlap with EMCO's native gag/lock/notify/title at all. The remaining retrofit item (trimming the mydsl prefix off Scan/Target/Group/CreatureReference) is a real muscle-memory-changing rename, held for an explicit approach decision rather than just doing it.
- 2026-07-11: finished the command-surface retrofit's last item -- trimmed the mydsl prefix off Scan/Target/Group/CreatureReference, per Steven ("muscle memory is not a concern... needs to be consistent across all commands, and human speak/readable"). Hard rename, old mydsl-prefixed forms removed entirely. Caught a real collision before shipping: bare "target" would have silently broken a genuine DSL swashbuckler combat skill ("target head/body/legs", confirmed via DSL_Helpfiles/target.txt) -- a bare "^target (.+)$" catch-all would have swallowed it before it ever reached the server. Checked scan/group/lore against their own real helpfiles too -- both safe (no real collision), only target needed a different word. Steven picked "focus" for the whole TargetView module, applied consistently to every sub-command, not just the one that had to change: focus <name>/clear/mobset .../playerset .../mobset reset/playerset reset/action .... GroupView -> group gag/ungag/quickset .../quickset reset. ScanView -> scan gag/ungag (mydsl righthere dump, a debug diagnostic, deliberately left alone). CreatureReference -> lore <name|hide|show>. Updated mydsl help's static list to match. Found and fixed a second real bug while renaming: the bare focus (.+)$ catch-all also matched every one of TargetView's own specific sub-commands since ".+" matches their arguments too -- Mudlet fires every alias whose pattern matches, not just the most specific one, so a real "focus mobset murder consider ..." command was ALSO silently setting the target to a mob literally named "mobset murder consider ..." every time. Existed under the old "mydsl target (.+)$" name too, just never surfaced -- the old guard only excluded the literal word "clear," missing the other three reserved words. Fixed by checking the first word against the full reserved list. Verified via emulation: all 4 modules load cleanly with the renamed aliases registered, and the reserved-word guard correctly excludes every sub-command while still setting real targets normally.
- 2026-07-11: corrected the "lore" alias same day, per Steven ("lore is a game command and sholdnt be used as an alias"). The earlier reasoning ("lore is passive/automatic, no typed syntax to collide with") was the wrong bar -- lore is still real DSL vocabulary (a skill name), and claiming it for a different purpose is exactly the confusion the retrofit is supposed to avoid, syntax collision or not. Renamed to "bestiary <name|hide|show>" (Steven's pick, confirmed not real DSL vocabulary anywhere in DSL_Helpfiles), updated mydsl help to match, verified via emulation. Also investigated Steven's poison question ("i see some echos from actions/skills but i didnt see the actual 'gets poisoned/shivers and suffer' successlines, are we gaging any actions in combat besides swings?") -- checked log/2026-07-11#12-15-16.html directly and confirmed nothing in our code gags this; no trigger recognizes poison-spell text at all, so it just passes through untouched like anything else unhandled. The confirmed "gets poisoned"/"shivers and suffers" text from earlier this session is specifically the weapon-proc version; Vexgar's "c poison" here is the spell-cast version, apparently different text. Found a real candidate for the spell's onset text right after a successful cast -- "A gnome in a protective heat suit looks very ill." -- doesn't match our condition ladder, only seen once so far, parked in the PAUSED section pending another confirmed cast rather than acted on from one occurrence.
- 2026-07-11: ran /code-review (8-angle, high effort) against the whole session's work (12 commits, base ce7ea20), then fixed everything it found. Critical: table.unpack() doesn't exist in Mudlet's real embedded Lua runtime (only bare unpack) -- confirmed by mounting the actual Mudlet AppImage and grepping its bundled mudlet-lua library directly (zero table.unpack anywhere, unpack(...) used throughout, matching every vendored PNP file's own convention) -- every emulation test this session passed using a standalone luajit binary that apparently provides table.unpack as an extra global Mudlet's sandbox doesn't have, so focus/group reset commands would have crashed live despite passing every test. Fixed both occurrences (MyDSL_TargetView.lua, MyDSL_GroupView.lua). Real reachable bug: toggle moons/toggle ticktimer could get stuck doing the opposite of what's needed after a reload, since MW.toggle()/V.toggle() decided from a local config.shown flag instead of WindowRegistry's real state -- confirmed loadState() bypasses both entirely, and V.config.shown has its own separate save file. Fixed by delegating to MyDSL.Windows.show/hide/toggle() (MoonWeather) and preferring the real registry state when available (TickView), verified via emulation against the exact desync scenario. Confirmed structural fragility: TargetView's focus catch-all guard was a hardcoded reserved-word list independent of the actual registered sub-commands -- the same desync had already happened once. Fixed by co-locating each sub-command's reserved word with its own alias registration instead of a separate list. Cleanup: combatMode's 3 near-identical if-blocks collapsed into one lookup table; new MyDSL.charName()/MyDSL.safeFileName()/MyDSL.copyArray() (MyDSL_DataLayer.lua) replace what had become 3-4 independent copies of the same logic across CombatView/TargetView/GroupView; docs/MyDSL_MudletAPIReference.md's dated bug-fix table got a note clarifying its command examples are pre-rename. All fixes verified via syntax check + emulation before commit.
- 2026-07-11: built a "normalize the look" visual-design pass for Steven, then wired it in for real. First presented 3 concrete visual directions (Refined Convergence, Terminal Purist, Zoned HUD) as an HTML Artifact with real Combat/Affects/Vitals/Target mockups, grounded in a full 13-window styling survey (found 6+ windows with zero background/border at all, Live/Tick duplicating the exact same hardcoded panel style as two independent copies, Location with its own private ad-hoc theme table, and MyDSL_ThemeEngine.lua sitting fully built but completely unconsumed -- zero callers of MyDSL.Theme.get() anywhere). After Steven reviewed 10 ChatGPT concept renders he'd generated separately and asked "can we make a few different themes off what you provided? and some from the gpt images?", built it for real: added 5 named presets to MyDSL_ThemeEngine.lua (the 3 Artifact options, plus obsidian_ember/arcane_midnight adapted from 2 of the ChatGPT renders that needed zero new art assets) with MyDSL.Theme.setTheme()/.list(), a shared (not character-bound) persistence file, and a theme/theme list/theme set <name> alias. Discovered MyDSL_WindowRegistry.lua's applyTheme() placeholder was flatly wrong -- its comment claimed Geyser.UserWindow doesn't support setStyleSheet, but Mudlet's real source (GeyserUserWindow.lua:91) confirms it does, wrapping the native setUserWindowStyleSheet() and documented as theming exactly "border and title area." Made applyTheme() real for all 14 UserWindow registry entries at once, which is what actually makes a single setTheme() call reach every window. Added MyDSL.Theme.styleConsole() and used it to theme the 8 windows that previously had no background/border/font at all (Affects, Combat, Target, Group, Scan, RightHere, PlayersNear, CreatureReference), then migrated the windows with existing bespoke styling (Live, Tick, Portrait, Location, MoonWeather) to pull chrome colors from the active theme, deliberately leaving semantic colors (HP/mana/move gradients, tick urgency palette, damage-tier colors) untouched since those carry meaning independent of visual theme. Chat's outer window chrome themes too via the same universal registry fix; its inner EMCO tab styling was left alone this pass (separate, more complex system). Verified with a dedicated emulation harness covering all 5 presets, zone differentiation, override precedence, and settings persistence across a simulated reload.
- 2026-07-11: confirmed the new ThemeEngine live in-game, after chasing down a false alarm first. Steven's first two screenshots (theme set refined_convergence, then theme set terminal_purist) showed zero visible change anywhere, including on Live/Tick which were definitely migrated to read from the theme -- looked like a real bug. Root cause traced to a stale mid-session reload, not the new code: every module in this codebase guards its event-handler setup with an "already installed" flag living on the persistent MyDSL.* globals, which survive a script-file reload by design (so Geyser windows don't get recreated on every save). Since those modules' installHandlers() had already run earlier in the same session, before today's edits existed on disk, reloading the .lua files didn't re-run them -- the brand-new "MyDSL.theme.changed" listeners never actually registered until a genuine process restart. Worth remembering for any future change that adds a new registerAnonymousEventHandler call to an already-initialized module. After Steven restarted Mudlet and retried, both themes "worked as intended" -- confirmed visually distinct, correct borders/colors/fonts. Closes the live-confirmation gap for all 5 presets (the other 3 share the identical code path with different color values).
- 2026-07-11: three visual follow-ups to the ThemeEngine work, per Steven's feedback after seeing it live. (1) Border thickness -- every theme's border was already at 1px, the CSS floor for a visible solid line, so "slimmer" meant color intensity, not width (confirmed via a clarifying question, Steven picked "reduce color intensity"): dimmed/desaturated every preset's border color (and zoned_hud's 4 zone border colors) roughly 35-40% darker plus reduced alpha, while leaving title/text colors at full brightness for legibility. (2) Removed the scrollbar from History and PlayersNear (MyDSL_RouteHelper.lua's shared getOrCreateConsole() had hardcoded scrollBar=true, inconsistent with Scan/RightHere/Target/Group which never had one). (3) Fixed window titles across the board: every window created via MyDSL.Windows.ensure() (Affects, Combat, Target, Group, Scan, RightHere, PlayersNear, CreatureReference, History, Chat) was still showing Mudlet's raw default title ("User window - DSL2 - MyDSL_Scan") because nothing ever called setTitle() on the returned object -- AffectsView and ChatWrapper both had title-setting code that looked like it should have worked but was actually dead (Affects' real branch returned before reaching its own setTitle call; Chat referenced MyDSL.Windows.setTitle/setFont, neither of which has ever existed on WindowRegistry, silently no-op'd forever behind a guard). Added a real setTitle() call at each window's actual creation point instead, matching Live/Tick/Portrait/Location's existing "-= Name =-" convention (CreatureReference titled "-= Bestiary =-" to match its current alias name). Verified via syntax check across all 9 touched files and a re-run of the ThemeEngine emulation harness (dimmed colors don't break zone differentiation, override precedence, or persistence).
- 2026-07-11: rebuilt MyDSL_LiveView.lua's whole layout (v1A14 -> v1A15) to actually show the score/character info Steven asked for -- stats, alignment, god, items, weight, gold/silver, TNL, armor, hit/dam, wimpy -- fitted inside the window's real confirmed pixel size (974x186, via a new pixel-size report added to "mydsl live status") rather than resizing it. Iterated the design through several Artifact mockups before writing any Lua, converging on Steven's own hand-sketched layout (Downloads/"liveview layout"): room title, terrain+exits on one line, a rule, then a 2-column x 7-row body -- left column has the HP/Mana/Move bars (now inline label+track+value instead of full-width with centered overlay text), a gap, identity (level/name/READY-or-FIGHTING/class/alignment/god), then two info rows; right column has STR-CON each paired with the exact combat/status info from Steven's sketch (Armor on STR, Hit/Dam on INT, Stance on WIS, TNL on DEX), a gap, then the Improve bar. Dropped the compact/full mode entirely, per Steven -- this is the one standard layout now. Found and fixed two real, previously-undiscovered bugs via the dedicated emulation test written for this rebuild: L.data()'s TNL computation only ever checked field names that have never existed (score.xpToLevel/xpToLevelText) instead of the real bridged score.tnl, so TNL silently showed "--" even in the old display; and confirmed the MyDSL_DataBridge.lua hit/dam/armor key-name bug (see the ThemeEngine entry) needed fixing first for any of this to have real data to show. Removed 3 functions that went dead once the old time/XP-total display was replaced. All verified via emulation: correct HTML/colors against sample data, alignment color-codes correctly, the status pill swaps on fighting state, zero-max-stat and empty-score-table (fresh login) don't crash, theme switching still restyles cleanly.
- 2026-07-11: four fixes to the new LiveView v1A15 layout from Steven's first live screenshot. Reverted the bar fill's corner rounding back to fully-rounded (matching the track underneath) -- the brief left-only rounding looked mismatched at 100% fill, which read as "changed from the previous style." Bumped font size +1pt again for identity/info/attribute rows and bar label/value text specifically (room title/terrain/exits left untouched, per exact request). Made the exits MiniConsole resize to roughly match the exits text length instead of sitting in a fixed-width box, since MiniConsole has no setStyleSheet to strip its native Qt frame directly. Found and fixed a real, confirmed bug along the way: MyDSL_DataBridge.lua's sync() unconditionally rebuilt the whole MyDSL.DB.room table on every call, silently wiping the exitsColoredSource/exitsLine fields LiveView's colored-exits capture sets -- since sync() runs far more often than a fresh room description scrolls by, the flag cleared within moments of being set, so the next render tick always fell back to the plain-white version. This is why the exits line "changed from original game text to white text" -- the colored capture was working, just getting immediately clobbered. Fixed by preserving those fields across the rebuild, same pattern already used for DB.tick.
- 2026-07-11: polish pass on the new LiveView layout from a cropped screenshot of just the body content, per Steven ("size the text and spacing to make it look more filled... color need to increase... no big position changes, just size and spacing and color"). Bumped row/bar font sizes +1pt again, widened row height slightly (same row slot positions, just fuller), widened inter-field spacing. Added a real feature along with the polish: Items/Weight now redden at >=95% of max and amber at >=80% (fullnessColor(), same 2-tier shape AffectsView's colorForDuration() already uses elsewhere), per Steven's explicit "objects like item weight need to go red when close to max." Also colored several previously-plain fields for more visual distinction: Pos'n (Flying/Sitting-Resting/Sleeping), Stance (Offensive/Defensive), Hit/Dam (green/red by sign), Bank/Silver/QPoints/Armor/TNL each a distinct tint. Verified via emulation: fullness thresholds and all new color-coding trigger correctly against sample and edge-case data.
- 2026-07-11: five more LiveView changes from a full-window screenshot. Made every text category's font size independently adjustable (new L.config.infoFont/terrainFont alongside the already-separate titleFont/barFont, each with its own "mydsl live <x>font <n>" alias) per Steven's request to adjust informational/title/terrain text sizes separately. Widened the HP/Mana/Move bar tracks (28% -> 36%) to fill the gap between label and numbers, per his explicit ask. Found and fixed two real bugs: terrain text was always a flat cyan regardless of actual terrain type (now color-codes forest/desert/water/mountain/cave/city/snow/plains/etc, with a distinct new fallback for unrecognized terrain instead of reusing that same flat cyan) and the room title was rendering white instead of the theme's color because a hardcoded inline color in the echo() call was competing with the theme-driven stylesheet color already set on the widget -- same root-cause shape (inline HTML silently overriding a widget-level stylesheet property) also found in terrain's own font-size handling and fixed the same way. Also added Hit/Dam's B:/P: display (both base and practiced values, matching the real score command's own labeling) -- the data was already correctly bridged by today's earlier DataBridge fix, LiveView's own L.data() just never read it. All verified via emulation.
- 2026-07-11: real bug found and fixed, per Steven ("font didnt chagne that i noticed") after testing the new infofont command live. mydsl live infofont <n> correctly updated config and re-ran applyStyles()/render() (confirmed via console output echoing the new value), but the identity/info/attribute rows never actually visibly resized -- because kv()/identityLine()/attrLine() only ever set color/font-weight inline in their HTML spans and relied on the widget's own setStyleSheet() for font size, which Qt's rich-text renderer doesn't reliably inherit for HTML content. Confirmed by contrast: roomTitle/terrainBadge() already set font-size inline explicitly and did resize correctly with titlefont/terrainfont. Fixed by threading an inline font-size (reading L.config.infoFont live) onto every span these 3 functions generate. Verified via emulation that the actual rendered HTML changes size correctly when setInfoFont() runs.
- 2026-07-11: built a new MyDSL_AlterformView.lua timer, per Steven ("create an alterform timer like tick to go beside it when we have the alterform affect"). Confirmed alterform is a normal tracked affect via log-corpus grep (already shows in the Affects window, just no dedicated widget); checked PNP's own DSL_PNP_Statusbar.alterform.lua first per the reuse mandate -- it only exposes duration via a prompt token, nothing to port directly. New window mirrors MyDSL_TickView.lua's exact structure, positioned beside Tick, with a genuine "not active" state and its own violet accent. Added MyDSL.Affects.getRemaining(name) as the public read API instead of having the new module re-parse anything. While wiring this up, found and fixed a significant real bug: MyDSL_AffectsView.lua's real-time conversion for every tracked affect (not just alterform) was using the combat-tick average (~40s) to convert duration cycles into real seconds, but a DSL affect cycle is confirmed via 80 unique real log examples (zero deviation) to be exactly 1800 seconds (30 minutes) -- completely unrelated units that just share the word "cycle". This was invisible before since the default Affects display mode never showed the converted estimate, only the non-default time/both modes did. Fixed the conversion and added hour formatting to both MyDSL_AffectsView.lua and the new timer. Verified via two dedicated emulation harnesses that the corrected conversion matches DSL's own reported hours exactly. Needs a dofile() Script entry added before the new file can load.
- 2026-07-11: alterform follow-ups from Steven's first live test. Real bug caught by his own "focus status" test: forgot to reserve "status" as a word the "focus (.+)$" catch-all should skip, so it also fired and set the live target to a mob literally named "status" -- same bug-class the _focusReserved mechanism exists to prevent, fixed the same way (co-located reservation next to the new alias). Also changed the Alterform window to auto-hide when the affect isn't active instead of showing a dim "OFF" state, per Steven's explicit direction -- a manual toggle/hide still wins over the affect's active state. Verified via emulation.
- 2026-07-11: rebuilt TargetView per Steven's own rough sketch (Downloads/targetview.txt), fitted to the real confirmed 422x186 size via a new "focus status" command (same pixel-report pattern as Live's). Found two real things while researching creaturelore data for it: the live creaturelore command's actual text includes magic/damage/immunities/resistances/vulnerabilities/affects, none of which MyDSL.parseCreatureLoreLine() was capturing (only race/align/gold/hp were); and Kills/Avg XP/Last XP from Steven's sketch aren't part of creaturelore's output at all -- confirmed nothing anywhere tracks per-creature kills/XP, the only trace is a month-old dead data file. Asked Steven directly rather than guess -- his call was to build with what's real now and skip Kills/XP. Extended the parser (verified regex-by-regex against real log lines) and added a creaturelore stat block to Target's display that only shows when the lore record's key matches the live target, so stale lore from a different creature never displays under the wrong name. Icon is a text placeholder for now, not a real image widget, since Steven said that's for later. Every existing feature (M/P toggle, DslColors friend/enemy tag, 6-button grid, consider history) preserved unchanged. Verified via emulation.
- 2026-07-11: converted MyDSL_AlterformView from a detachable Geyser.UserWindow to an Adjustable.Container anchored in the main console, per Steven ("alterform window change to same as moonweather, we will keep it inside the main window for layout cleanness") -- F.ensureUI() now builds the container directly, matching MyDSL_MoonWeather.lua's _buildUI() exactly (percentage geometry, lockStyle="padding", transparent/borderless adjLabelstyle, blanked title). Verified via an updated emulation harness stubbing Adjustable.Container/MyDSL.Layout.get -- all prior active/inactive/auto-hide behavior still passes.
- 2026-07-11: fixed TargetView's layout to actually match the approved targetview-design mockup, per Steven's follow-up clarification ("the layout of targetview does not mach the layout visuals of the targetview-design web page we created"). Two real deviations found: the [M]/[P] toggle and [Clear] button were rendered before the name instead of after it (mockup groups identity left, controls right), and no divider lines were rendered anywhere despite the mockup's clear dashed rules. Fixed by reordering the header row and adding a new tvRule() helper called in 3 spots (after the header, before the Race/Align row, between the Dmg row and the Immune/Resist row). Verified via the existing emulation harness.
- 2026-07-11: TargetView polish round 2, per Steven's follow-up feedback. Added font adjustability (TV.config.fontSize + TV.setFont() + "focus font <n>" alias, matching CombatView's CV.setFont() pattern). Fixed a real gap: creaturelore stats now populate whenever a previously-lore'd creature is re-selected as the target, not just the single most-recently-lore'd one -- added MyDSL.State.creatureLoreCache (MyDSL_DataLayer.lua), keyed by normalized name, populated in endCreatureLore(); TargetView's stat block now reads the cache by the current target's key instead of the old single MyDSL.State.creaturelore slot (left untouched for MyDSL_CreatureReference.lua, which still uses it as-is). Moved the [M]/[P] toggle and Clear button back to the left of the icon/name (reverting the same-day mockup-matching reorder, per Steven's explicit direction), changed Clear to render as "[X]" instead of the word, and removed the divider line directly below the header row (the other 2 stat-block dividers stay). Verified via an updated emulation harness covering all 4 changes, including the actual cache fix (lore creature A, switch to never-lore'd B, switch back to A, confirm A's stats reappear).
- 2026-07-11: TargetView round 3, per Steven's follow-up ("creaturelore should be persistent and tabled... change target window to focus to match commands... make it look pretty like the one we designed"). Built the real MyDSL_CreatureLore.lua persistent DB (new file, disk-backed, shared across characters) -- MyDSL_DataLayer.lua's endCreatureLore() and MyDSL_CreatureReference.lua's Bestiary window both already called into MyDSL.CreatureLore.merge()/.get(), guarded against the module never actually existing (a confirmed dead reference from an earlier session); this file is the real implementation, superseding the same-day session-only creatureLoreCache. Renamed the Target window to Focus (title + WindowRegistry/LayoutEngine/ThemeEngine keys + log category) to match its existing "focus <verb>" command surface, same title/command-name divergence precedent as the Bestiary window; left the underlying Lua namespace (MyDSL.Target/MyDSL.TargetView) untouched. Added MyDSL.getTargetCondition() (MyDSL_DataLayer.lua), a public read-only API over the existing combat-condition tracker, and wired a real HP bar into TargetView from it (block-character bar + real percent range, independent of creaturelore data) -- this is the actual "healthbars that work on the enemies health message" ask, previously unbuilt. Confirmed via Mudlet's real decho/MiniConsole behavior that the mockup's CSS pill-button styling can't be reproduced without losing the button grid's real click functionality (only Label supports rich HTML, only MiniConsole supports dechoLink), so left the button grid as-is and focused the visual work on the one genuinely reproducible piece (the HP bar), now wired to real data. Verified via 3 dedicated harnesses (CreatureLore DB round-trip including a simulated fresh session, getTargetCondition()'s exact 7-tier mapping, and TargetView's rendering of all combinations of live-condition/lore presence).
- 2026-07-11: fixed a critical real bug found live from Steven's own screenshot -- a real "creaturelore" capture displayed correctly in the main console but never updated the Focus window or the persistent DB. Root cause: MyDSL_DataLayer.lua's endCreatureLore() still had a stray write to MyDSL.State.creatureLoreCache[key], a session-only cache whose initializer was correctly removed earlier the same day but this one write site was missed -- every real capture threw "attempt to index a nil value" and aborted before ever reaching MyDSL.CreatureLore.merge()/MyDSL.emit(). Fixed by deleting the dead write. Added a new end-to-end regression test that loads the real MyDSL_DataLayer.lua (not a hand-copied replica) and replays the exact live capture -- confirmed it reproduces the identical crash against the pre-fix code and passes clean after, closing a real gap where the existing test suite only ever exercised the parsing regexes or a stubbed merge() in isolation, never the real integrated pipeline.
- 2026-07-11: TargetView round 4, per Steven's follow-up ("focus is better. healthbar is still missing. id like the buttons to line up and be uniform. could we make a more graphical focus window... maybe research buttons in mudlet manual?"). Padded every button label in the current set to a uniform width so columns line up. Added a diagnostic to "focus status" to help pin down the still-missing healthbar (most likely explanation: the fight had already ended by the time the screenshot was taken, since the underlying getTargetCondition() logic checked out correctly in isolation). Corrected an earlier wrong claim from the same day's Round 3 entry: read Mudlet's actual bundled source (GeyserButton.lua/GeyserLabel.lua) and confirmed Geyser.Label has real native click-handling (setClickCallback etc.) and Geyser.Button is a ready-made, fully CSS-styleable clickable widget -- a genuinely graphical Focus window (real pill buttons, a real colored HP bar) built as an Adjustable.Container like MoonWeather/Alterform is achievable, just not yet built pending Steven's go-ahead.
- 2026-07-11: TargetView round 5, per Steven ("so we can or cant have graphical buttons in the focus window as is without changing container? Just the buttons, keep it as-is for now"). Converted the [M]/[P] toggle, [X] clear, and all 6 action-grid slots from dechoLink() text to real Geyser.Button widgets (dark pill background, colored border per action's semantic color, inline font-size/color HTML labels), created once and updated in place by render(). Split the single MiniConsole into 2 (header text console + stats console) since a rectangular text console can't leave a hole for the button grid to sit in; the window stays a UserWindow, unconverted, per Steven's explicit direction. Everything else (identity text, creaturelore stats, live HP bar, consider-lines) is unchanged. Verified via a rewritten emulation harness with a real Geyser.Button stub covering creation, show/hide per target state, label/color/tooltip/click-handler correctness, the group-member attack-guard now hiding buttons outright, font-size propagation into button HTML, and clean state on target switch.
- 2026-07-11: TargetView round 6, from Steven's screenshot + Kien's logs. Confirmed the still-missing healthbar was correctly showing "no data" (focus status's diagnostic showed active_entry=false -- no live fight at that moment, not a bug). Found and fixed a real font-persistence bug: targetview_config_Kien.lua on disk correctly had fontSize=9 after "focus font 9," so the save path always worked, but TV.init() built the header/stats consoles using whatever fontSize was in memory before loadConfig() ever pulled the real value off disk, and nothing re-applied it afterward -- a fresh session's loaded value never reached the actual widgets. Fixed by extracting applyFontSize() and calling it after every loadConfig() call site. Halved the button grid's row height (freed space handed to the stats console, which was getting clipped) and removed the "[img]" icon text placeholder entirely, per Steven. Verified via a new regression test reproducing the exact persistence bug (confirmed it fails pre-fix, passes post-fix) plus the existing suite updated for the new layout/missing icon.
- 2026-07-11: TargetView round 7, from a real mid-fight screenshot + Kien's logs ("bar isnt working during a fight either"). Found and fixed a real bug distinct from the earlier "no data" case: combat-condition data was updating correctly (confirmed via the screenshot's own repeated "is in awful condition" lines for the selected target) but nothing ever told the Focus window to re-render while a fight was ongoing -- TV.render() was only wired to target-selection, creaturelore, and theme-change events, never to combat data changing, so the window looked frozen from the moment the target was set for the rest of the fight. Fixed by subscribing to MyDSL.Timers.Slow, the same shared 1/sec heartbeat LiveView/AffectsView already use for this exact class of problem. Verified via a new regression test that changes condition data and fires only the heartbeat (no direct render() call, no target change) -- confirmed it fails against the pre-fix code and passes after.
- 2026-07-11: TargetView round 8, per Steven ("update the health smoothly... excellent through to awful to dead. then clear target, or populate with next in room mob from scan, maybe datalayer collects the targets health?"). Confirmed the "smooth tracking" and "DataLayer collects health" parts already existed (Round 7's heartbeat + MyDSL.getTargetCondition()). Built the death-handling part: asked Steven directly which behavior he wanted (auto-advance vs. always-clear); he chose auto-advance. Added a new MyDSL.combat.died event in MyDSL_DataLayer.lua's parseCombatDeathLine() (distinct from the generic combat.ended, which also fires for flee/rescue), and wired TargetView to auto-select another mob from Right Here/Scan on the current target's death, or clear if none remain -- still never sends a game command. Verified via 2 new test harnesses covering the real DataLayer event-raising logic and TargetView's 3 real scenarios (auto-advance, clear, and an unrelated kill not touching the current target).
- 2026-07-11: TargetView round 9, per Steven ("instead of the healthbar on a new line, can it overlay the mobs name and the mob name be centered in the bar... the creaturelor is missing a couple fields and the align field needs tweaking... we arent capturing the offensive tactics and level of the mobs"). Merged the header name and the live health bar into one real Geyser.Label ("nameplate") whose background is a genuine Qt gradient health-fill with a hard color edge, name/relation/percent centered on top -- replacing the separate plain-text header console and the separate "HP:" stats row. Fixed 3 creaturelore parsing gaps: align narrowed to just good/evil/neutral (confirmed real phrasing varies more than the old raw capture kept), added Offensive Tactics parsing, and added the "level" field (DSL's own "cycle of training" number, displayed as Lvl:). All persisted via MyDSL_CreatureLore.lua's FIELDS list and wired into both Focus's stats block and the Bestiary window. Verified via 3 test harnesses covering the real parsing against exact log lines, DB round-trip of the new fields, and the nameplate's gradient/text rendering.
- 2026-07-11: fixed a critical regression that broke profile load, found live from Steven's error paste ("attempt to call method 'setState' (a nil value)" at MyDSL_TargetView.lua:684). Root cause: the nameplate (a Geyser.Label) was updated using the Button-only .style/.msg + :setState() pattern copy-pasted from updateActionButton(), but :setState() only exists on Geyser.Button, not Label, despite Button being Label-based -- confirmed via Mudlet's real bundled source. Fixed by calling Label's own real :setStyleSheet()/:echo() methods directly. Also fixed a real gap in the test suite that let this ship: the Geyser.Label/Button stub gave both classes an identical fake setState(), masking the real API difference -- corrected the stub to mirror Mudlet's actual split, confirmed it now reproduces the exact live error when the bug is reintroduced.
- 2026-07-11: investigated Steven's report that Focus's font and creaturelore aren't persisting across a reload. Could not reproduce: confirmed via direct disk inspection that both save correctly, and built 2 full end-to-end boot simulations (real dofile order, real files, both with character known immediately and with the more realistic delayed-GMCP ordering) -- both pass, code is logically correct in every constructible scenario. Also ruled out a directory-sync issue (confirmed the two paths this session edits under are the same underlying files). Matched CL.load()/TV.loadConfig() to the exact io.open()-precheck pattern MyDSL_ThemeEngine.lua/MyDSL_WindowRegistry.lua already use (per Steven's explicit ask), and added loud echo() boot-time confirmation lines (debugc() alone only shows in Mudlet's separate Errors console). Confirmed there's no existing "item/reference module" in the codebase to route through -- MyDSL/data/db_creatures.lua etc. are inert, unwired data dumps from the not-yet-audited Shattered Archive tooling; MyDSL_CreatureLore.lua already is the shared reference store for both Focus and the Bestiary window. Needs a genuine full Mudlet restart to get real evidence via the new boot echoes.
- 2026-07-11: architecture fix for font-size persistence, per Steven ("something isnt saving the settings, per profile not user remember for the layout"). After repeated reports that Focus's and History's font settings weren't surviving a reload -- despite both having the standard character-identification re-load fix (structurally identical to MyDSL_ChatWrapper.lua's confirmed-working pattern) and passing every constructed test -- moved font-size persistence entirely out of each module's own character-bound file and into a new shared, profile-level store in MyDSL_WindowRegistry.lua (MyDSL.Windows.getFontSize()/setFontSize(), one file MyDSL_windowfonts.lua), matching how Layout/Theme already persist. This removes the character-name dependency, and the whole class of timing bug being chased, rather than continuing to hunt an unreproducible root cause. MyDSL_TargetView.lua and MyDSL_RouteHelper.lua (History) both migrated; their old per-character font files/reload handlers removed. Verified via 3 test harnesses: a new dedicated WindowRegistry font-store test (set/get round-trip, survives a simulated fresh session, confirmed no gmcp/character interaction anywhere), an updated TargetView harness proving a fresh session picks up the shared value immediately with no reload event needed, and a rewritten full-boot simulation proving the font is correct even before the character is ever identified.
- 2026-07-11: added a 3-way font-size diagnostic ("focus status" extended, new "mydsl history status") to answer Steven's direct question ("are the settings loading at creating from save files or they saving and never reading/updating?") -- reports disk value (re-read fresh, not from memory), in-memory value, and Mudlet's own real live getFontSize() on the actual widget, so a live run pinpoints exactly which stage is wrong instead of guessing further. Confirmed via direct disk inspection that the new shared MyDSL_windowfonts.lua file has the correct, freshly-written values for both Focus and History, proving the save side works with the new architecture.
- 2026-07-11: CRITICAL project-wide fix -- table.load() was being called wrong in almost every persistence function in the codebase. Steven's direct question ("are the settings loading at creating from save files or they saving and never reading/updating?") led to checking Mudlet's own bundled source directly: table.load(file, target) returns NOTHING -- it unpickles saved data into an explicit second-argument table (or _G if none given), confirmed independently via PNP's and EMCO's own real source both always passing a destination table. Almost every "local data = table.load(path)" call in this codebase (no second argument) was silently getting nil back, every time, forever -- meaning theme persistence, window visibility, the character-data restore (MyDSL.load()), and several other config loads never actually survived a real restart despite being "confirmed working" (which only ever verified in-session behavior, never a genuine restart). Fixed in MyDSL_DataLayer.lua, MyDSL_ThemeEngine.lua, MyDSL_WindowRegistry.lua (both visibility state and the new font store), MyDSL_CreatureLore.lua, MyDSL_TargetView.lua, MyDSL_GroupView.lua, MyDSL_CombatView.lua, MyDSL_LayoutEngine.lua, MyDSL_PromptView.lua. Also found why this session's own tests never caught it: every prior test used a hand-rolled table.save/table.load stub that (unlike the real thing) incorrectly returned the loaded data directly. Built a faithful replica of Mudlet's actual algorithm (copied from its bundled source, confirmed to reproduce the exact real on-disk file format) and re-ran every persistence test against it -- several failed against the old code and pass against the fix, proving both the bug and the repair. MyDSL_TickView.lua/MyDSL_LiveView.lua/MyDSL_AlterformView.lua checked and confirmed unaffected (they use their own io.open+dofile persistence instead).
- 2026-07-11: removed Combat window's scrollbar (per Steven) -- turned out to be the original, never-touched value from Combat's first version, missed by the earlier pass that removed scrollbars from History/PlayersNear/Scan/RightHere/Target/Group for consistency. Also experimentally extended MyDSL.Theme.panelCSS() with a QDockWidget::title CSS rule, reusing each preset's already-defined (previously unwired) titleColor/titleBgColor/titleFont/titleFontSize fields, to test whether Mudlet's native UserWindow title bars can be colored and shrunk (tight 2px vertical padding) via CSS -- applied automatically through the existing applyTheme() path, needs a live screenshot to confirm since Mudlet's C++ implementation of setUserWindowStyleSheet() isn't visible from the Lua side to verify in advance.
- 2026-07-11: title-bar CSS round 2. Confirmed via pixel-sampling a live screenshot that round 1's approach (appending QDockWidget::title directly onto panelCSS()'s bare declarations in one string) did not work -- every window's title bar background was Mudlet's exact default gray (53,53,53) and text was plain white, while the window border color was correctly themed, proving the mixing of bare declarations with a trailing selector block in one Qt stylesheet string is invalid. Split into two functions: panelCSS() reverted to its original bare-declarations-only form (used by Label panels), new windowChromeCSS() is all selector-qualified rules (QDockWidget { ... } QDockWidget::title { ... }), wired into applyTheme() for real native UserWindow objects only. Added a test assertion confirming windowChromeCSS()'s output has no bare declarations before its first selector brace, so this class of mistake can't silently regress again. Still unverified whether Mudlet's UserWindow chrome responds to QDockWidget selectors at all -- needs another live screenshot.
- 2026-07-12: researched round 2's title-bar CSS against Mudlet's real C++ source before another live test, per Steven's explicit ask. Confirmed via src/TDockWidget.h (Mudlet/Mudlet GitHub) that TDockWidget is a thin QDockWidget subclass with no custom title-bar widget and no paintEvent override, so QDockWidget/QDockWidget::title selectors are structurally correct; independently confirmed via GitHub PR #4046 (the PR that added setUserWindowStyle()/Geyser :setStyleSheet() support for UserWindows), whose own official example uses the exact same two-full-selector-block shape windowChromeCSS() already produces. That PR's discussion also flagged a real Qt/Mudlet limitation: border styling on a UserWindow only persists while floating, not while docked -- title-bar styling alone works in both states. Steven then tested live and confirmed exactly that split (floating: works: docked: doesn't).
- 2026-07-12: removed the title-bar/border CSS experiment entirely, per Steven ("should we revert to before those titlebar and bordersizing changes?"). Since MyDSL's UserWindows normally run docked and the feature is confirmed inert in that state, kept it around only as dead code with no upside. Deleted MyDSL.Theme.windowChromeCSS() (MyDSL_ThemeEngine.lua) and reverted WindowRegistry's applyTheme() back to its pre-experiment form (plain panelCSS() call, no windowChromeCSS()/fallback branching). titleCSS() (predates this experiment, still unused elsewhere) and the preset titleColor/titleBgColor/titleFont/titleFontSize fields (predate this experiment too) left untouched. Combat's scrollbar fix from the same original ask is unrelated and unaffected.
- 2026-07-12: docs audit before a live-testing session, per Steven ("keep scanning" for stale claims). Found and fixed one real doc/reality mismatch: `docs/TODO.md` claimed `MyDSL_CreatureLore.lua` still needed a `dofile()` Script entry -- checked the actual most-recently-modified Mudlet snapshot (`current/autosave.xml`) and confirmed it's already wired and loading, between MyDSL_TargetView.lua and MyDSL_CreatureReference.lua. Spot-checked several other claims against real source and confirmed them accurate: MyDSL_PromptSetup.lua genuinely not wired, ChatWrapper's tab CSS genuinely hardcoded, MyDSL.State.equipment genuinely unread by any display window, the 4 "dead-but-registered" windows (Inventory/Equipment/AsciiMap/Banner) aren't in MyDSL_WindowRegistry.lua's registry at all. Also found a harmless stray file worth noting: MyDSL_creaturelore.lua (lowercase, profile root, 52KB, dated 2026-06-06) is leftover DSL1 carry-over data, not a module, superseded by MyDSL/creaturelore_db.lua -- tracked in git but unused, left alone pending Steven's call on deleting it.
- 2026-07-12: live-confirmed a large backlog in one sitting after the docs audit above. Steven confirmed: (1) Focus window -- nameplate health bar, creaturelore stats, action buttons, and auto-advance/clear on target death all working; (2) the critical table.load() persistence fix survives a genuine full Mudlet restart -- theme, window visibility, Focus/History font size, Focus button config, Group quickset, and window layout all came back correctly; (3) Combat's scrollbar stays gone through real fights, and Combat/History font size persists; (4) GroupView's follower-not-showing bug (the leading-space level-field fix) holds -- group members display correctly now. Corresponding TODO.md entries pruned per its own stated convention (resolved items get removed, not archived -- this changelog is the permanent record).
- 2026-07-12: continued the live-confirmation pass. Steven confirmed LiveView's v1A15 layout (including exits-color persistence across room changes) and AffectsView's real-time countdown are both working. Fixed a real bug Steven flagged from notes_utf8.txt while testing the Improve countdown: the Improve bar's percent/skill text ("<skill> NN%") lived in a 7%-wide slot past the track's right edge, sized for HP/Mana/Move's short numbers, and could run off the window for longer skill names. Restored the pre-v1A15 centered-text-on-the-fill look for the Improve bar specifically (new styleBarNumCentered(), MyDSL_LiveView.lua, and widened its num field to overlay the track instead of sitting past it) -- Steven's stated preference ("put the text on the improve bar, that was the old design"). HP/Mana/Move bars untouched. Not yet live-confirmed.
- 2026-07-12: reviewed MyDSL_CharacterAssist.lua in detail to give Steven concrete test direction for the still-open rearm/spellup/blind-vision confirmation item. Confirmed only 1 of 6 disarm-trigger patterns (weapon DISARMS / grabs / controls, shield x3) is corpus-confirmed real DSL2 text ("<mob> DISARMS you and sends your weapon flying!"); the other 5 are PNP-ported guesses, comma made optional rather than picked, still unconfirmed. Confirmed the equipment parser's exact target format against a real log sample (log/830am_2465hp_210m_406mv_U_Defens, "<worn on finger> (Glowing) (Humming) a giant ring of smashing" etc.) -- multi-flag items parse correctly as written, no code change needed there, "confirm" just means checking `MyDSL.State.equipment.slots` actually populates after `eq` since no display window reads it yet.
- 2026-07-12: fixed a real bug found live immediately after the Improve-bar overlay fix (Steven: "reloaded improve bar looks good except i dont see the countdown now, just skill and percent"). L.data() already computed the live-ticking "(Mm SSs)" countdown into d.improveText via improveLiveText() -- built 2026-07-11 for exactly this -- but the render call rebuilt a bare "skill NN%" string from d.improveSkill/d.improvePercent instead of using it, so the countdown was computed every render and simply never displayed. Fixed by using d.improveText when present (MyDSL_LiveView.lua). Not yet live-confirmed; the combined "skill NN% (Mm SSs)" string is longer than plain "skill NN%" and may need the bar widened further if it clips.
- 2026-07-12: confirmed live -- Improve bar's countdown now displays and ticks down correctly ("peek 77% (24m53s)" in Steven's screenshot). Widened the bar's track (18% -> 26% of window width) per his follow-up ("lets make the bar longer(wider) since we have room for longer skill names now") -- now runs to 98%, matching the STR/INT/WIS/DEX/CON attribute column's right edge directly above it, using the room visible as empty space in the screenshot.
- 2026-07-12: Improve bar confirmed live at the widened size ("beautiful and functional" -- Steven). Closes the whole Improve-bar thread: text-overflow fix, countdown-not-displaying fix, and the width increase, all confirmed working together. Pruned from TODO.md.
- 2026-07-12: fixed a real bug found live in AlterformView (Steven: "everything seems to work, just the window needs to not be visible unless the alterform affect is active. you can see the min/close buttons"). Read Mudlet's actual bundled GeyserAdjustableContainer.lua (via the mounted AppImage) and confirmed the constructor's lockStyle="padding" field has done nothing since it was written: Mudlet only calls lockContainer() internally if locked=true is ALSO passed at creation (never was), and "padding" isn't even a real lockStyle name ("standard"/"border"/"full"/"light" are the only valid ones) -- it silently fell back to fully unlocked, meaning the min/restore/close/lock/save/load buttons were always live and visible regardless of the countdown's own active-based show/hide state. Fixed in MyDSL_AlterformView.lua by calling the real API, lockContainer("light"), after creation -- Mudlet's own documented style for hiding just the min/restore and close labels without affecting borders/margin. MyDSL_MoonWeather.lua has the identical dead lockStyle="padding" pattern and likely the same bug -- flagged in TODO.md, not fixed yet pending Steven's confirmation he wants it there too.
